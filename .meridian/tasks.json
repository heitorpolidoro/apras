[
  {
    "id": "APRAS-1",
    "title": "Implement password recovery flow (forgot password / reset password) in frontend and backend",
    "status": "done",
    "justification": "Implemented JWT password reset token mechanism, forgot/reset password API endpoints, forgot/reset frontend screens, and updated user table checks.",
    "running": false,
    "completed_at": null,
    "priority": "medium"
  },
  {
    "id": "APRAS-2",
    "title": "Refactor task visibility from boolean flag to dynamic UserType-based targeting",
    "status": "done",
    "justification": "Successfully implemented many-to-many User-UserType relationships, replaced manager_visible with visible_to_id in Task, created and ran Alembic migrations on prod database, updated backend RBAC checks/endpoints, and refactored the frontend dashboard checklist and task creation/edit select dropdown. Fully tested and deployed.",
    "running": false,
    "completed_at": null,
    "priority": "medium"
  },
  {
    "id": "APRAS-3",
    "title": "Replace all inline Alerts with modal dialogs on the frontend",
    "status": "done",
    "justification": "Created premium reusable AlertModal component with backdrop blur and animations. Replaced all inline Alert banners across 8 pages and verified 100% passing tests.",
    "running": false,
    "completed_at": null,
    "priority": "medium"
  },
  {
    "id": "APRAS-4",
    "title": "Add mandatory CPF field on user signup with mathematical validation",
    "status": "done",
    "justification": "Added mandatory CPF field to User SQLModel, implemented Pydantic check-digit validation, generated Alembic migration with automatic valid placeholder CPFs for existing database rows, added masked CPF input to SignupPage and read-only CPF display in AdminUserDashboard, and verified 100% test coverage.",
    "running": false,
    "completed_at": null,
    "priority": "medium"
  },
  {
    "id": "APRAS-5",
    "title": "Allow administrators to simulate/impersonate other roles and user types in frontend",
    "status": "done",
    "justification": "Implemented frontend-only role/UserType simulation per docs/superpowers/specs/2026-08-10-admin-role-simulation-design.md: SimulationContext, useEffectiveIdentity hook, permission-mirroring functions for task visibility/edit gating, an Axios interceptor blocking all mutations while simulating, and Navbar controls + persistent banner. 366 frontend tests passing. Merged to master (e0582d4) and pushed.",
    "running": false,
    "completed_at": null,
    "priority": "medium"
  },
  {
    "id": "APRAS-6",
    "title": "Add address, block/lot, and phone fields to user registration and administration",
    "status": "done",
    "justification": "Scope reduced during brainstorming to data-layer-only (SQLModel, Alembic migration 0005_add_profile_fields_to_user, Pydantic schemas, frontend TS type) per docs/superpowers/specs/2026-08-10-user-profile-fields-design.md. UI work split into user-profile-fields-ui (backlog). 165 backend tests passing (96.23% coverage), 366 frontend tests passing. Merged to master (b003f6e) and pushed.",
    "running": false,
    "completed_at": null,
    "priority": "medium"
  },
  {
    "id": "APRAS-7",
    "title": "Build UI to view/edit user phone and address (dedicated contact-info screen for Administrator/Manager)",
    "status": "done",
    "justification": "Rescoped on 2026-08-27 to drop block_lot (superseded by Lot/Resident, APRAS-15/16). Independent code review and independent QA both APPROVED with no blocking findings — QA verified live via curl that a Manager sending role/is_active/user_type_ids alongside phone/address cannot escalate them (fields stay unchanged on the target user). Reused the existing ProtectedRoute requiredRoles array prop from a later task instead of a redundant mechanism. Backend 353 passed/3 skipped/96.98% coverage, frontend 511 passed — independently reproduced. Merged to master (4ccce9a) and pushed. QA also surfaced an unrelated latent bug (tracked as APRAS-30): UserRead's cpf validator can throw ResponseValidationError/500 for a non-canonical CPF on serialization, affecting GET /users/ and PATCH /users/{id} too.",
    "expected_results": [
      "PATCH /users/{id}/contact-info endpoint restricted to Administrator/Manager, accepting only phone/address",
      "New /users/contact-info page listing users with editable phone/address, gated to Administrator/Manager via ProtectedRoute",
      "AdminUserDashboard, SignupPage, and the existing PATCH /users/{id} endpoint remain untouched"
    ],
    "blockedBy": [],
    "spec_path": "docs/superpowers/specs/2026-08-10-user-profile-fields-ui-design.md",
    "spec_iterations": 1,
    "qa_iterations": 1,
    "last_review_findings": [],
    "created_at": "2026-08-10T00:00:00-03:00",
    "updated_at": "2026-08-27T16:05:00-03:00",
    "running": false,
    "completed_at": "2026-08-27T16:05:00-03:00",
    "priority": "medium",
    "moved_at": "2026-08-27T16:05:00-03:00"
  },
  {
    "id": "APRAS-8",
    "title": "Restrict access to Tarefas and Categorias navigation menus to specific UserTypes",
    "status": "done",
    "justification": "UserType gains a generic allowed_menus JSON list (opt-in). Only ADMINISTRATOR exempt from the gate -- DIRECTOR now also requires a qualifying UserType, a deliberate breaking change. Migration starts every UserType with allowed_menus=[] (empty): intentionally disruptive rollout. Implemented on branch worktree-agent-a914db690224995c4 (commit 593e962). Independent code review and independent QA both APPROVED with no blocking findings -- QA verified live via HTTP that the gate covers all 13 task/category routes, Administrator is exempt even with a fully-empty UserType, and the field is genuinely editable and takes effect (PATCH /user-types/{id} flips access live). Backend 377 passed/3 skipped/96.92% coverage, frontend 530 passed -- independently reproduced by both reviewers. A stray git stash incident during developer cleanup was verified fully resolved with no side effects by two independent reviewers. Merged to master (b2e1b40) and pushed; migrate.yml confirmed green in production.",
    "expected_results": [
      "UserType.allowed_menus (JSON list, default empty) exists and is editable via API and UI",
      "GET/POST/PUT/DELETE on /tasks/* and /categories/* return 403 for any non-Administrator user unless a qualifying UserType allows it",
      "Administrator is never blocked by this gate",
      "Navbar hides gated links and ProtectedRoute blocks direct navigation for users failing the gate",
      "AGENTS.md RBAC table updated to reflect Director's now-conditional task access"
    ],
    "blockedBy": [],
    "spec_path": "docs/tasks/APRAS-8-spec.md",
    "spec_iterations": 2,
    "qa_iterations": 1,
    "last_review_findings": [],
    "created_at": "2026-08-27T16:20:00-03:00",
    "updated_at": "2026-08-27T19:05:00-03:00",
    "running": false,
    "completed_at": "2026-08-27T19:15:00-03:00",
    "priority": "medium",
    "moved_at": "2026-08-27T19:05:00-03:00"
  },
  {
    "id": "APRAS-9",
    "title": "Treat UserRole as a UserType for permission evaluation",
    "status": "done",
    "justification": "UserRole treated as implicit UserType (computed, not stored) for both APRAS-8 menu gate and Task.visible_to_id targeting. Role-types start empty, same disruptive philosophy as APRAS-8. Spec approved after 2 revision rounds. Implementation dispatched.",
    "running": false,
    "priority": "medium",
    "spec_iterations": 2,
    "spec_path": "docs/tasks/APRAS-9-spec.md",
    "completed_at": "2026-08-27T23:59:00-03:00"
  },
  {
    "id": "APRAS-10",
    "title": "Create dedicated landing/welcome page for Guest users",
    "status": "done",
    "justification": "Dedicated /welcome landing page for active GUEST users, replacing the generic APRAS-8 restricted-access message. Independent code review and QA both APPROVED with no blocking findings. 543/543 frontend tests passing. Merged to master (f007505) and pushed.",
    "running": false,
    "priority": "medium",
    "qa_iterations": 1,
    "completed_at": "2026-08-27T23:10:00-03:00"
  },
  {
    "id": "APRAS-11",
    "title": "Add UserTypes to task visibility configuration",
    "status": "done",
    "justification": "Multiple UserTypes per task visibility target, replacing single visible_to_id FK with a many-to-many relation. Spec approved after 2 revision rounds. Blocked on APRAS-9 (shares the same call sites: assert_manager_can_see_task, list_tasks filter, task_service.py, simulatedPermissions.ts) -- will move to implementation once APRAS-9 merges. APRAS-9 merged; unblocked, implementation dispatched.",
    "running": false,
    "priority": "medium",
    "blockedBy": [],
    "spec_path": "docs/tasks/APRAS-11-spec.md",
    "spec_iterations": 2,
    "completed_at": "2026-08-28T06:49:17.293458-03:00"
  },
  {
    "id": "APRAS-12",
    "title": "Create PORTEIRO (Gatekeeper) user role and dedicated Portaria dashboard",
    "status": "done",
    "justification": "Dedicated role and interface for gatekeepers to check-in/check-out visitors and contractors. Picked up from backlog, exploring design. Independent spec review round 1: NEEDS_REVISION -- excluded-by-construction claim is false for /lots,/authorizations,/occurrences,/documents,/projects,/announcements (bare ProtectedRoute, zero backend role checks), GatekeeperDashboard itself needs lots.py read access or check-in breaks entirely, redirect-loop traced definitively (no loop, clean dead-end to generic message) but PORTEIRO-vs-generic-message choice left unresolved, missing blockedBy APRAS-10 (spec assumes its unmerged ProtectedRoute shape). Redispatching spec-generator. Independent spec review round 1: NEEDS_REVISION -- excluded-by-construction claim is false for /lots,/authorizations,/occurrences,/documents,/projects,/announcements (bare ProtectedRoute, zero backend role checks), GatekeeperDashboard itself needs lots.py read access or check-in breaks entirely, redirect-loop traced definitively (no loop, clean dead-end to generic message) but PORTEIRO-vs-generic-message choice left unresolved, missing blockedBy APRAS-10 (spec assumes its unmerged ProtectedRoute shape). Redispatching spec-generator. Spec approved after 3 revision rounds (finally closed: full backend+frontend gap coverage for 6 previously-unguarded routes, unconditional redirect resolution). APRAS-10 (its blocker) has now merged -- ready for implementation.",
    "running": false,
    "priority": "medium",
    "spec_path": "docs/tasks/APRAS-12-spec.md",
    "expected_results": [
      "PORTEIRO role exists in the database enum (verified against real Postgres)",
      "PORTEIRO can check visitors in and out via the existing /gate flow",
      "ADMINISTRATOR/DIRECTOR/MANAGER retain unchanged access to /gate",
      "/gate route is now properly role-gated (fixes pre-existing unguarded-route gap)",
      "PORTEIRO lands on /gate after login and cannot reach any other authenticated route, including /gate-monitor"
    ],
    "blockedBy": [],
    "spec_iterations": 3,
    "completed_at": "2026-08-28T01:57:00.752651-03:00"
  },
  {
    "id": "APRAS-13",
    "title": "Integrate WhatsApp API for sending QR passes, visitor entry alerts, and announcements",
    "status": "blocked",
    "justification": "Automated WhatsApp notifications for visitor entry, QR pass sharing, and urgent HOA notices. BLOCKED: requires a real WhatsApp Business API provider account (Twilio, Meta Cloud API, etc.) and credentials, which do not exist yet. User confirmed on 2026-08-27 nothing is set up. Cannot spec or implement a real integration without this -- needs the user to choose a provider and provision an account first.",
    "running": false,
    "priority": "medium"
  },
  {
    "id": "APRAS-14",
    "title": "Generate dynamic QR code passes for visitor/contractor entry validation at turnstiles/gates",
    "status": "done",
    "justification": "Implemented per approved spec: backend GET /authorizations/{id} and GET /authorizations/{id}/qr-code (PNG via qrcode package, no role check, matches module convention); frontend VisitorAuthPage 'Ver QR' modal fetches the PNG as an authenticated blob via apiClient.get(..., {responseType:'blob'}) and renders it via URL.createObjectURL (revoked on close), since a bare <img src> cannot carry the Bearer token; GatekeeperDashboard 'Escanear QR' uses html5-qrcode to scan, validates the decoded text as a UUID, fetches the authorization, overwrites selectedLotId unconditionally, and now threads the scanned authorization_id explicitly through GatekeeperEntryModal into checkInMutation.mutateAsync (previously-dead authorizationId prop is now wired up); scanned state is cleared on modal close/cancel/complete. Independent review round 1: NEEDS_REVISION -- the two new endpoints discarded current_user and skipped the module's lot-scoping check (_check_lot_access), letting any authenticated user pull another lot's visitor PII by guessing an authorization UUID. Fixed via a new VisitorService.get_authorization_for_user that enforces the same lot check as the other routes, with an explicit PORTEIRO (Gatekeeper) bypass since the QR-scan flow's whole point is condo-wide lookup for a role with no lot linkage (mirrors check-in/check-out's existing gatekeeper carve-out). Also merged master (APRAS-9/29/12/25/30 + test fix) into this branch, resolving a real conflict in authorizations.py from APRAS-12's new PORTEIRO role-gate helper, and gave the two new endpoints an explicit PORTEIRO carve-out (with a regression test) so the Gatekeeper -- who has no lot linkage -- can still use the QR-scan flow post-merge. Backend (post-merge): 429 passed, 9 skipped, 97.32% coverage. Frontend (post-merge): 628 passed, 0 failed.",
    "running": false,
    "priority": "medium",
    "spec_path": "docs/tasks/APRAS-14-spec.md",
    "expected_results": [
      "Any visitor authorization has a corresponding QR code image viewable/downloadable",
      "Gatekeeper can scan the QR with device camera and reach the check-in confirmation modal",
      "All existing check-in validation rules remain unaffected -- scanning is a faster path, not a bypass"
    ],
    "spec_iterations": 2,
    "completed_at": "2026-08-28T02:34:43.764550-03:00"
  },
  {
    "id": "APRAS-15",
    "title": "Cadastro de Lotes (quadra, lote, endereço) e vinculação de usuários",
    "status": "done",
    "justification": "Independently verified by QA. Backend tests passing with 97.01% coverage (>90%), 373 frontend vitest tests passing (100%), Alembic migration 0006 verified, composite unique constraints and RBAC rules fully enforced.",
    "expected_results": [
      "Lot CRUD data model and endpoints created",
      "User-to-Lot binding mechanism implemented",
      "Frontend Lot list and detail management pages active"
    ],
    "blockedBy": [],
    "spec_path": "docs/tasks/T001-spec.md",
    "spec_iterations": 1,
    "qa_iterations": 1,
    "last_review_findings": [],
    "created_at": "2026-08-23T02:22:42-03:00",
    "updated_at": "2026-08-24T04:14:17-03:00",
    "running": false,
    "completed_at": "2026-08-24T04:14:17-03:00",
    "priority": "medium",
    "moved_at": "2026-08-24T04:14:17-03:00"
  },
  {
    "id": "APRAS-16",
    "title": "Cadastro de Moradores por Lote e vinculação de contas de usuário",
    "status": "done",
    "justification": "Independently verified by QA. Backend tests passing with 96.26% coverage (>90%), 381 frontend vitest tests passing (100%), Alembic migration 0007 verified, CPF check digit validation, signup auto-linking, manual binding, soft deletion, and RBAC rules fully enforced.",
    "expected_results": [
      "Resident model tied to Lot created",
      "Resident profile linking with User accounts implemented",
      "Resident management UI by lot enabled"
    ],
    "blockedBy": [],
    "spec_path": "docs/tasks/T002-spec.md",
    "spec_iterations": 1,
    "qa_iterations": 1,
    "last_review_findings": [],
    "created_at": "2026-08-23T02:22:42-03:00",
    "updated_at": "2026-08-25T14:55:00-03:00",
    "running": false,
    "completed_at": "2026-08-25T14:55:00-03:00",
    "priority": "medium",
    "moved_at": "2026-08-25T14:55:00-03:00"
  },
  {
    "id": "APRAS-17",
    "title": "Cadastro e Autorização de Visitantes e Prestadores de Serviço",
    "status": "done",
    "justification": "Independently verified by QA. Backend tests passing with 95.39% coverage (>90%), 394 frontend vitest tests passing (100%), Alembic migration 0008 verified, Visitor/VisitorAuthorization/AccessLog data models with FKs and indices created, single vs permanent authorization rules with shift/day/date window validation enforced, gatekeeper check-in/out logic with search by name/CPF/plate verified, auto-expiration of single-entry authorizations enforced, and RBAC rules for Admin/Director/Manager/Resident/Gatekeeper fully satisfied.",
    "expected_results": [
      "Visitor and pre-authorization models created",
      "Rules for single/recurring authorizations with day and shift constraints enforced",
      "Portaria gatekeeper check-in/check-out endpoints and interface built"
    ],
    "blockedBy": [],
    "spec_path": "docs/tasks/T003-spec.md",
    "spec_iterations": 1,
    "qa_iterations": 1,
    "last_review_findings": [],
    "created_at": "2026-08-23T02:22:42-03:00",
    "updated_at": "2026-08-25T15:21:40-03:00",
    "running": false,
    "completed_at": "2026-08-25T15:21:40-03:00",
    "priority": "medium",
    "moved_at": "2026-08-25T15:21:40-03:00"
  },
  {
    "id": "APRAS-18",
    "title": "Gestão e Armazenamento de Fotos nos Cadastros",
    "status": "done",
    "justification": "Independently verified by QA. Frontend tests passing (414/414, 100%). All backend source code verified through deep review: MediaAsset model with FKs and indexes, Alembic migration 0011 well-formed, storage abstraction with LocalStorageProvider under static/uploads, 5MB max file size and JPEG/PNG/WebP validation, auto-approval for Admin/Director/Manager uploads, pending approval queue with approval and rejection requiring reason, all 6 API endpoints under /api/v1/uploads created, reusable frontend components (PhotoUploadModal, WebcamCaptureDialog, AvatarCropEditor, AvatarWithFallback, PhotoApprovalQueuePage) implemented, RBAC rules enforced, i18n keys added to pt.json and en.json under photos.* namespace, and Navbar badge for Admin/Director roles. Backend pytest could not be executed due to sandbox restrictions on Python binary, but test files (test_uploads.py, test_uploads_rbac.py) verified by source review covering all required scenarios.",
    "expected_results": [
      "Secure file storage backend service configured",
      "Photo upload, thumbnail generation, and cropping API ready",
      "Webcam and photo selector frontend component created"
    ],
    "blockedBy": [],
    "spec_path": "docs/tasks/T004-spec.md",
    "spec_iterations": 1,
    "qa_iterations": 1,
    "last_review_findings": [],
    "created_at": "2026-08-23T02:22:42-03:00",
    "updated_at": "2026-08-25T17:15:00-03:00",
    "running": false,
    "completed_at": "2026-08-25T17:15:00-03:00",
    "priority": "medium",
    "moved_at": "2026-08-25T17:15:00-03:00"
  },
  {
    "id": "APRAS-19",
    "title": "Controle de Acesso e Integração de Reconhecimento Facial",
    "status": "done",
    "justification": "Independently verified by QA on first pass (no iterations). Backend: AccessDevice/FacialTemplate/FacialAccessEvent models, endpoints under /api/v1/access-control, Alembic migration 0012, 21 new tests (100% coverage on new files, full suite 269 passing at 96.05%). Frontend: AccessControlPage, GateMonitorPage, device/sync/event-feed components, ProtectedRoute extended for multi-role, 420/420 tests passing. Also fixed a pre-existing missing Pillow dependency that silently broke pytest. Known non-blocking gap: migration 0012 not run against a real Postgres instance (no Docker in sandbox, same as T004).",
    "expected_results": [
      "AccessDevice model and device management API created",
      "Facial template sync and verification webhooks implemented",
      "Live access control and gate monitor UI implemented"
    ],
    "blockedBy": [],
    "spec_path": "docs/tasks/T005-spec.md",
    "spec_iterations": 1,
    "qa_iterations": 1,
    "last_review_findings": [],
    "created_at": "2026-08-23T02:22:42-03:00",
    "updated_at": "2026-08-27T09:00:00-03:00",
    "running": false,
    "completed_at": "2026-08-27T09:00:00-03:00",
    "priority": "medium",
    "moved_at": "2026-08-27T09:00:00-03:00"
  },
  {
    "id": "APRAS-20",
    "title": "Blog/Feed de Comunicados e Notícias",
    "status": "done",
    "justification": "Implemented on branch worktree-agent-a91e71f770443d920 (commits f49a537, d3f58f7). Independent code review APPROVED (verified migration against real Postgres, RBAC via live curl, tests). Independent QA round 1: NEEDS_REVISION (announcement-feed Vitest function coverage 74.57% < 75%, MediaCarousel prev/next handlers untested). Developer added a real navigation test closing the gap (module now 83.05%, MediaCarousel 100%). Independent QA round 2: APPROVED, re-verified coverage numbers from raw coverage-summary.json rather than trusting the report. 439 frontend tests passing.",
    "expected_results": [
      "Announcement post and media models created",
      "Instagram-style news feed with image/PDF carousels built",
      "Comment threads, read receipts, and publisher role guards active"
    ],
    "blockedBy": [],
    "spec_path": "docs/tasks/T006-spec.md",
    "spec_iterations": 1,
    "qa_iterations": 2,
    "last_review_findings": [],
    "created_at": "2026-08-23T02:22:42-03:00",
    "updated_at": "2026-08-27T13:10:00-03:00",
    "running": false,
    "completed_at": "2026-08-27T13:10:00-03:00",
    "priority": "medium",
    "moved_at": "2026-08-27T13:10:00-03:00"
  },
  {
    "id": "APRAS-21",
    "title": "Acompanhamento Físico e Financeiro de Obras",
    "status": "done",
    "justification": "Independently verified by QA. Backend tests passing with 96.04% coverage (>90%) across 287 tests; frontend vitest tests passing (431/431, 100%). Alembic migration 0013 well-formed with cascading deletes and indices; ConstructionProject, ProjectMilestone, and ProjectUpdate models implemented; milestone status workflow with auto completion dates verified; budget vs actual execution and updates feed with photos/cost impacts verified; full RBAC permissions verified across Admin, Director, Manager, Resident, and Guest roles.",
    "expected_results": [
      "Construction project and milestone data models implemented",
      "Physical progress indicator and milestone status workflow built",
      "Budget vs executed financial tracking and project updates feed created"
    ],
    "blockedBy": [],
    "spec_path": "docs/tasks/T007-spec.md",
    "spec_iterations": 1,
    "qa_iterations": 1,
    "last_review_findings": [],
    "created_at": "2026-08-23T02:22:42-03:00",
    "updated_at": "2026-08-27T10:22:00-03:00",
    "running": false,
    "completed_at": "2026-08-27T10:22:00-03:00",
    "priority": "medium",
    "moved_at": "2026-08-27T10:22:00-03:00"
  },
  {
    "id": "APRAS-22",
    "title": "Área e Dashboard Financeiro da Associação",
    "status": "done",
    "justification": "Association financial health, cash balance, statement, and invoice drill-down. Spec approved after one revision round (added invoice_file_path). Independent code review and independent QA (both run separately from the implementing agent) APPROVED with no blocking findings — QA functionally exercised RBAC live via curl against a running instance and independently recomputed cash-balance/statement figures to confirm correctness, not just read tests. Migration renumbered 0015->0016 at merge time to chain after 0015_merge_heads (APRAS-28). Backend 347 passed/3 skipped/96.95% coverage, frontend 492/492 passing — independently reproduced by both reviewers. Merged to master (756c934) and pushed; migrate.yml confirmed green in production.",
    "expected_results": [
      "Financial transaction and budget models created",
      "Cash balance and statement of inflows/outflows calculated",
      "Budget vs actual execution table with 1-click invoice PDF drill-down completed"
    ],
    "blockedBy": [],
    "spec_path": "docs/tasks/APRAS-22-spec.md",
    "spec_iterations": 1,
    "qa_iterations": 1,
    "last_review_findings": [],
    "created_at": "2026-08-23T02:22:42-03:00",
    "updated_at": "2026-08-27T15:05:00-03:00",
    "running": false,
    "completed_at": "2026-08-27T15:05:00-03:00",
    "priority": "medium",
    "moved_at": "2026-08-27T15:05:00-03:00"
  },
  {
    "id": "APRAS-23",
    "title": "Central de Documentos e Arquivos da Associação",
    "status": "done",
    "justification": "Independently verified by QA. Backend tests passing with 95.63% coverage (>90%), 406 frontend vitest tests passing (100%), Alembic migration 0010_add_document_tables verified, DocumentFolder, AssociationDocument, and DocumentDownloadLog models created with FKs and indexes, role-gated folder security matching user's effective role enforced, document versioning chain preserved, download audit logging verified, inline PDF viewer modal and multi-parameter document search active, and RBAC rules for Admin, Director, Manager, Resident fully satisfied.",
    "expected_results": [
      "Document folder tree and document metadata models created",
      "Role-gated folder access control implemented",
      "Document previewer, tag search, and download tracker built"
    ],
    "blockedBy": [],
    "spec_path": "docs/tasks/T009-spec.md",
    "spec_iterations": 1,
    "qa_iterations": 1,
    "last_review_findings": [],
    "created_at": "2026-08-23T02:22:42-03:00",
    "updated_at": "2026-08-25T16:22:45-03:00",
    "running": false,
    "completed_at": "2026-08-25T16:22:45-03:00",
    "priority": "medium",
    "moved_at": "2026-08-25T16:22:45-03:00"
  },
  {
    "id": "APRAS-24",
    "title": "Livro de Ocorrências e Atendimento de Reclamações",
    "status": "done",
    "justification": "Independently verified by QA. Backend tests passing with 95.22% coverage (>90%), 399 frontend vitest tests passing (100%), Alembic migration 0009_add_occurrence_tables verified, Occurrence and OccurrenceTimeline data models with FKs and indices created, protocol sequence generator (OCO-YYYY-XXXXXX) verified, public vs private visibility rules, anonymous masking, internal timeline auditing, and RBAC rules fully satisfied.",
    "expected_results": [
      "Occurrence model with auto-generated protocol numbers created",
      "Status workflow (Open -> Under Review -> Resolved) with internal timeline log built",
      "Resident occurrence submission UI with photo evidence attachment active"
    ],
    "blockedBy": [],
    "spec_path": "docs/tasks/T010-spec.md",
    "spec_iterations": 1,
    "qa_iterations": 1,
    "last_review_findings": [],
    "created_at": "2026-08-23T02:22:42-03:00",
    "updated_at": "2026-08-25T15:59:30-03:00",
    "running": false,
    "completed_at": "2026-08-25T15:59:30-03:00",
    "priority": "medium",
    "moved_at": "2026-08-25T15:59:30-03:00"
  },
  {
    "id": "APRAS-25",
    "title": "Canal de Críticas, Sugestões e Fale Conosco",
    "status": "done",
    "justification": "Feedback/suggestion/complaint channel to the board, mirroring the shipped Occurrence Book architecture. Spec approved after 1 revision round (fixed: role-restriction precedent error, wrong migration idiom cited, backend/frontend seen-flip gap). Implementation dispatched.",
    "expected_results": [
      "Feedback message model supporting anonymous options created",
      "Board response workflow and response notification system built",
      "Categorized inbox for administration and resident contact UI deployed"
    ],
    "blockedBy": [],
    "spec_path": "docs/tasks/APRAS-25-spec.md",
    "spec_iterations": 2,
    "qa_iterations": 0,
    "last_review_findings": [],
    "created_at": "2026-08-23T02:22:42-03:00",
    "updated_at": "2026-08-23T02:22:42-03:00",
    "running": false,
    "priority": "medium",
    "moved_at": "2026-08-23T02:22:42-03:00",
    "completed_at": "2026-08-28T01:56:17.723521-03:00"
  },
  {
    "id": "APRAS-26",
    "title": "Análise Comparativa e Recursos Avançados COM21",
    "status": "done",
    "justification": "Sourced comparative analysis of COM21 (real product, via web search, sources cited in the document) against APRAS current features. APRAS matches/exceeds on occurrence-tracking and RBAC granularity; clear gaps are space reservations, package tracking, online assembly/voting (surfaced but not in original scope), and push notifications. Full architectural specs written for space reservations and package tracking (spun off as APRAS-31/APRAS-32 backlog tasks for future implementation). Push notifications and offline gatekeeper mode kept at roadmap level per their cross-cutting/security complexity, each needing its own future brainstorming pass.",
    "expected_results": [
      "COM21 parity matrix and gap analysis completed",
      "Architectural specifications for package tracking (encomendas) and area reservations created",
      "Roadmap for web push notifications and offline gatekeeper mode finalized"
    ],
    "blockedBy": [],
    "spec_path": "docs/tasks/APRAS-26-analysis.md",
    "spec_iterations": 0,
    "qa_iterations": 0,
    "last_review_findings": [],
    "created_at": "2026-08-23T02:22:42-03:00",
    "updated_at": "2026-08-23T02:22:42-03:00",
    "running": false,
    "priority": "medium",
    "moved_at": "2026-08-23T02:22:42-03:00",
    "completed_at": "2026-08-27T23:45:00-03:00"
  },
  {
    "id": "APRAS-27",
    "title": "Fix missing RESIDENT value in Postgres userrole enum",
    "status": "done",
    "justification": "URGENT hotfix, discovered by independent QA while verifying APRAS-20 against a real Postgres instance. Added Alembic migration 0014_add_resident_userrole (chained after 0013_add_construction_project, same ALTER TYPE ... ADD VALUE IF NOT EXISTS idiom as 0003_add_guest_to_userrole_enum.py). Independent code review and independent QA (both run separately from the implementing agent, each spinning up their own throwaway Postgres 16 container) APPROVED with no blocking findings: fresh-database upgrade through all 14 migrations applies cleanly, a real INSERT with role='RESIDENT' succeeds, alembic downgrade -1 is a safe no-op (Postgres cannot drop enum values), upgrade head re-applies idempotently. backend/tests/test_migrations_postgres.py (3 tests, Postgres-only, skip-gated behind TEST_POSTGRES_URL since CI has no Postgres service) verified to genuinely execute and pass against real Postgres by both independent reviewers, not just the developer. Full backend suite: 287 passed/3 skipped without TEST_POSTGRES_URL (matching CI), 290 passed with it set, 96.22% coverage. Scope strictly limited to the database enum fix, no resident UI/business logic added. Merged to master (pending push).",
    "expected_results": [
      "Alembic migration adds RESIDENT to the Postgres userrole enum type (same idiom as migration 0003_add_guest_to_userrole_enum.py)",
      "A test verifies the enum accepts RESIDENT against a real Postgres instance (not just SQLite)",
      "Migration runs cleanly upgrade+downgrade against a real Postgres instance"
    ],
    "blockedBy": [],
    "spec_path": "docs/tasks/APRAS-27-spec.md",
    "spec_iterations": 1,
    "qa_iterations": 1,
    "last_review_findings": [],
    "created_at": "2026-08-27T12:50:00-03:00",
    "updated_at": "2026-08-27T14:00:00-03:00",
    "running": false,
    "completed_at": "2026-08-27T14:00:00-03:00",
    "priority": "medium",
    "moved_at": "2026-08-27T14:00:00-03:00"
  },
  {
    "id": "APRAS-28",
    "title": "Fix diverging Alembic migration heads (two concurrent 0013 migrations)",
    "status": "done",
    "justification": "URGENT — discovered while implementing APRAS-22: T006 (announcement tables) and APRAS-21 (construction project tables) were both authored concurrently as revision 0013 against the same parent (0012_add_access_control_tables), creating two diverging heads (0013_add_announcement_tables and 0014_add_resident_userrole, the latter having chained after the other 0013 branch). This broke `alembic upgrade head` with 'Multiple head revisions', which caused the last two production migrate.yml runs to FAIL (confirmed via `gh run list`/`gh run view`) — meaning T006's and APRAS-27's schema changes were never actually applied to production. Fixed via `alembic merge heads` generating 0015_merge_migration_heads (no-op merge revision, standard tooling, not hand-written). Verified myself against a real throwaway Postgres 16 container: fresh upgrade head applies all 15 migrations cleanly (both 0013 branches' tables + RESIDENT enum value present), downgrade -1 correctly fails with Alembic's expected 'Ambiguous walk' at a merge point (not a bug), explicit-target downgrade past the merge point works, re-upgrade to head works. Full backend suite: 301 passed, 3 skipped, 96.52% coverage, no regressions. Committed directly (not via a developer/reviewer/QA agent cycle) given production was actively broken and the fix is Alembic's own standard auto-generated merge tooling with no custom logic.",
    "expected_results": [
      "alembic heads shows exactly one head",
      "alembic upgrade head from a blank database applies all migrations cleanly, including both prior 0013 branches",
      "The next migrate.yml run on master succeeds"
    ],
    "blockedBy": [],
    "spec_path": "",
    "spec_iterations": 0,
    "qa_iterations": 0,
    "last_review_findings": [],
    "created_at": "2026-08-27T14:45:00-03:00",
    "updated_at": "2026-08-27T14:45:00-03:00",
    "running": false,
    "completed_at": "2026-08-27T14:45:00-03:00",
    "priority": "medium",
    "moved_at": "2026-08-27T14:45:00-03:00"
  },
  {
    "id": "APRAS-29",
    "title": "Remove orphaned User.block_lot column",
    "status": "done",
    "justification": "block_lot was added to User (APRAS-6) before the Lot/Resident subsystem (APRAS-15/16) existed. Now that Lot/UserLotLink properly models which unit a user is associated with, block_lot is redundant and no UI writes to it (APRAS-7 was rescoped to drop it). Low priority cleanup: drop the column via migration once confirmed nothing reads it either. Independent spec review round 1: NEEDS_REVISION -- 7 backend tests directly reference block_lot, contradicting the zero-references claim. Fixed inline (listed exact tests to update). Ready for a quick re-review or direct implementation given the fix was small and mechanical. Correction approved on re-review (all 7 test references confirmed exact match, instruction confirmed mechanically applicable). Implementation dispatched.",
    "expected_results": [
      "Confirm no code reads User.block_lot before removing it",
      "Alembic migration drops the block_lot column from the user table",
      "User model and schemas no longer reference block_lot"
    ],
    "blockedBy": [],
    "spec_path": "docs/tasks/APRAS-29-spec.md",
    "spec_iterations": 2,
    "qa_iterations": 1,
    "last_review_findings": [],
    "created_at": "2026-08-27T15:20:00-03:00",
    "updated_at": "2026-08-27T15:20:00-03:00",
    "running": false,
    "priority": "medium",
    "moved_at": "2026-08-27T15:20:00-03:00",
    "completed_at": "2026-08-28T00:07:21.232381-03:00"
  },
  {
    "id": "APRAS-30",
    "title": "Fix UserRead 500 on non-canonical CPF during response serialization",
    "status": "done",
    "justification": "Discovered by independent QA while verifying APRAS-7: UserRead's cpf field validator re-runs on response serialization, so any user row with a non-canonical CPF format (e.g. a historically-seeded/imported value) causes a ResponseValidationError (500) on every endpoint that returns UserRead -- GET /users/, PATCH /users/{id}, and PATCH /users/{id}/contact-info all affected. Not introduced by APRAS-7, pre-existing since the cpf-validation task. Spec APPROVED on first review round (bug mechanism confirmed real via live repro, existing duplication-convention match confirmed, testing approach confirmed achievable against existing fixture patterns). Implementation dispatched.",
    "expected_results": [
      "Endpoints returning UserRead no longer 500 for a user with a non-canonical (but previously-accepted) CPF value",
      "Existing CPF validation on write (signup, PATCH) is unaffected"
    ],
    "blockedBy": [],
    "spec_path": "docs/tasks/APRAS-30-spec.md",
    "spec_iterations": 1,
    "qa_iterations": 0,
    "last_review_findings": [],
    "created_at": "2026-08-27T16:05:00-03:00",
    "updated_at": "2026-08-27T16:05:00-03:00",
    "running": false,
    "priority": "medium",
    "moved_at": "2026-08-27T16:05:00-03:00",
    "completed_at": "2026-08-28T01:56:17.723521-03:00"
  },
  {
    "id": "APRAS-31",
    "title": "Implement space/amenity reservation system",
    "status": "done",
    "justification": "Space/amenity reservation system (ReservableSpace + SpaceReservation). Spec approved after 1 revision round (fixed a stale migration number, a false CategoriesPage.tsx UI-pattern claim, and an underspecified query-param interaction). Implementation dispatched.",
    "expected_results": [
      "ReservableSpace admin-managed CRUD (Administrator/Director)",
      "SpaceReservation booking with server-side overlap/double-booking prevention",
      "Resident-facing calendar/booking view per space"
    ],
    "blockedBy": [],
    "spec_path": "docs/tasks/APRAS-31-spec.md",
    "spec_iterations": 1,
    "qa_iterations": 0,
    "last_review_findings": [],
    "created_at": "2026-08-27T23:45:00-03:00",
    "updated_at": "2026-08-27T23:45:00-03:00",
    "running": false,
    "priority": "low",
    "completed_at": "2026-08-28T03:27:24.068682-03:00",
    "moved_at": "2026-08-27T23:45:00-03:00"
  },
  {
    "id": "APRAS-32",
    "title": "Implement package tracking (encomendas) for Gatekeeper",
    "status": "done",
    "justification": "Package tracking (encomendas) for Gatekeeper. Spec approved after 1 revision round (fixed a stale migration number, a GUEST RBAC bypass gap, and a mischaracterized convention claim). Implementation dispatched.",
    "expected_results": [
      "Package model with AWAITING_PICKUP/PICKED_UP status",
      "Gatekeeper (incl. PORTEIRO) can log arrival and mark pickup",
      "Resident can see their own lot-scoped package status"
    ],
    "blockedBy": [],
    "spec_path": "docs/tasks/APRAS-32-spec.md",
    "spec_iterations": 1,
    "qa_iterations": 0,
    "last_review_findings": [],
    "created_at": "2026-08-27T23:45:00-03:00",
    "updated_at": "2026-08-27T23:45:00-03:00",
    "running": false,
    "priority": "low",
    "completed_at": "2026-08-28T02:58:37.033900-03:00",
    "moved_at": "2026-08-27T23:45:00-03:00"
  },
  {
    "id": "APRAS-33",
    "title": "Scope online assembly/voting feature (COM21 parity gap)",
    "status": "done",
    "justification": "Assembly voting + poll. Spec went through 4 independent review rounds after the initial user brainstorm, each catching a real gap: per-lot eligibility model (co-ownership + manual LotVoterEligibility), a retraction deadlock (holder losing eligibility mid-window could softlock a lot indefinitely), missing ENQUETE retraction semantics, and a missing lot_id on the multi-lot retract endpoint. Spec APPROVED. Not yet dispatched for implementation, pending user go-ahead.",
    "blockedBy": [],
    "spec_path": "docs/tasks/APRAS-33-spec.md",
    "spec_iterations": 4,
    "qa_iterations": 0,
    "last_review_findings": [],
    "created_at": "2026-08-27T23:45:00-03:00",
    "updated_at": "2026-08-28T21:57:29.510Z",
    "running": false,
    "priority": "low",
    "moved_at": "2026-08-28T21:57:29.510Z",
    "expected_results": [
      "Administrator/Director cria uma assembleia (AGO/AGE) com N votações de pauta, e cada votação aceita exatamente um voto por lote.",
      "Elegível de dois lotes (dono de ambos, ou dono de um e cadastrado via `LotVoterEligibility` no outro) vota nos dois, uma cédula por lote, e a apuração conta os dois.",
      "Lote marcado como inadimplente é impedido de votar no momento do voto, com a recusa registrada e visível na minuta; se quitar durante a janela, passa a votar.",
      "Com a votação aberta, o endpoint de apuração devolve apenas a contagem de votantes para qualquer papel, incluindo Administrator, e em `ENQUETE` sem denominador; o resultado por opção só aparece depois do fechamento.",
      "Enquete com anonimato ligado não devolve identificação do votante na resposta da API nem depois de fechar, e o votante continua vendo o próprio voto.",
      "Um lote com mais de um elegível (co-proprietários, ou proprietário + `LotVoterEligibility`) tem exatamente um voto ativo por vez: qualquer elegível pode lançar o primeiro; depois disso, só quem lançou pode trocar ou retirar; a retirada libera o lote para qualquer elegível votar de novo; qualquer elegível do lote enxerga a cédula ativa mesmo sem ser quem a lançou.",
      "Em `ENQUETE` não anônima, moradores do mesmo lote enxergam o voto individual uns dos outros (não só o próprio), mesmo com a votação aberta; em `ENQUETE` anônima, ninguém vê a identidade de ninguém, nem morador do mesmo lote.",
      "`MANAGER` vê apuração e contagem de participação nas duas modalidades, mesmo sem vínculo a nenhum lote.",
      "A minuta da assembleia é gerada em HTML com resultado por pauta, atribuição por Bloco/Lote, denominador de lotes ativos e lista de lotes barrados por inadimplência, e é salva no Document Center numa pasta \"Atas de Assembleia\" criada automaticamente na primeira vez."
    ],
    "code_review_iterations": 2,
    "completed_at": "2026-08-28T21:57:29.510Z"
  },
  {
    "id": "APRAS-34",
    "title": "Implementar controle de ativos e estoque do condomínio",
    "status": "done",
    "justification": "Implemented complete backend models (Asset, InventoryMovement), migration 0026, AssetService with atomic balance calculations, REST endpoints, full RBAC rules, frontend TypeScript types, API client, React Query hooks, UI components (AssetsInventoryPage, AssetSummaryCards, AssetTable, AssetFormModal, StockMovementModal, AssetMovementHistoryModal), routing, and 100% passing test suite with high coverage (backend 98.01%, frontend 958 passed).",
    "priority": "low",
    "expected_results": [
      "CRUD de ativo patrimonial (nome, categoria, localização, data de aquisição, valor, estado) restrito a Administrator/Director",
      "Registro de movimentação de estoque (entrada/saída com quantidade e responsável) com saldo atual calculado por item",
      "Listagem de ativos filtrável por categoria e localização, visível a Administrator/Director/Manager"
    ],
    "running": false,
    "blockedBy": [],
    "spec_path": "docs/tasks/APRAS-34-spec.md",
    "spec_iterations": 1,
    "qa_iterations": 0,
    "last_review_findings": [],
    "created_at": "2026-08-28T16:06:19.091Z",
    "moved_at": "2026-08-29T13:06:00.000Z",
    "updated_at": "2026-08-29T13:06:00.000Z",
    "completed_at": "2026-08-29T13:06:00.000Z"
  },
  {
    "id": "APRAS-35",
    "title": "Fix frontend TypeScript errors that break npm run build and the Vite dev app",
    "status": "done",
    "justification": "",
    "priority": "high",
    "expected_results": [
      "`cd frontend && npm run build` exits 0 and prints no line matching `error TS`.",
      "`cd frontend && npx tsc -b --force` exits 0 with empty output.",
      "`cd frontend && npx vite build` exits 0 and emits no `MISSING_EXPORT` diagnostic.",
      "With `npm run dev` running, loading the app at `/login` renders the login form and the browser console shows no `does not provide an export named` error (previously: `The requested module '/src/types/resident.ts' does not provide an export named 'ResidentDetail'`).",
      "`grep -n \"str\" frontend/src/types/media_asset.ts` shows no `url: str`; the field is `url: string`.",
      "`grep -rn \"^export enum\" frontend/src` returns no matches; `ResidentRelationship` and `PackageStatus` are `as const` objects with matching union types, and their string values are unchanged.",
      "`grep -rn \"manager_visible\" frontend/src` returns no matches.",
      "`cd frontend && npx vitest run` reports 71 passed test files and 769 passed tests, 0 failed.",
      "`cd frontend && npm run lint` reports no more than the 479 pre-existing errors - the count must go down or stay equal, never up.",
      "`.github/workflows/ci.yml` contains a step in the `frontend` job that runs `npm run build`, and the CI run for the PR shows that step passing.",
      "`src/api/projects.ts` imports no `ProjectRead`; `createProject` and `updateProject` are typed `Promise<ConstructionProject>`.",
      "`useUsers()` is consumed as `data` (not `users`) in `LinkUserAccountModal.tsx` and `UserLotAssignmentModal.tsx`, and the mocks in `LotsPage.test.tsx` and `ResidentsTab.test.tsx` return `{ data: [...] }`.",
      "`badgeVariants` in `src/components/ui/badge.tsx` includes `destructive` and `warning`; all previously existing variants are unchanged."
    ],
    "running": false,
    "blockedBy": [],
    "created_at": "2026-08-29T11:21:33.818Z",
    "moved_at": "2026-08-29T12:15:31.531Z",
    "updated_at": "2026-08-29T12:15:31.531Z",
    "spec_path": "docs/tasks/APRAS-35-spec.md",
    "last_review_findings": [],
    "completed_at": "2026-08-29T12:15:31.531Z"
  },
  {
    "id": "APRAS-36",
    "title": "Raise frontend test coverage above the 75% functions gate",
    "status": "done",
    "justification": "",
    "priority": "medium",
    "expected_results": [
      "`cd frontend && npm run test:coverage` exits 0; its printed summary shows `Functions : >= 78%` (baseline 73.16%, gate 75%) and no `ERROR: Coverage for ...` line.",
      "The same run reports statements >= 80%, branches >= 76% and lines >= 80% - no metric regresses below its baseline (80.14 / 76.00 / 80.73).",
      "`frontend/coverage/coverage-summary.json` reports >= 90% functions for each of `src/api/finance.ts`, `announcements.ts`, `visitors.ts`, `projects.ts`, `documents.ts`, `lots.ts`, `residents.ts`, `accessControl.ts`, `packages.ts`, `uploads.ts` (all 0% at baseline), and every exported function of those ten modules is exercised by a named test asserting HTTP verb, exact path, payload/params and unwrapped return value.",
      "`frontend/coverage/coverage-summary.json` reports >= 60% functions for `src/features/visitor-management/components/AuthorizationFormModal.tsx` (12.5% at baseline - this is the file the task description called `VisitorFormModal.tsx`, which does not exist in the repo) and >= 60% functions for `src/features/project-management/components/ConstructionTrackerPage.tsx` (18.8% at baseline).",
      "A new test `frontend/src/__tests__/AppRouting.smoke.test.tsx` renders the real `App` (real `BrowserRouter` and page modules; no `vi.mock` of anything under `src/features/**`, of `react-router-dom`, or of any `src/api/*` module except `src/api/client`) and asserts that `/login`, `/signup`, `/forgot-password` and `/reset-password?token=test-token` (token query param required - `ResetPasswordPage` renders no form without it) each render their real form inputs and submit button; that `/reset-password` with no token renders no password input and shows the `Link invalido` alert; and that `/dashboard` while unauthenticated lands on the login form. Deleting the body of `LoginPage`'s returned JSX makes this test fail.",
      "`cd frontend && npx vitest run` passes with > 769 tests and 0 skipped/`.todo`; no pre-existing test is deleted, renamed away, or weakened.",
      "`cd frontend && npm run build` (`tsc -b && vite build`) still exits 0 with the new test files in the type-check graph.",
      "After a coverage run, `cd frontend && npx eslint .` reports zero findings whose path is under `frontend/coverage/` (baseline: 3 phantom `Unused eslint-disable directive` warnings), achieved by adding `coverage` to `globalIgnores` in `frontend/eslint.config.js`; the `src/` error count does not increase (baseline 457 errors), and `npx eslint` on the files added by this task is clean.",
      "No production source file changed: `git diff --name-only master -- frontend/` lists only files under a `__tests__/` directory plus `frontend/eslint.config.js` (and at most `frontend/vitest.config.ts`, and only with thresholds raised, never lowered). The assertion is scoped to `frontend/` because the branch is expected to also carry `docs/**` and `.meridian/tasks.json`. `coverage.exclude` in `vitest.config.ts` is unchanged and no `v8 ignore` comment is added to any file under `frontend/src/` outside `__tests__/`."
    ],
    "running": false,
    "blockedBy": [],
    "created_at": "2026-08-29T11:21:41.786Z",
    "moved_at": "2026-08-29T13:06:38.900Z",
    "updated_at": "2026-08-29T13:06:38.900Z",
    "spec_path": "docs/tasks/APRAS-36-spec.md",
    "spec_iterations": 1,
    "last_review_findings": [],
    "completed_at": "2026-08-29T13:06:38.900Z"
  },
  {
    "id": "APRAS-37",
    "title": "Implementar cotação de compras com orçamentos e escolha justificada",
    "status": "done",
    "justification": "Pedido de compra com múltiplos orçamentos e escolha justificada. Decisão de escopo do usuário (2026-08-29): o módulo é ISOLADO - sem integração com Financeiro, Patrimônio & Estoque ou Obras.",
    "priority": "medium",
    "expected_results": [
      "`POST /api/v1/purchase-requests` com `{title, description, general_notes}` como ADMINISTRATOR, DIRECTOR ou MANAGER retorna `201` e o corpo traz `id`, `status: \"OPEN\"` e os textos de `description` e `general_notes` gravados; o mesmo POST como RESIDENT, PORTEIRO ou GUEST retorna `403`.",
      "`POST /api/v1/purchase-requests/{id}/quotes` aceita vários orçamentos no mesmo pedido; cada resposta `201` traz `supplier_name`, `unit_price`, `quantity` e `total_price` igual a `unit_price × quantity`; `GET /api/v1/purchase-requests/{id}` devolve todos eles em `quotes`, ordenados por `total_price` crescente, com `is_lowest_price: true` apenas no mais barato.",
      "Um orçamento aceita `extra_fields` como lista ordenada de pares `{label, value}` (ex.: `{\"label\": \"Prazo de entrega\", \"value\": \"15 dias\"}`), devolvidos íntegros e na mesma ordem pelo `GET` do pedido, com `label` e `value` já sem espaços nas pontas; retornam `422`: rótulo vazio (`{\"label\": \"\"}`), rótulo só com espaços (`{\"label\": \"   \"}`), rótulos repetidos ignorando caixa e espaços (`[{\"label\": \"Prazo\"}, {\"label\": \" prazo \"}]`) e mais de 20 itens. Um `value` vazio ou só com espaços é ACEITO e gravado como `\"\"`.",
      "`POST /api/v1/purchase-requests/{id}/decision` com `{quote_id, justification}` como ADMINISTRATOR ou DIRECTOR retorna `201`; o pedido passa a `status: \"DECIDED\"` e o `GET` do pedido mostra `current_decision` com a justificativa, o nome de quem decidiu e a data, e o orçamento escolhido com `is_selected: true`.",
      "A mesma chamada sem justificativa, com justificativa em branco ou com menos de 10 caracteres retorna `422` e nenhuma decisão é registrada (`decisions` continua vazio); a mesma chamada como MANAGER retorna `403`.",
      "Com o pedido já decidido, `POST /{id}/quotes`, `PUT /{id}/quotes/{quote_id}` e `DELETE /{id}/quotes/{quote_id}` retornam `409`; uma segunda decisão sobre o mesmo pedido é aceita e o `GET` passa a listar as duas justificativas em `decisions`, a mais recente com `is_current: true` e a anterior preservada com `is_current: false`.",
      "`GET /api/v1/purchase-requests` como RESIDENT, PORTEIRO ou GUEST retorna `403`; um MANAGER que tenta `PUT`/`DELETE` em pedido ou orçamento criado por outro usuário recebe `403`.",
      "A migração `0027_add_purchase_quotation` aplica-se sobre a head `0026_add_asset_and_inventory`, cria apenas as tabelas `purchase_request`, `purchase_quote` e `purchase_quote_decision`, e `alembic heads` continua reportando uma única head; `alembic downgrade -1` remove exatamente essas três tabelas.",
      "O teste `backend/tests/test_purchase_isolation.py` passa e cobre os cinco arquivos do módulo - `app/models/purchase.py`, `app/schemas/purchase.py`, `app/services/purchase_service.py`, `app/api/v1/endpoints/purchases.py` e `alembic/versions/0027_add_purchase_quotation.py` (a migração NÃO pode ser excluída da varredura) - via `ast.parse()`, verificando que: (a) nenhum `import`/`from ... import` tem módulo igual a um de FORBIDDEN_MODULES (comparação por caminho pontilhado completo, de modo que `app.models.media_asset` continua permitido); (b) nenhum `from app.models|app.schemas|app.services import ...` importa um nome de FORBIDDEN_NAMES; (c) os nomes de tabela usados como argumento de `op.create_table`/`op.drop_table`/`op.add_column`/`op.drop_column`/`op.alter_column`/`op.create_index`/`op.drop_index`/`sa.ForeignKey`/`sa.ForeignKeyConstraint`/`op.create_foreign_key` na migração são subconjunto de `{purchase_request, purchase_quote, purchase_quote_decision, user}`. Docstring do módulo e as atribuições `revision`/`down_revision`/`branch_labels`/`depends_on` ficam fora da varredura por construção - o teste ainda afirma positivamente que `down_revision == \"0026_add_asset_and_inventory\"`. Além disso, depois de criar pedido + orçamentos + decisão, as tabelas `financial_transaction`, `inventory_movement`, `asset` e `construction_project` continuam com zero linhas.",
      "O diff da tarefa não altera nenhum arquivo de Financeiro, Patrimônio & Estoque ou Obras - no backend (`app/models/finance.py`, `app/models/asset.py`, `app/models/project.py`, `app/services/finance_service.py`, `app/services/asset_service.py`, `app/services/project_service.py`, `app/api/v1/endpoints/{finance,assets,inventory_movements,projects}.py`) nem no frontend (`src/features/{finance,asset-management,project-management}`, `src/api/{finance,assets,projects}.ts`).",
      "No frontend, a rota `/purchases` renderiza a página para ADMINISTRATOR, DIRECTOR e MANAGER e redireciona os demais papéis para `/dashboard`; o item de menu \"Cotações de Compra\" aparece só para esses três papéis; o controle de escolher orçamento não é renderizado para MANAGER e, para DIRECTOR/ADMINISTRATOR, o botão de confirmar fica desabilitado enquanto a justificativa tiver menos de 10 caracteres.",
      "`cd backend && uv run pytest` passa com os módulos novos em >= 90% de cobertura de linhas; `cd frontend && npm run test:coverage` passa dentro dos limiares atuais (80/78/76/80) e `npm run build` termina sem erros de TypeScript."
    ],
    "running": false,
    "blockedBy": [],
    "created_at": "2026-08-29T22:57:17.853Z",
    "moved_at": "2026-08-30T22:26:47.597Z",
    "updated_at": "2026-08-30T22:26:47.597Z",
    "spec_path": "docs/tasks/APRAS-37-spec.md",
    "spec_iterations": 1,
    "last_review_findings": [],
    "completed_at": "2026-08-30T22:26:47.597Z"
  },
  {
    "id": "APRAS-38",
    "title": "Integrate multi-tenancy end to end: tenant switcher UI and tenant_admin experience",
    "status": "done",
    "justification": "Final integration slice of the multi-tenant SaaS direction (captured 2026-08-30): the original APRAS-38 scope was decomposed into APRAS-41 (tenant schema + membership), APRAS-42 (request-scoped resolution + isolation) and APRAS-43 (tenant_admin RBAC), and this task closes the loop with the frontend tenant switcher and the end-to-end behaviour. Kept as APRAS-38 so the APRAS-39/APRAS-40 dependency chains stay valid.",
    "priority": "medium",
    "expected_results": [
      "`GET /api/v1/auth/me` returns today's body plus a `tenants` array of the caller's own memberships (`{tenant_id, name, is_active, is_tenant_admin}`) ordered by tenant name; a dual-membership user who is `is_tenant_admin` in A and a plain member of B gets `true` for A and `false` for B in one response, and a user with no memberships gets `[]` with status 200 - `backend/tests/test_auth_me_tenants.py`, all 6 cases",
      "`UserRead` is unchanged: `GET /api/v1/users/`, `POST /api/v1/auth/signup` and `GET /api/v1/auth/dev-users` responses contain no `tenants` key, and `backend/tests/test_tenant_route_scope.py` passes unmodified with `(\"GET\", \"/api/v1/auth/me\")` still in GLOBAL_ROUTES",
      "The Axios request interceptor (`frontend/src/api/client.ts`) sets `X-Tenant-Id` to the stored acting tenant on every request and omits it when none is set, without disturbing `Authorization` - `src/api/__tests__/client.tenantHeader.test.ts` (3 cases); existing `client.test.ts` passes unmodified",
      "The selection survives a page reload: written to `localStorage` when the JWT is in `localStorage` and to `sessionStorage` otherwise under key `actingTenantId`, and a fresh read of `tenantState` picks it up before the first request - `tenantState.test.ts`, 3 persistence cases",
      "Pre-selection is deterministic and never leaves a multi-membership user headerless: `resolveActingTenantId` keeps a valid stored id, otherwise picks the first active membership, keeps a stored non-membership id for an ADMINISTRATOR, and returns `null` only for a zero-membership user - `tenantState.test.ts`, 4 ladder cases; `AuthContext` applies it before `setUser`",
      "The Navbar renders a tenant `<select>` labelled `tenant.switcherLabel` with exactly one option per active tenant when, and only when, the user has 2+ options: with exactly 1 option it renders no switcher, and with 0 options it renders neither a switcher nor any message - no `tenant.noAccess`, no placeholder, no added text (`TenantSwitcher.test.tsx`, 3 render cases).",
      "Switching tenant swaps the dashboard task list with no stale row: `TenantSwitch.integration.test.tsx::\"shows only the acting tenant's tasks after switching, with no stale rows\"` asserts `Tarefa A` visible, then after `selectOptions` on the switcher `Tarefa B` visible and `queryByText(\"Tarefa A\") === null`",
      "An ADMINISTRATOR's dropdown lists every tenant returned by `GET /api/v1/tenants` (including tenants they are not a member of, minus inactive ones) and switching into one shows that tenant's data - `TenantContext.test.tsx::\"lists every tenant ... for an ADMINISTRATOR\"`, `...::\"filters inactive tenants out of the options\"`, `TenantSwitch.integration.test.tsx::\"an ADMINISTRATOR can switch into a tenant they are not a member of and sees its data\"`",
      "`useAdminCapability()` mirrors `deps.has_admin_capability`: true for a global ADMINISTRATOR in any tenant, true for a tenant_admin only in the granting tenant, and the effective variant is false while a simulation is active - `useAdminCapability.test.tsx`, 3 cases",
      "Route + menu gating follows the capability, per tenant: with `/admin/users` on `requiredCapability=\"admin\"`, a tenant_admin acting in the granting tenant renders `AdminUserDashboard` and sees the `nav.administration` entry; the same user acting in the other tenant is redirected to `/dashboard` and the entry is gone; a global ADMINISTRATOR still renders it - `ProtectedRoute.capability.test.tsx` (3 cases), `Navbar.tenantAdmin.test.tsx` (2 cases)",
      "`requiredCapability` ORs with `requiredRoles`: on `/users/contact-info` a MANAGER without the capability still renders the page - `ProtectedRoute.capability.test.tsx::\"requiredCapability ORs with requiredRoles\"`",
      "A forced 403 from `GET /api/v1/users/` renders `t(\"admin.errorLoadingUsers\")` and throws no uncaught error - `AdminUserDashboard.forbidden.test.tsx`",
      "A UserType belonging to another tenant grants no menu in the acting tenant - `useMenuAccess.crossTenant.test.tsx` (closes the practical risk of the deferred /auth/me user_types residual)",
      "`tenant.switcherLabel` exists in both `src/i18n/locales/pt.json` and `src/i18n/locales/en.json`, and `tenant.noAccess` exists in NEITHER, asserted by `TenantSwitcher.test.tsx::\"defines tenant.switcherLabel in both pt and en, and defines no tenant.noAccess\"`; no existing key is renamed or removed.",
      "`frontend/src/__tests__/AppRouting.smoke.test.tsx` passes unmodified with `TenantProvider` mounted (it mocks only `src/api/client`, so the `/tenants` query rejects and the Navbar renders no switcher and no message), and so do `Navbar.test.tsx`, `ProtectedRoute*.test.tsx`, `AuthContext.test.tsx`, `AdminUserDashboard.test.tsx`, `useEffectiveIdentity.test.tsx`, `useMenuAccess.test.tsx`, `client.test.ts` and `App.test.tsx` - including across the change to the SOURCE file `src/hooks/useUserTypes.ts`, which is invisible to the four of them that vi.mock that module. `useUserTypes.ts`, `client.ts`, `AuthContext.tsx`, `ProtectedRoute.tsx`, `Navbar.tsx` and `App.tsx` are the six pre-existing source files this task edits; no pre-existing TEST module changes, and any that does is named in the report with its reason.",
      "Full pipeline green: `npm run test:coverage` passes the 80/78/76/80 gates with at least 112 files / 1055 tests passed (baseline on 86c038d: 101 files / 1020 tests; this task adds 11 files and 35 named cases); `npm run build` succeeds; `npm run lint` reports no more than the baseline on 86c038d: 467 problems (465 errors, 2 warnings), none of them in a file this task creates; `uv run pytest` is green with >=90% coverage and at least 1070 passed (baseline 1064 collected at 86c038d + the 6 new /auth/me cases); `ruff check` clean on touched files; `alembic heads` still reports the single head `0029_add_is_tenant_admin` with no new migration.",
      "No tenant-scoped request is issued before the acting tenant is resolved: with the real `App` mounted at `/dashboard` for a DUAL-MEMBERSHIP, NON-ADMINISTRATOR user holding a stored token and NO stored `actingTenantId`, the first request is `GET /auth/me` and every request to a tenant-scoped path (`/user-types/` included) is issued with a non-null acting tenant; and with the mocked client REJECTING `/user-types/` with a 400 whenever the acting tenant is null at call time, that user's dashboard still renders the `nav.tasks` link rather than `common.restrictedAccess` (`src/__tests__/TenantBootstrapOrder.test.tsx`, first 2 cases). The gate is `enabled: useActingTenantReady()` on `useUserTypes`; on 86c038d without it the same probe records `[\"/user-types/\",\"/auth/me\"]`.",
      "The capability gate never observes a stale acting tenant at boot: the real `App` mounted at `/admin/users` for a NON-ADMINISTRATOR user who is `is_tenant_admin` in tenant A, holding a stored token and NO stored `actingTenantId`, renders `AdminUserDashboard` and NEVER navigates to `/dashboard` (a sentinel rendered by the `/dashboard` route is asserted never to appear at any point) (`src/__tests__/TenantBootstrapOrder.test.tsx`, third case). The mechanism is `TenantContext` reading `actingTenantId` through `useSyncExternalStore(subscribeActingTenantId, getActingTenantId, getActingTenantId)` over the `tenantState` mirror, so the context value IS the mirror in every commit and no useState/useEffect re-sync lag exists."
    ],
    "running": false,
    "blockedBy": [
      "APRAS-41",
      "APRAS-42",
      "APRAS-43"
    ],
    "created_at": "2026-08-30T14:32:24.587Z",
    "moved_at": "2026-09-01T15:45:36.635Z",
    "updated_at": "2026-09-01T15:45:36.635Z",
    "spec_path": "docs/tasks/APRAS-38-spec.md",
    "spec_iterations": 2,
    "last_review_findings": [],
    "code_review_iterations": 1,
    "completed_at": "2026-09-01T15:45:36.635Z"
  },
  {
    "id": "APRAS-39",
    "title": "Modularizar features com ativação por tenant controlada pelo admin global",
    "status": "done",
    "justification": "Cada funcionalidade do APRAS vira um módulo que pode ser ligado/desligado por associação/condomínio. Capturado em 2026-08-30. Depende de APRAS-38 (o conceito de tenant precisa existir antes de haver algo a ligar por tenant). NOTA 2026-09-01: com a cadeia IAM (APRAS-45..49) aprovada, o ER de composição desta tarefa deve compor com PERMISSÕES/grupos, não mais com UserType.allowed_menus (que será dropado na F5). Módulo desligado no tenant => permissões daquele módulo inertes.",
    "priority": "medium",
    "expected_results": [
      "ER-1 - modules are declared, and every tenant has an enumerable active set. `app/core/permissions.py` exports `MODULES` (derived from `PERMISSIONS`, 26), `CORE_MODULES` (3: `tenants`, `users`, `roles`) and `TOGGLEABLE_MODULES` (23), asserted by `tests/test_module_vocabulary.py`. `GET /api/v1/tenants/{tenant_id}/modules` as a superuser returns 200 with 26 `{module, is_core, is_active}` rows sorted by `module`; a tenant created by `POST /api/v1/tenants` returns 26 rows all `is_active: true` and stores `disabled_modules == []`.",
      "ER-2 - the global superuser, and only the superuser, toggles a module for one tenant. `PUT /api/v1/tenants/{tenant_id}/modules` `{\"disabled_modules\":[\"finance\"]}` returns 200 with `finance.is_active false`, and `{\"disabled_modules\":[]}` restores it. The same call returns 403 `\"The user doesn't have enough privileges\"` for an ordinary user and for the tenant_admin of that very tenant, 401 unauthenticated, 404 for an unknown tenant, and 400 for an unknown module string or for any of the three core modules. Pinned by `tests/test_tenant_modules_api.py`.",
      "ER-3 - a disabled module disappears from the navigation *and* its endpoints refuse. With `finance` disabled in tenant A, a user whose role carries `finance:read`: (a) `GET /api/v1/permissions/me` contains no `finance:*` and lists `\"finance\"` in `disabled_modules`; (b) `GET /api/v1/finance/transactions` answers 403, and `DELETE /api/v1/lots/{id}` answers 403 with `lots` disabled — i.e. both the in-handler and the route-level guard refuse; (c) Vitest: the `/finance` nav link is absent and navigating directly to `/finance` renders the `common.moduleUnavailable` message. Pinned by `tests/test_module_gating.py`, `Navbar.modules.test.tsx` and `ProtectedRoute.modules.test.tsx`.",
      "ER-4 - activation composes with permissions and never replaces them, and never freezes role administration. *Access* requires the module active and the permission held: a user without `finance:read` still gets 403 while `finance` is active; an acting tenant_admin's whole-catalogue set equals `PERMISSIONS` minus exactly the 11 `finance:*` strings while `finance` is off. The disabled module's permissions are inert, not deleted: after a disable → re-enable cycle the role row's `permissions` list is byte-identical — asserted by a direct `SELECT` on the role row after `session.expire_all()`, not through the API — and re-enabling restores 200 with no data step. *Granting* is deliberately not stripped (§5.5): with `finance` off in tenant A, a tenant_admin renaming a seeded role that carries `finance:read` gets 200 (the editor's full resend, `permissions` unchanged in the row), assigning a user to that role gets 200 while that user's `/permissions/me` still contains no `finance:*`, and adding `finance:read` to another role succeeds for an author who holds it unstripped — because `assert_can_grant` / `assert_can_assign_user_types` compare against `get_grantable_permissions` (unstripped) while everything else reads `get_effective_permissions` (stripped). The escalation pins still hold with the module off: an author who lacks `finance:category_create` even unstripped is still 403 with F2's message, on both the grant and the assignment surface, and `SUPERUSER_ONLY_PERMISSIONS` stays un-grantable to everyone. Pinned by `tests/test_module_gating.py` and `tests/test_module_gating_grants.py`, whose `test_the_unstripped_resolver_is_used_only_by_the_escalation_guards` asserts `get_grantable_permissions` is referenced nowhere else in `backend/app/`.",
      "ER-5 - isolation, and the deliberate superuser exemption. One user who is a member of tenants A (`finance` off) and B (`finance` on) gets a 403 with `X-Tenant-Id: A` and a 200 with `X-Tenant-Id: B` on the same endpoint, with no other request difference, and B's `disabled_modules` stays `[]`. A superuser is not filtered: `/permissions/me` returns the whole 159-string catalogue and the finance endpoint answers 200 in tenant A. A session with no acting tenant strips nothing. Pinned by `test_module_isolation_between_tenants`, `test_a_superuser_keeps_a_disabled_modules_permissions` and `test_no_acting_tenant_strips_nothing`.",
      "ER-6 - one small, reversible migration, all-on backfill. `alembic heads` reports exactly one head, `0034_add_tenant_modules`; against a real throwaway Postgres, `upgrade head` adds `tenant.disabled_modules` `JSON NOT NULL DEFAULT '[]'`, every pre-existing tenant (including `00000000-0000-0000-0000-000000000001`) reads `[]`, `downgrade -1` drops the column and a re-`upgrade` succeeds. No data statement in either direction. Pinned by the new `0034` section of `tests/test_migrations_postgres.py`.",
      "ER-7 - the parity world and the enforcement surface do not move. `tests/data/parity_matrix_baseline.json` is byte-identical (absent from `git diff --stat`), `EXPECTED_CELL_COUNT == 1080`, `len(ROUTE_PERMISSIONS)` and `len(PERMISSIONS)` are unchanged from the F5 merge base (predicted 180 / 159), and `test_the_matrix_world_runs_with_every_module_active` asserts every matrix-world tenant has `disabled_modules == []`. `len(UNGUARDED_ROUTES)`, the total route count and `len(GLOBAL_ROUTES)` are each merge base + 2 (predicted 15 / 195 / 20), with the measured base quoted in the PR body. Both exact-set pins on the `deps.get_current_superuser` surface grow by the same two pairs and stay green — `ADMIN_ONLY_ROUTES` in `tests/test_permission_enforcement.py` and its independent duplicate in `tests/test_tenant_admin.py`, 5 → 7 in each, with `test_get_current_superuser_is_exactly_the_five_tenant_writes` renamed `..._is_exactly_the_seven_tenant_routes` in both — and both new routes satisfy them by declaring `Depends(api_deps.get_current_superuser)`: an inlined `if not user.is_superuser` that keeps the pins at five is a rejection, not an implementation.",
      "ER-8 - gates green and i18n at parity. `pytest` ≥ 90 % and `ruff check backend` with zero new findings; `npm run build` passes; Vitest statements/branches/functions/lines ≥ 80/76/78/80 and ≥ the F5 merge-base measurement; `npm run lint` with no new non-test findings; and `src/i18n/__tests__/index.test.ts` asserts the `en`/`pt` key sets are identical and that `modules.names` carries all 26 module labels in both languages. ---"
    ],
    "running": false,
    "blockedBy": [
      "APRAS-38"
    ],
    "created_at": "2026-08-30T14:38:03.352Z",
    "moved_at": "2026-09-03T02:21:38.886Z",
    "updated_at": "2026-09-03T02:21:38.886Z",
    "spec_path": "docs/tasks/APRAS-39-spec.md",
    "spec_iterations": 1,
    "last_review_findings": [
      "CR1 (BLOCKING) AGENTS.md:100 - the new line puts the retired term 'menu gate' in ### Route Map, a section tests/test_docs_agents_md.py::test_agents_md_does_not_describe_the_retired_model (RETIRED_TERMS l.30) forbids; the staged backend suite is 1 failed / 2398 passed, not 0 failed as the report claims. Reword without the retired term (or move the sentence to the permitted section) and re-run the FULL backend suite before reporting.",
      "CR2 (BLOCKING) App.tsx:100-107 vs ProtectedRoute.tsx:98-104 - RootRedirect's fallback chain targets /dashboard and /categories, which carry landingRedirect: true (routeAccess.ts:22-23), so ProtectedRoute bounces the caller straight back to the inaccessible landing_path and §10.4's 'ends somewhere renderable' fails. Reproduced with the PORTEIRO persona (landing /gate, module gate disabled): body renders 'Módulo não contratado'. Fix: the landing-redirect bounce must not fire when the landing itself is inaccessible (disabled module or missing permission) - make ProtectedRoute's landingRedirect check use the same accessibility evaluation RootRedirect uses, and add a test that mounts RootRedirect with the REAL ProtectedRoute/ROUTE_ACCESS and that persona (RootRedirect.test.tsx's bare <div> mounts cannot catch this).",
      "CR3 (BLOCKING) App.tsx:98 - `if (set.isLoading) return <Navigate to={landing ?? '/dashboard'} />` fires on the first render of `/` because useMyPermissions is pending-and-disabled until useActingTenantReady(), so RootRedirect navigates away and never re-evaluates: the chain never runs on the primary path. Deviation 8 REJECTED: render the project's <Spinner /> while the query settles (ProtectedRoute.tsx:103 is the pattern), then evaluate once. Test the cold-load sequence (disabled -> loading -> settled).",
      "NON-BLOCKING, apply all: AGENTS.md:98-99 misattributes landingRedirect to /welcome and /gate (it is on /dashboard and /categories); tenant_service.py:290-303 _to_modules_read must subtract CORE_MODULES so GET agrees with the strip for a hand-edited row; TenantModulesPage.tsx:28-51,158-159 hand-lists the 26 modules and silently hides an ungrouped 27th - derive the groups from the catalogue or fail loudly with a test; i18n/__tests__/index.test.ts:30-56 must fail when the backend catalogue gains a module; types/permissions.ts:21 disabled_modules is always sent - make it required."
    ],
    "completed_at": "2026-09-03T02:21:38.886Z"
  },
  {
    "id": "APRAS-40",
    "title": "Criar área de pagamento/assinatura com contratação de módulos pelo tenant",
    "status": "done",
    "justification": "Área de assinatura por tenant, onde tenant_admin e diretores contratam ou cancelam módulos, com o pagamento definindo o que o tenant pode ativar. Capturado em 2026-08-30. Depende de APRAS-38 (tenant) e APRAS-39 (módulos ligáveis). ATENÇÃO: cobrança real exige uma conta de provedor de pagamento provisionada - mesmo tipo de dependência externa que mantém APRAS-13 bloqueada.",
    "priority": "medium",
    "expected_results": [
      "**ER-1 — a plan governs what a tenant may activate.** `Plan` (global,\n  superuser-managed via four `/api/v1/plans` routes) and `TenantSubscription`\n  (at most one per tenant, `uq_tenant_subscription_tenant`) exist.\n  `PUT /api/v1/tenants/{tenant_id}/subscription {\"plan_id\": …}` as a\n  superuser returns **200** and, for a tenant that was all-on, leaves exactly\n  the plan's `included_modules` active: `GET /api/v1/tenants/{tenant_id}/modules`\n  (APRAS-39) reports every uncovered toggleable module `is_active: false` and\n  `tenant.disabled_modules == sorted(TOGGLEABLE_MODULES - plan.included_modules)`.\n  A tenant with **no** subscription answers `GET /api/v1/subscription` with\n  **200**, `plan: null`, **one module row per `MODULES` entry**\n  (`len(body[\"modules\"]) == len(app.core.permissions.MODULES)`, predicted 27 —\n  the derived expression is the assertion, the number is the prediction),\n  every active toggleable one `source: \"UNMANAGED\"` and every one\n  `can_contract: false` (the contracting `PUT` is a 404 there, §4.6) — i.e.\n  today's all-on behaviour is preserved and adopting billing is opt-in. Pinned\n  by `tests/test_subscription_admin_api.py` and `tests/test_subscription_api.py`.",
      "**ER-2 — tenant-side actors contract and cancel within the plan, and\n  what is charged moves with them.** `GET /api/v1/subscription`,\n  `PUT /api/v1/subscription/modules` and `GET /api/v1/subscription/history`\n  exist, take **no** `{tenant_id}` path parameter (they act on the\n  `X-Tenant-Id` tenant), and are guarded by\n  `require_permission(\"billing:read\" / \"billing:manage\")` against the new\n  catalogue strings. A user holding `is_tenant_admin` on the acting tenant\n  reaches all three with **no special case in the code** (APRAS-47's\n  whole-catalogue short-circuit, `billing` being core), and a\n  \"Diretor\"-profile user reaches them **exactly when the condominium's role\n  row carries the two permissions** — asserted 403-before / 200-after in\n  `test_subscription_access.py::test_a_director_reaches_the_area_once_the_role_grants_it`.\n  `PUT {\"active_modules\": [... + \"finance\"]}` returns 200, `finance` becomes\n  active, `GET /api/v1/permissions/me` gains the 11 `finance:*` strings, and\n  `estimated_monthly_total` rises by exactly `plan.module_prices[\"finance\"]`;\n  removing it reverses all three. A no-op `PUT` is a 200 that appends **no**\n  history row. The body is **the contracted set only**: with a courtesy module\n  and an `OVERRIDE` module also active, a `PUT` that names neither leaves both\n  active (`test_subscription_ceiling.py::test_a_tenant_side_write_preserves_courtesy_and_override_activations`),\n  and `SubscriptionPage.test.tsx::test_the_payload_carries_only_the_contracted_set`\n  asserts the frontend sends exactly the checked plan-covered modules.",
      "**ER-3 — a module outside the subscription cannot be activated from\n  inside the tenant.** `PUT /api/v1/subscription/modules` naming a module not\n  in `plan.included_modules ∪ subscription.courtesy_modules` returns **400**\n  with detail `\"Modules not covered by the subscription: <sorted names>\"` and\n  leaves `tenant.disabled_modules` **byte-identical** — for a tenant_admin\n  and for a plain `billing:manage` holder alike. `PUT` with no subscription\n  row returns **404** `\"This tenant has no subscription\"`. Unknown module\n  strings return 400; core modules in `active_modules` are a silent no-op.\n  Pinned by `tests/test_subscription_ceiling.py`.",
      "**ER-4 — the superuser overrides the plan, and the override is\n  distinguishable three ways.** `PUT /api/v1/tenants/{tenant_id}/subscription/courtesy\n  {\"courtesy_modules\": [\"finance\"], \"reason\": \"negociação\"}` returns **200**\n  and, in one call, activates `finance` outside the plan: `GET\n  /api/v1/tenants/{tenant_id}/modules` shows it `is_active: true`. It is\n  distinguishable from a contracted activation by (a) `source: \"COURTESY\"` in\n  `GET /api/v1/subscription` — never `\"PLAN\"`, per §4.7's priority; (b) a\n  `SubscriptionChange` row with `kind: \"COURTESY_GRANT\"`, its `reason` and\n  its author; and (c) `estimated_monthly_total`, which does **not** move,\n  because courtesy is free. Revoking it deactivates the module unless the\n  plan covers it. APRAS-39's raw `PUT /api/v1/tenants/{tenant_id}/modules`\n  survives unchanged, is **not** bounded by the plan, and any module it\n  activates outside the entitlement reads `source: \"OVERRIDE\"` and appends a\n  `kind: \"OVERRIDE\"` history row. All three superuser subscription routes\n  answer **403** for a tenant_admin — including of that very tenant — **401**\n  unauthenticated and **404** for an unknown tenant.",
      "**ER-5 — the area shows current state and the full change history.**\n  `GET /api/v1/subscription` returns `{plan, status, started_at, notes,\n  modules, estimated_monthly_total, currency}`, where `modules` has\n  **`len(app.core.permissions.MODULES)` entries** (predicted 27), one per\n  catalogue module, sorted by `module`, each carrying\n  `{is_core, is_active, in_plan, courtesy, can_contract, monthly_price,\n  source}` — and `can_contract` is `false` for every module of a tenant with\n  no subscription, because that tenant's contracting `PUT` is a 404 (§4.6). `GET /api/v1/subscription/history` returns the tenant's\n  `SubscriptionChange` rows newest-first, each with `kind` ∈\n  `{CONTRACTED, PLAN_CHANGE, COURTESY_GRANT, COURTESY_REVOKE, OVERRIDE}`,\n  the modules added/removed, both plan ids on a plan change, the reason and\n  the author's name — and **only** that tenant's rows. The history is\n  append-only: no route updates or deletes a `subscription_change` row, and\n  `tests/test_subscription_history.py::test_the_history_is_never_updated_or_deleted`\n  proves `SubscriptionChange` is never passed to `session.delete` anywhere in\n  `backend/app/**`. Vitest: `SubscriptionPage.test.tsx` renders the plan card,\n  one history row per kind, and the module list in **exactly four mutually\n  exclusive row states, resolved by the stated precedence\n  `OVERRIDE > COURTESY > PLAN > out-of-plan`** (§8.4): an `OVERRIDE` row and a\n  courtesy row carry a badge and **no checkbox**, an out-of-plan row carries\n  the \"fora do plano\" hint and no checkbox, and only an `in_plan` row is a\n  checkbox — one named case per state.",
      "**ER-6 — one rule composes with APRAS-39, and the resolver is\n  untouched.** `git diff` touches **no line** of\n  `deps.get_effective_permissions`, `deps.disabled_modules` or\n  `permissions.filter_by_modules`; `tenant.disabled_modules` remains the only\n  input to permission resolution. With\n  `over(t) := (active(t) & TOGGLEABLE_MODULES) - entitlement(t).all`, §4.5's\n  three-part invariant holds and is asserted per part per path: **I1** no\n  APRAS-40 write escalates (`over(t)` never grows, all three paths), **I2**\n  the plan-change path leaves `over(t) == ∅`, **I3** the tenant-side path\n  leaves `over(t)` unchanged. `SubscriptionService.entitlement` is the **single\n  loader**: one query joining `TenantSubscription` and `Plan` (no\n  `Relationship` on the model), returning the frozen\n  `Entitlement(subscription, plan, included, courtesy)` with `.managed` and\n  `.all` as properties, `.all` being the single ceiling and `plan is None` iff\n  `subscription is None`. `build_read` derives **everything it returns** —\n  `plan`, `status`, `started_at`, `notes`, `currency`, and per module\n  `in_plan` / `courtesy` / `can_contract` / `source` / `monthly_price`, plus\n  `estimated_monthly_total` — from that result alone, and its body touches the\n  session not at all: `test_build_read_derives_everything_from_the_entitlement_result`\n  asserts (AST-scoped to the function) no `session.` access of any kind, no\n  `select(`, no reference to `Plan` / `TenantSubscription` /\n  `SubscriptionChange`, and no `.included_modules` / `.courtesy_modules`, with\n  exactly one call to `entitlement`; `test_included_modules_has_at_most_four_homes`\n  asserts (over AST attribute accesses **and** field declarations) that the\n  name `included_modules` occurs in `backend/app/**` in **at most** the four\n  declared places — `models/plan.py`, `schemas/plan.py`,\n  `services/plan_service.py` and, inside `services/subscription_service.py`,\n  only within `entitlement`'s body — and in none of them is `build_read`.\n  `billing` is the **fourth** core\n  module: `CORE_MODULES == {\"tenants\",\"users\",\"roles\",\"billing\"}`,\n  `len(MODULES) == 27`, `len(TOGGLEABLE_MODULES) == 23` (unchanged),\n  `PUT /api/v1/tenants/{id}/modules {\"disabled_modules\":[\"billing\"]}` → 400,\n  and a hand-written row containing `\"billing\"` still yields every\n  `billing:*`. `subscription.status` gates nothing: the same tenant's\n  `/permissions/me` is byte-identical under `ACTIVE`, `SUSPENDED` and\n  `CANCELED`. Entitlement is isolated per tenant: one user in A (plan without\n  `finance`) and B (plan with it) gets 403 with `X-Tenant-Id: A` and 200 with\n  `X-Tenant-Id: B`, nothing else differing.",
      "**ER-7 — one reversible migration, no seeded plan.** `alembic heads`\n  reports exactly one head, `0035_add_subscription_tables`. Against a real\n  throwaway Postgres on port **5436** (never `nexdom`), `upgrade head`\n  creates `plan`, `tenant_subscription` and `subscription_change` with the\n  declared constraints (`uq_tenant_subscription_tenant`, `plan_id` and\n  `tenant_id` `ON DELETE RESTRICT`, `subscription_id` `ON DELETE CASCADE`),\n  **both `plan` and `tenant_subscription` are empty afterwards** (no-seeds\n  doctrine — a fresh install has no plans, so every tenant keeps APRAS-39's\n  all-on default), `downgrade -1` drops all three and a re-`upgrade`\n  succeeds. No data statement in either direction. `tests/test_tenant_models.py`\n  classifies the three new tables — `tenant_subscription` directly scoped via\n  the new `POST_0028_SCOPED_TABLES` constant, `subscription_change` inherited\n  from it, `plan` unscoped — with **both**\n  `test_partition_of_metadata_is_exhaustive` (**53** tables, base + 3) and\n  `test_scoped_tables_have_a_not_null_tenant_id_fk` iterating\n  `scoped_tables | POST_0028_SCOPED_TABLES`, so `tenant_subscription.tenant_id`\n  is shape-checked (NOT NULL, `server_default`, FK to `tenant.id`,\n  `ix_tenant_subscription_tenant_id`); and without editing migration `0028`'s\n  frozen literal, `test_scoped_tables_list_has_27_real_tables` staying at 27.",
      "**ER-8 — no payment provider, and it is provable.** `git diff --stat`\n  shows **no** change to `backend/pyproject.toml` or `frontend/package.json`;\n  `grep -rniE \"stripe|pagarme|pagar\\.me|mercadopago|iugu|asaas|paypal|payment_gateway|checkout_session\" backend/app frontend/src`\n  returns **nothing**; no code in `backend/app/**` makes an outbound HTTP\n  call for billing. Every price field is inert: `base_price`,\n  `module_prices`, `currency` and `estimated_monthly_total` are stored,\n  returned and summed for display only, and the tenant-side page renders an\n  explicit \"valores informativos — nenhuma cobrança é feita\" notice in both\n  languages. Charging, invoices, proration, dunning, trials and tenant-\n  initiated plan changes are out of scope and named as such in\n  `AGENTS.md` — a follow-up task wires a provider once an account exists\n  (same blocker class as `APRAS-13`).",
      "**ER-9 — the accounting moves by exactly the declared deltas, and every\n  superuser route declares a real guard.** (The F2 baseline freeze is ER-10's,\n  not this one's.) Measured at the actual merge base and\n  quoted base-beside-final in the PR body: `len(PERMISSIONS)` **+2**,\n  `len(ROUTE_PERMISSIONS)` **+3**, `len(UNGUARDED_ROUTES)` **+7**, total\n  routes **+10**, `len(GLOBAL_ROUTES)` **+7**, `ADMIN_ONLY_ROUTES` **+7** in\n  **both** independent copies (`tests/test_permission_enforcement.py` and\n  `tests/test_tenant_admin.py`), and `len(REQUEST_BODIES)` /\n  `EXPECTED_REQUEST_BODY_COUNT` **+1** each (predicted 89 → **90**) with\n  `tests/matrix_world.py`'s docstring write-route count moved to match — each\n  exact-set case renamed to its new count, and every one of the seven\n  superuser routes satisfying its pin by **declaring\n  `Depends(api_deps.get_current_superuser)`**; an inlined\n  `if not user.is_superuser` that keeps the pins from moving is a rejection,\n  not an implementation.\n  `test_tenant_admin.py::test_no_tenant_scoped_route_keeps_a_global_admin_guard`\n  stays green.",
      "**ER-10 — the F2 baseline is byte-identical and the 18 new cells live\n  in a second file.** `git diff --name-only <merge-base>..HEAD` does **not**\n  contain `backend/tests/data/parity_matrix_baseline.json`; its sha256 is\n  quoted before and after and is the same string;\n  `test_the_f2_baseline_is_untouched_by_apras_40` pins that sha256 plus\n  `_meta.merge_base_sha == \"02c2025abcda4626569921eafb3863dfc540eb9e\"`,\n  `_meta.cell_count == 1080` and `\"git worktree add\" in _meta.regenerate`; and\n  `test_the_f2_sha_anchors_still_agree_with_the_frozen_file` proves that sha\n  still appears verbatim in `tests/test_legacy_role_permissions.py` and\n  `tests/test_permission_enforcement.py`, neither of which this task edits.\n  The 18 new cells (6 profiles × 3 billing routes) are in the **new**\n  `backend/tests/data/parity_matrix_baseline_40.json`, with its own `_meta`\n  carrying the same five keys, `cell_count: 18`, its own `merge_base_sha` (the\n  `APRAS-39` merge base) and its own `regenerate` — the literal scoped\n  recorder invocation, which contains `--routes` and **no** `git worktree add`\n  — produced by exactly one run of\n  `python -m tests.tools.record_parity_baseline --out tests/data/parity_matrix_baseline_40.json --merge-base <sha> --routes 'GET /api/v1/subscription' --routes 'GET /api/v1/subscription/history' --routes 'PUT /api/v1/subscription/modules'`\n  issued **after `backend/app/` was committed** (the recorder exits 2 on a\n  dirty `app/`), with the command line and its 18 recorded statuses pasted in\n  the PR body. The recorder declares exactly\n  `{\"--out\", \"--overwrite\", \"--routes\", \"--merge-base\"}`, still has no\n  `--allow-dirty`, and refuses with exit 2 to write the frozen F2 path at all\n  (`test_the_recorder_refuses_to_write_the_frozen_f2_baseline`).\n  `test_the_two_baselines_partition_route_permissions_exactly` asserts the two\n  files are **disjoint** and that their route keys union to\n  `set(ROUTE_PERMISSIONS)` **exactly**, with\n  `EXPECTED_CELL_COUNT == 1080 + 18 == len(CELLS) == len(load_union())`; the\n  union loader keys both files the way `CELLS` is keyed, applying `APRAS-49`'s\n  `F5_PATH_RENAMES`, and the additive file carries hygiene checks of its own —\n  `test_the_apras_40_baseline_carries_no_absolute_path_and_no_timestamp` and\n  `test_the_apras_40_baseline_records_only_integer_status_codes`, the F2 pair's\n  bodies over `BASELINE_40_PATH`, with the F2 pair itself unedited. The\n  18 recorded values equal §9.2.4's prediction — 403 on all three routes for\n  the five non-`ADMINISTRATOR` profiles, and 200 / 200 / **404** (never 422)\n  for `ADMINISTRATOR`. Those three `ADMINISTRATOR` cells are reconciled with the\n  matrix's semantic oracle by **extending the predicate, not the exception\n  tables** (§9.2.4.1): `holds()` splits into **three** functions —\n  `bundle()` (the recorded legacy bundle of a profile, the seam `APRAS-49` §11.1\n  re-sources), `holds_by_bundle()` (the F2 predicate, verbatim, over `bundle()`)\n  and `holds()` = \"a `SUPERUSER_PROFILES` profile holds every catalogue\n  permission outside `MATRIX_ADMIN_GAP`, else `holds_by_bundle()` decides\". The\n  extension is proven inert on the recorded matrix:\n  `test_the_superuser_branch_changes_no_f2_verdict` asserts `holds` and\n  `holds_by_bundle` agree on **every** cell outside the three new routes, so all\n  **1080** F2 verdicts are unchanged, and\n  `test_the_admin_gap_is_the_only_bundle_gap_the_superuser_branch_excludes`\n  asserts `GET /api/v1/packages/my-lots` (`packages:my_lots_read`, recorded 403)\n  is the only mapped route `ADMINISTRATOR`'s bundle misses and that its\n  permission equals `MATRIX_ADMIN_GAP & set(ROUTE_PERMISSIONS.values())` — the\n  single cell the gap clause exists for, whose measurement is re-run at the\n  merge base and pasted in the PR body.\n  `test_the_superuser_profiles_are_the_ones_the_world_seeds`\n  reads `SUPERUSER_PROFILES` back off the seeded world. **No\n  `DENIAL_SHAPE_OVERRIDES`, `NON_ROLE_403` or `PERMITTED_422` entry is added or\n  removed**, all three keep their current size and contents, and\n  `test_denial_shape_overrides_is_exactly_six` (`len == 6`, roles `== {\"GUEST\"}`),\n  `test_non_role_403_is_bounded`, `test_permitted_422_is_bounded` and the three\n  `..._has_a_reason` cases stay green **wholly unedited** — they read no\n  baseline, so §9.2.5's loader switch does not reach them.\n  The six `holds()` callers are **not** in that group and are **not** claimed\n  unedited: each is one of §9.2.5's nine `load_union()` call sites, so each\n  changes in **exactly one line** — `baseline = load_baseline()` becomes\n  `baseline = load_union()` — and in no other line: every assertion, every\n  `holds()` call and argument, and the collection each iterates are\n  byte-identical. Three of them\n  (`test_every_denied_cell_is_denied_in_the_baseline`,\n  `test_no_permitted_cell_is_403_in_the_baseline`,\n  `test_no_permitted_cell_is_422_in_the_baseline`) iterate `CELLS` and would\n  `KeyError` on the 18 new cells without the switch; the other three\n  (`test_every_denial_shape_override_is_needed`,\n  `test_every_non_role_403_is_needed`, `test_every_permitted_422_is_needed`)\n  iterate their own dicts, whose keys are all recorded F2 cells, and would stay\n  green on `load_baseline()` — they switch for uniformity, so that every\n  semantic oracle in the module reads one map. A diff showing those six bodies\n  otherwise modified, or showing any of the nine still on `load_baseline()`, is\n  a fail.",
      "**ER-11 — gates green and pt/en at parity.** `pytest` ≥ 90 % and ≥ the\n  merge-base measurement; `ruff check backend` with **zero** new findings;\n  `npm run build` passes; Vitest statements/branches/functions/lines ≥\n  80/76/78/80 **and** ≥ the merge-base measurement for each; `npm run lint`\n  with no new non-test `src/**` findings; and\n  `src/i18n/__tests__/index.test.ts` stays green with `en`/`pt` key sets\n  identical and `modules.names` carrying all **27** module labels in both\n  languages, including `modules.names.billing`. `AGENTS.md` carries the\n  *Assinatura e planos* subsection with §4.1's rule quoted verbatim, the\n  amended *Módulos por tenant* note, the four new endpoint rows and the three\n  `curl` examples.\n\n---"
    ],
    "running": false,
    "blockedBy": [
      "APRAS-38",
      "APRAS-39"
    ],
    "created_at": "2026-08-30T14:38:16.013Z",
    "moved_at": "2026-09-03T18:21:01.197Z",
    "updated_at": "2026-09-03T18:21:01.197Z",
    "spec_path": "docs/tasks/APRAS-40-spec.md",
    "spec_iterations": 3,
    "last_review_findings": [
      "B1 (BLOCKING, round 2) tests/test_migrations_postgres.py:715 (constant :536) - test_inherited_tables_have_no_tenant_id is red on real Postgres: tenant_subscription is the 28th scoped table but TENANT_SCOPED_TABLES was not updated. Also 0035_add_subscription_tables.py:93-95 declares the tenant FK unnamed (Postgres generates tenant_subscription_tenant_id_fkey) while all 27 existing scoped tables use fk_<table>_tenant_id - once registered, test_every_scoped_table_has_the_four_properties (:683-693) goes red until the constraint is named fk_tenant_subscription_tenant_id. Fix both; run the module with TEST_POSTGRES_URL set against a throwaway DB.",
      "B2 (BLOCKING, round 2) tests/test_migrations_postgres.py:1857 - test_effective_permissions_are_unchanged_by_0033 asserts len(post) == 159; APRAS-40 takes PERMISSIONS to 161. Update the pin to 161 (or derive it from len(PERMISSIONS) with an explicit 'unchanged by 0033' comparison against pre) and say so in the accounting table.",
      "B3 (BLOCKING, round 2) AGENTS.md:329-330 still says 27 scoped / 20 inherited; the diff itself asserts 28/21/4 (tests/test_tenant_models.py:259-261, tenant_context.py:69-71). Update the prose. All three blockers exist because test_migrations_postgres.py skips itself without TEST_POSTGRES_URL - in this round run it FOR REAL and quote the pass count from that run.",
      "NON-BLOCKING, apply all: N-1 tests/test_plans_api.py:228 does not discriminate the name-only-PATCH fix (passes with the fix reverted) - make it fail without the fix (assert the JSON columns' updated values or the emitted statements) and tone down the two docstrings; N-2 subscription_service.py:457-459 set_plan still commits twice - one transaction like tenant_service; N-3 add TEST_POSTGRES_URL to .github/workflows/ci.yml with a Postgres service so this module runs in CI (small, self-contained - do it if the workflow already has a services block, otherwise log it); N-4 groupsCovering dedupe; N-5 two pre-existing COM819 in matrix_world.py:917,920 - fix since the file is touched."
    ],
    "completed_at": "2026-09-03T18:21:01.197Z"
  },
  {
    "id": "APRAS-41",
    "title": "Add Tenant model, multi-tenant user membership, and default-tenant backfill migration",
    "status": "done",
    "justification": "First slice of the multi-tenant SaaS feature (split out of APRAS-38, captured 2026-08-30). Establishes the tenant ('associacao/condominio') concept at the data layer: Tenant model, UserTenantLink membership join table (a user can belong to more than one tenant), tenant_id FK on every tenant-scoped table, and admin-only tenant CRUD so tenants can be created and members linked. Later slices (request-scoped resolution, tenant_admin, switcher UI) build on this schema.",
    "priority": "medium",
    "expected_results": [
      "`backend/app/models/tenant.py` defines `DEFAULT_TENANT_ID = UUID(\"00000000-0000-0000-0000-000000000001\")`, `Tenant` (UUID id, unique `name`, `is_active`, timestamps) and `UserTenantLink` (surrogate UUID id, `user_id`, `tenant_id`, unique constraint `uq_user_tenant_link_user_tenant`, `__tablename__ = \"user_tenant_link\"`); all three are exported from `backend/app/models/__init__.py`, and `backend/app/schemas/tenant.py` defines `TenantCreate`, `TenantUpdate`, `TenantRead`, `TenantMemberCreate`, `TenantMemberRead`",
      "Every one of these 27 tables has a NOT NULL `tenant_id` FK to `tenant.id` (ondelete RESTRICT) with a `server_default` of the default-tenant UUID and an `ix_<table>_tenant_id` index: `task`, `category`, `user_type`, `lot`, `resident`, `visitor`, `visitor_authorization`, `access_log`, `announcement`, `document_folder`, `association_document`, `occurrence`, `package`, `reservable_space`, `space_reservation`, `construction_project`, `purchase_request`, `asset`, `inventory_movement`, `access_device`, `finance_category`, `budget_line`, `financial_transaction`, `assembly`, `vote`, `feedback`, `media_asset`",
      "These 20 tables have NO `tenant_id` column and inherit scope through a NOT NULL FK to a directly-scoped parent: `taskcomment`, `taskhistory`, `task_visible_to_link`, `user_user_type_link`, `user_lot_link`, `announcement_media`, `announcement_comment`, `announcement_read_receipt`, `occurrence_timeline`, `document_download_log`, `project_milestone`, `project_update`, `purchase_quote`, `purchase_quote_decision`, `vote_option`, `ballot`, `ballot_rejection`, `lot_voter_eligibility`, `facial_template`, `facial_access_event`; `user` stays global (membership is many-to-many), and a test asserts `direct ∪ inherited ∪ {user, tenant, user_tenant_link} == set(SQLModel.metadata.tables)`",
      "Alembic migration `0028_add_tenant_and_membership` (30-char revision id, `down_revision = \"0027_add_purchase_quotation\"`) creates `tenant` and `user_tenant_link`, adds the 27 `tenant_id` columns, seeds exactly one tenant row whose id equals `DEFAULT_TENANT_ID`, backfills every existing row's `tenant_id` and one `user_tenant_link` row per existing user; `alembic upgrade head` and `alembic downgrade -1` both run cleanly against `postgresql://postgres:postgres@localhost:5436/nexdom`, `alembic heads` reports exactly one head afterwards, and re-running `upgrade head` after the downgrade succeeds; StrEnum-backed columns use `sa.String()`, never `sa.Enum()`",
      "The 8 global uniques become tenant-composite (`category.name`, `user_type.name`, `user_type.role`, `lot.(block, lot_number)`, `occurrence.protocol_number`, `reservable_space.name`, `asset.asset_tag`, `finance_category.(name, type)`) in both the SQLModel `__table_args__` and the migration; `user.email`, `user.cpf`, `access_device.device_key` and `tenant.name` stay globally unique; a Postgres test proves two `category` rows with the same name in different tenants insert while a duplicate within one tenant raises",
      "`POST /api/v1/tenants` returns 201 and `PATCH /api/v1/tenants/{id}` returns 200 for ADMINISTRATOR, and 403 for DIRECTOR, MANAGER, RESIDENT, PORTEIRO and GUEST; `GET /api/v1/tenants` returns all tenants for ADMINISTRATOR and only the caller's own memberships (empty list, not an error, when none) for every other role; `GET /api/v1/tenants/{id}` returns 404 (not 403) to a non-admin non-member",
      "`POST /api/v1/tenants/{id}/members` returns 201 and `DELETE /api/v1/tenants/{id}/members/{user_id}` returns 204 for ADMINISTRATOR and 403 for all other roles; linking a user already linked to another tenant succeeds (multi-membership), and re-linking the same `(user, tenant)` pair returns 409",
      "No file under `frontend/` is modified; `npm run build`, `npm run lint` and Vitest with its coverage gate all still pass",
      "Every existing endpoint behaves exactly as today: no existing test module is edited except `backend/tests/conftest.py` (autouse `default_tenant` fixture only) and additive new cases appended to `backend/tests/test_migrations_postgres.py`, whose existing cases must pass unmodified; and a regression test proves creating a second tenant leaves `get_effective_user_type_ids` output unchanged",
      "`uv run pytest` passes with the 90% coverage gate, with new `tests/test_tenants.py`, `tests/test_tenants_rbac.py`, `tests/test_tenant_models.py` and new Postgres-gated cases appended to `tests/test_migrations_postgres.py` - isolated by function-scoped `isolated_pg_engine` / `pg_engine_at_0027` fixtures that reset the `public` schema on setup and teardown and leave the database at head - covering the model, the migration and every endpoint above"
    ],
    "running": false,
    "blockedBy": [],
    "created_at": "2026-08-31T13:58:12.702Z",
    "moved_at": "2026-08-31T15:19:24.294Z",
    "updated_at": "2026-08-31T15:19:24.294Z",
    "spec_path": "docs/tasks/APRAS-41-spec.md",
    "spec_iterations": 1,
    "last_review_findings": [],
    "completed_at": "2026-08-31T15:19:24.294Z"
  },
  {
    "id": "APRAS-42",
    "title": "Enforce request-scoped tenant resolution and per-tenant query isolation across the backend",
    "status": "done",
    "justification": "Second slice of the multi-tenant SaaS feature (split out of APRAS-38); blocked on APRAS-41 for the tenant schema. Silent leakage is the failure mode: a query missing the tenant filter returns another condo's data with no error, so this task adds a get_current_tenant dependency in backend/app/api/deps.py and threads the acting tenant through every service and endpoint query (task_service, category_service, lot_service, resident_service, announcement_service, document_service, occurrence_service, package_service, reservation_service, project_service, purchase_service, asset_service, finance_service, visitor_service, access_control_service, feedback_service, media_service, voting_service), proven by dedicated cross-tenant isolation tests.",
    "priority": "medium",
    "expected_results": [
      "`backend/app/api/deps.py` exposes `get_current_tenant`; on a tenant-scoped route it returns 400 for a malformed/empty `X-Tenant-Id`, 403 for a non-ADMINISTRATOR sending a tenant they are not a member of, 403 for any role sending an inactive tenant, 404 for an ADMINISTRATOR sending an unknown tenant id, and 200 for an ADMINISTRATOR sending any existing active tenant.",
      "With no `X-Tenant-Id` header: a caller with exactly one membership acts in that tenant (200), a caller with zero memberships acts in the default tenant `00000000-0000-0000-0000-000000000001` (200), and a caller with two or more memberships gets 400.",
      "Every create endpoint stamps `tenant_id` server-side: `POST /api/v1/categories/` by an actor in tenant B persists a row with `tenant_id == B` (201) even when the request body contains `\"tenant_id\": \"<tenant A>\"`; and no `*Create`/`*Update` schema in `backend/app/schemas/` declares a `tenant_id` field.",
      "For a user who is a member of tenant A only, with data seeded in both tenants, every tenant-scoped collection endpoint returns 200 with zero tenant-B rows - at minimum `GET /api/v1/tasks/`, `/api/v1/categories/`, `/api/v1/user-types/`, `/api/v1/lots/`, `/api/v1/announcements`, `/api/v1/documents`, `/api/v1/documents/folders`, `/api/v1/occurrences`, `/api/v1/packages`, `/api/v1/packages/queue`, `/api/v1/space-reservations/`, `/api/v1/reservable-spaces/`, `/api/v1/projects`, `/api/v1/purchase-requests`, `/api/v1/assets`, `/api/v1/inventory-movements`, `/api/v1/finance/categories`, `/api/v1/finance/transactions`, `/api/v1/finance/budget-lines`, `/api/v1/visitors`, `/api/v1/access-logs`, `/api/v1/access-control/devices`, `/api/v1/access-control/events`, `/api/v1/feedback`, `/api/v1/assemblies/` and `/api/v1/votes/`.",
      "The residents collection is `GET /api/v1/lots/{lot_id}/residents` (there is no `GET /api/v1/residents` route): for that tenant-A caller, `GET /api/v1/lots/{tenant_b_lot_id}/residents` returns 404 and `GET /api/v1/lots/{tenant_a_lot_id}/residents` returns 200 with only tenant-A residents. Likewise `GET /api/v1/lots/{tenant_b_lot_id}/authorizations` -> 404, and `GET /api/v1/lots/{tenant_b_lot_id}/voter-eligibility` -> 200 with zero rows.",
      "GET, PATCH/PUT and DELETE by id of a tenant-B resource by that same tenant-A caller return 404 on every scoped resource exposing those verbs - exercised against a harness in which each request gets its own SQLAlchemy Session (as production does), so the 404 comes from the tenant filter and not from a shared session's identity map; `backend/tests/test_tenant_isolation.py` contains a self-test proving two consecutive requests ran on two distinct sessions.",
      "An ADMINISTRATOR sending `X-Tenant-Id: <tenant B>` receives 200 and tenant-B rows on those same list endpoints.",
      "Child tables are protected through their scoped parent: `DELETE /api/v1/announcements/comments/{tenant_b_comment_id}` -> 404, `DELETE /api/v1/announcements/{b}/media/{b_media_id}` -> 404, `PUT /api/v1/projects/{b}/milestones/{b_milestone_id}` -> 404, `GET /api/v1/votes/{tenant_b_vote_id}/tally` -> 404, `GET /api/v1/purchase-requests/summary` counts no tenant-B quote or decision, and `GET /api/v1/access-control/events` returns no tenant-B event.",
      "`GET /api/v1/access-control/residents/{tenant_b_resident_id}/facial-template` returns 404 to a tenant-A administrator (it returns 200 with the tenant-B template on master today), while the same route for a tenant-A resident with no template still returns 200 with a null body.",
      "`POST /api/v1/access-control/webhook/verification` resolves its tenant from the device and never confirms a foreign resident's existence: a tenant-A device key posting a tenant-B `resident_id` returns 201 with `matched=false` and `access_granted=false` - identical to an unmatched face, not a 404 - and persists a `FacialAccessEvent` whose `tenant_id` is the device's tenant A and whose `resident_id` is null, while the tenant-B `FacialTemplate` row still exists in the database. A tenant-A device posting a tenant-A resident with a SYNCED template still returns 201 with `matched=true`.",
      "`backend/tests/test_tenant_isolation.py` exists and automates the cross-tenant matrix above; it fails if any covered endpoint returns a tenant-B row to a tenant-A caller.",
      "`backend/tests/test_tenant_route_scope.py` asserts that every `APIRoute` in `app.routes` either depends (transitively) on `get_current_tenant` or is one of the 17 allowlisted global routes, and that every allowlist entry names a route that exists.",
      "A PORTEIRO in tenant A keeps working within its tenant - `/api/v1/packages/queue` returns 200 with only tenant-A packages - and registering a package for a tenant-B lot returns 404; `backend/tests/test_porteiro_role.py` passes unmodified.",
      "`POST /api/v1/tenants` seeds one role-linked `UserType` per `UserRole` in the new tenant, and `get_effective_user_type_ids` resolves the role-type of the acting tenant (falling back to the default tenant when no tenant is resolved), with `backend/tests/test_role_type_permissions.py` and `backend/tests/test_tenant_no_behaviour_change.py` passing unmodified.",
      "`backend/tests/conftest.py` is the only pre-existing test module modified - in particular `backend/tests/test_access_control.py` passes unmodified, including `test_webhook_unmatched_face`, `test_webhook_matched_and_granted`, `test_webhook_matched_but_resident_inactive` and `test_get_facial_template_status`; the whole suite passes with `uv run pytest` and coverage stays >= 90% on `--cov=app`; `ruff check` is clean on the files this task touches.",
      "No new Alembic migration: `alembic heads` still reports the single head `0028_add_tenant_and_membership`, and `backend/tests/test_migrations_postgres.py` passes unmodified against Postgres on port 5436.",
      "Nothing under `frontend/` is modified; `npm run build` and `npm run test:coverage` still pass, and `npm run lint` reports no new findings."
    ],
    "running": false,
    "blockedBy": [
      "APRAS-41"
    ],
    "created_at": "2026-08-31T13:58:12.713Z",
    "moved_at": "2026-08-31T21:19:05.310Z",
    "updated_at": "2026-08-31T21:19:05.310Z",
    "spec_path": "docs/tasks/APRAS-42-spec.md",
    "spec_iterations": 2,
    "last_review_findings": [],
    "code_review_iterations": 2,
    "completed_at": "2026-08-31T21:19:05.310Z"
  },
  {
    "id": "APRAS-43",
    "title": "Add tenant_admin capability composed with the existing RBAC layers",
    "status": "done",
    "justification": "Third slice of the multi-tenant SaaS feature (split out of APRAS-38); blocked on APRAS-42 for tenant resolution. tenant_admin grants administrator-level permissions restricted to the tenant the user is acting in, and must COMPOSE with the existing layers -- UserRole enum, UserType.allowed_menus menu gate, get_effective_user_type_ids in backend/app/api/deps.py, per-lot UserLotLink scoping -- not replace them. Suggested shape (spec decides): an is_tenant_admin flag on UserTenantLink rather than a new UserRole value, so one user can admin condo A while being a plain RESIDENT in condo B. ADMINISTRATOR keeps global reach and remains the only role that manages tenants themselves.",
    "priority": "medium",
    "expected_results": [
      "`PATCH /api/v1/tenants/{tenant_id}/members/{user_id}` with `{\"is_tenant_admin\": true}` returns 200 with `is_tenant_admin: true` to an ADMINISTRATOR, the same value appears for that user in `GET /api/v1/tenants/{tenant_id}/members`, and `{\"is_tenant_admin\": false}` returns 200 with `is_tenant_admin: false`; the route returns 404 for an unknown tenant, an unknown user, or a user with no membership in that tenant.",
      "The same PATCH returns 403 to every non-ADMINISTRATOR caller, including a user holding `is_tenant_admin` on that very tenant, and `POST /api/v1/tenants`, `PATCH /api/v1/tenants/{id}`, `POST /api/v1/tenants/{id}/members` and `DELETE /api/v1/tenants/{id}/members/{user_id}` also return 403 to a tenant_admin.",
      "A non-ADMINISTRATOR user (role RESIDENT) holding `is_tenant_admin` on tenant A and acting in A passes every admin-gated tenant-scoped route: `GET /api/v1/users/` 200, `PATCH /api/v1/users/{id_of_an_A_user}` 200, `PATCH /api/v1/users/{id_of_an_A_user}/contact-info` 200, `POST /api/v1/user-types/` 201, `PATCH /api/v1/user-types/{A_type_id}` 200, `DELETE /api/v1/user-types/{A_non_role_type_id}` 204, `DELETE /api/v1/tasks/{A_task_id}` 204, `DELETE /api/v1/lots/{A_lot_id}` 204.",
      "The same user acting in a tenant where they are a plain member (`X-Tenant-Id: B`) gets 403 on `PATCH /api/v1/users/{id}`, `PATCH /api/v1/users/{id}/contact-info`, `POST /api/v1/user-types/`, `PATCH`/`DELETE /api/v1/user-types/{id}`, `DELETE /api/v1/tasks/{id}` and `DELETE /api/v1/lots/{id}`; and a plain member of A with no grant gets 403 on those same routes while acting in A.",
      "Acting in tenant A, that tenant_admin never receives tenant-B data: `PATCH /api/v1/users/{id_of_a_user_whose_only_membership_is_B}` -> 404, `PATCH /api/v1/user-types/{B_type_id}` -> 404, and `GET /api/v1/users/` returns exactly the users visible in A (linked to A, or link-less when A is the default tenant) and no B-only user.",
      "`GET /api/v1/users/` applies the same membership filter to an ADMINISTRATOR: acting in the default tenant it excludes a B-only user, and the same administrator sending `X-Tenant-Id: <B>` receives that user and 200. Every `UserRead.user_types` array returned by `GET /api/v1/users/`, `PATCH /api/v1/users/{id}` and `PATCH /api/v1/users/{id}/contact-info` contains only UserType rows of the acting tenant.",
      "A tenant_admin of tenant A, acting in A, is refused every privilege escalation on `PATCH /api/v1/users/{id}` against targets that are visible in A: body `{\"role\": \"ADMINISTRATOR\"}` -> 403; a target whose role is ADMINISTRATOR -> 403; a target that also holds a membership in another tenant -> 403.",
      "An ADMINISTRATOR sending `X-Tenant-Id: <A>` - so that each target is visible in the acting tenant - performs those same three `PATCH /api/v1/users/{id}` requests and gets 200 on all three (the ADMINISTRATOR-role target being a second administrator, not the caller, so the pre-existing self-protection 400s stay unchanged): the escalation rules never fire for a global ADMINISTRATOR. `backend/tests/test_user_admin.py` passes unmodified.",
      "The visibility 404 applies to a global ADMINISTRATOR too: an ADMINISTRATOR sending `X-Tenant-Id: <A>` gets 404 on both `PATCH /api/v1/users/{id_of_a_B_only_user}` and `PATCH /api/v1/users/{id_of_a_B_only_user}/contact-info`, and the same administrator sending `X-Tenant-Id: <B>` gets 200 on both.",
      "`PATCH /api/v1/users/{id}` with a `user_type_ids` entry that does not exist in the acting tenant returns 422 (it returns 200, silently dropping it, on master); with valid acting-tenant ids it returns 200, and a UserType link the target held in another tenant is still present in the database afterwards.",
      "Within their tenant a tenant_admin is exempt from the `allowed_menus` gate exactly as an ADMINISTRATOR is: a RESIDENT tenant_admin whose effective UserTypes contain no `\"tasks\"` menu gets 200 from `GET /api/v1/tasks/` acting in that tenant, and 403 with detail `\"Not enough privileges to access tasks\"` acting in a tenant where they hold no grant.",
      "`get_effective_user_type_ids` returns only the acting tenant's UserType ids for a user holding explicit types in two tenants (role-linked and explicit alike), and `is_acting_tenant_admin` returns False on a session with no acting tenant even for a user holding the flag; `backend/tests/test_role_type_permissions.py` and `backend/tests/test_tenant_no_behaviour_change.py` pass unmodified.",
      "A non-member non-ADMINISTRATOR sending `X-Tenant-Id` for an inactive tenant gets 403 with detail `\"Not a member of the requested tenant\"` - byte-identical to the answer for an active tenant they are not a member of - while a member of that inactive tenant still gets 403 `\"Tenant is inactive\"`, and `backend/tests/test_tenant_isolation.py` passes unmodified.",
      "A test walking `app.routes` asserts that no route depending on `get_current_tenant` also depends on `get_current_active_admin` or `get_current_admin_or_manager`, and that the routes still depending on `get_current_active_admin` are exactly these FIVE writes on the global `/api/v1/tenants` router and no others: `POST /api/v1/tenants`, `PATCH /api/v1/tenants/{tenant_id}`, `POST /api/v1/tenants/{tenant_id}/members`, `DELETE /api/v1/tenants/{tenant_id}/members/{user_id}` and `PATCH /api/v1/tenants/{tenant_id}/members/{user_id}`.",
      "`TenantService.ensure_role_types` is idempotent (a second call on the same tenant inserts nothing) and fills a tenant that has none; `POST /api/v1/tenants` still seeds one role-linked UserType per UserRole in the new tenant, and `python -m app.seed` runs clean against the local Postgres on port 5436 and leaves every tenant with a complete role-type set.",
      "Alembic: exactly one head, `0029_add_is_tenant_admin`; after `upgrade head` `user_tenant_link.is_tenant_admin` is `boolean NOT NULL DEFAULT false` and every row backfilled by 0028 reads false; after downgrading to 0028 the column is gone; `backend/tests/test_migrations_postgres.py` passes against Postgres on 5436 with only the two named assertion changes in `test_downgrade_removes_tenant_schema`.",
      "`uv run pytest` is green with coverage >= 90% on `--cov=app` (baseline on master @ e102660: 995 passed, 17 skipped, 98.21%), `ruff check` is clean on the files this task touches, and the only pre-existing test modules modified are `backend/tests/test_tenant_route_scope.py` (one allowlist entry) and `backend/tests/test_migrations_postgres.py`; the passing count is at least 995 plus the new cases.",
      "Nothing under `frontend/` is modified; `npm run build` and `npm run test:coverage` still pass and `npm run lint` reports no new findings."
    ],
    "running": false,
    "blockedBy": [
      "APRAS-42"
    ],
    "created_at": "2026-08-31T13:58:12.716Z",
    "moved_at": "2026-09-01T03:36:52.330Z",
    "updated_at": "2026-09-01T03:36:52.330Z",
    "spec_path": "docs/tasks/APRAS-43-spec.md",
    "spec_iterations": 1,
    "last_review_findings": [],
    "completed_at": "2026-09-01T03:36:52.330Z"
  },
  {
    "id": "APRAS-44",
    "title": "Implementar gestão de infrações com regras e escalonamento configuráveis por condomínio",
    "status": "done",
    "justification": "Registrar que a unidade X infringiu o art. Y do estatuto/RI/convenção e em que estágio o processo está. Decisões de desenho tomadas com o usuário em 2026-08-31: (1) multa ISOLADA do Financeiro - apenas registra que foi aplicada, com valor e data; nenhum lançamento é criado; (2) uma ocorrência do Livro de Ocorrências pode ser PROMOVIDA a infração mantendo o vínculo, e infração também nasce direto; (3) defesa = REGISTRO apenas: a notificação carrega prazo de defesa configurável por regra e a unidade pode anexar contestação escrita ao processo, sem workflow de julgamento; (4) ADMINISTRATOR/DIRECTOR/MANAGER registram infrações E avançam o processo (aplicam aviso/notificação/multa). Modelo em 3 camadas: catálogo de regras por tenant (artigo, origem Estatuto/RI/Convenção, descrição), política de escalonamento por regra (sequência ordenada de passos AVISO/NOTIFICACAO/MULTA com parâmetros, valor fixo ou múltiplo da taxa, janela de reincidência em dias), e processo por infração com histórico append-only de estágios (autor, data, observação) - o estágio atual é derivado do histórico, nunca um campo editável. O sistema sugere o próximo passo pela política + reincidência; o staff confirma. ADENDO 2026-08-31 (pesquisa juridica): multas punitivas por infracao de conduta sao PESSOAIS (vinculam o infrator, nao o imovel - jurisprudencia predominante; CC art. 1337 'reiteradamente' e da pessoa), ao contrario das cotas/multas moratorias que sao propter rem (CC art. 1345). Portanto NAO ha botao de reset: a infracao referencia lote E responsavel (Resident a epoca), a reincidencia conta por (regra, responsavel) e nao por lote, e venda/troca de inquilino zera a escada naturalmente sem apagar o historico do lote. Multa registrada contra o responsavel. Encerramento de ciclo manual pelo staff cobre troca de morador nao registrada a tempo.",
    "priority": "medium",
    "expected_results": [
      "**ER-1 — Catálogo de regras por tenant.** `POST/GET/PUT/DELETE /api/v1/infraction-rules` gravam e leem artigo, origem (`ESTATUTO` | `REGIMENTO_INTERNO` | `CONVENCAO`) e descrição; um papel com `infractions:rule_create` / `rule_update` / `rule_deactivate` (bundle recomendado: ADMINISTRATOR/DIRECTOR, §8.3) obtém 201 / 200 / 204 e um papel sem essas permissões obtém 403 em cada uma; o mesmo par (origem, artigo) é 409 no mesmo tenant e 201 em outro; `backend/tests/test_infraction_rules.py` passa.",
      "**ER-2 — Política de escalonamento por regra.** `PUT /api/v1/infraction-rules/{rule_id}/policy` grava uma sequência ordenada de passos `AVISO` | `NOTIFICACAO` | `MULTA`, cada `MULTA` com `fine_mode` `FIXED` (valor) ou `MULTIPLE` (multiplicador × a taxa condominial de `GET /api/v1/infraction-settings`), e a regra grava `recidivism_window_days`; duas regras do mesmo tenant com políticas diferentes produzem sugestões diferentes para a primeira infração — `test_infraction_escalation.py::test_two_rules_of_one_tenant_escalate_differently` passa.",
      "**ER-3 — Registro e avanço append-only, com estágio derivado.** `POST /api/v1/infractions` exige `rule_id`, `lot_id`, `responsible_resident_id`, `occurred_on` e `description`, aceita `evidence_urls`, e responde 422 `\"The rule is not active\"` para uma regra desativada e 422 `\"The responsible resident does not belong to this lot\"` para um responsável que não é morador ativo do lote; `POST /api/v1/infractions/{id}/stages` responde 201 gravando autor, data e observação; `InfractionRead.current_stage` é o último estágio do histórico, `Infraction.__table__.columns` não contém `status` nem `current_stage`, e mesmo assim `GET /api/v1/infractions?stage=NONE` (e `=AVISO` / `=NOTIFICACAO` / `=MULTA`) filtra pelo estágio atual derivado; nenhuma chave de `ROUTE_PERMISSIONS` edita ou apaga um estágio; as asserções estão em `backend/tests/test_infractions.py`.",
      "**ER-4 — Sugestão por (regra, responsável), nunca por lote.** `GET /api/v1/infractions/{id}/next-step` responde 200 e o corpo **contém pelo menos** as chaves `recidivism_count`, `window_start`, `cycle_closed_at`, `stages_applied`, `ladder_index`, `suggested_step_order`, `suggested_action`, `reason`, `fine_amount` e `is_saturated`, calculadas pelo §6; quando a regra não tem nenhum passo de política a resposta continua 200 com `suggested_step_order == null`, `suggested_action == null` e `reason == \"NO_POLICY\"`, e nesse caso `POST /api/v1/infractions/{id}/stages` sem `action` responde 409 `\"Rule has no escalation policy\"` enquanto o mesmo POST com `action` explícito responde 201; duas infrações da mesma regra no mesmo lote com responsáveis diferentes têm ambas `recidivism_count == 0` enquanto `GET /api/v1/infractions?lot_id=…` continua devolvendo as duas; `POST /stages` sem `action` aplica a sugestão e com `action` explícito grava `suggestion_followed = false`.",
      "**ER-5 — MULTA registrada e isolada do Financeiro.** Um estágio `MULTA` grava `fine_amount`, `applied_on` e o `responsible_resident_id` da infração, e o valor fica congelado quando a taxa muda depois; `backend/tests/test_infraction_isolation.py` passa, provando por AST que nenhum dos 5 arquivos do módulo importa `app.models.finance`, `app.schemas.finance`, `app.services.finance_service` ou `app.api.v1.endpoints.finance` nem nomeia `FinancialTransaction`/`FinanceCategory`/`BudgetLine`/`FinanceService`, e por contagem de linhas que essas três tabelas têm exatamente o mesmo número de linhas antes e depois do fluxo completo; `git diff --stat` dos quatro arquivos de Financeiro é vazio.",
      "**ER-6 — Promoção de ocorrência, com vínculo navegável nos dois sentidos.** `POST /api/v1/infractions/from-occurrence/{occurrence_id}` responde 201 com `source_occurrence_id` preenchido e escreve uma entrada na `OccurrenceTimeline`; `InfractionRead.source_occurrence_id`/`source_occurrence_protocol` e `OccurrenceDetailRead.infraction_ids` expõem o vínculo dos dois lados; como `Occurrence.lot_id` é anulável e `infraction.lot_id` não é, o lote efetivo é `body.lot_id` quando enviado e o da ocorrência quando não, com 422 `\"The occurrence has no lot; lot_id is required\"` se ambos faltarem e 422 `\"lot_id does not match the occurrence's lot\"` se ambos existirem e forem diferentes; a rota é 403 sem `infractions:promote`; `POST /api/v1/infractions` cria com `source_occurrence_id is None`.",
      "**ER-7 — Prazo de defesa e contestação pela unidade notificada.** Aplicar um passo `NOTIFICACAO` congela `defense_due_on = applied_on + defense_deadline_days` da regra no próprio estágio; `POST /api/v1/infractions/{id}/contestation` responde 201 dentro do prazo para um chamador ligado ao lote da infração via `UserLotLink` ou `Resident` ativo, 409 `\"The defense deadline has passed\"` depois do prazo e 409 `\"No open defense deadline\"` se não houver `NOTIFICACAO`; um chamador **não** ligado àquele lote recebe 403 `\"Only the notified unit may contest this infraction\"` mesmo detendo `infractions:contest` e mesmo sendo `is_superuser` ou `is_tenant_admin`, e nenhuma linha de `infraction_contestation` é gravada; a contestação aparece em `InfractionRead.timeline` com `kind == \"CONTESTATION\"` e não altera `current_stage`.",
      "**ER-8 — Papéis sem acesso, e a visão do morador.** Um papel sem nenhuma permissão `infractions:*` (PORTEIRO/GUEST no bundle recomendado) recebe 403 em **todas as 18 rotas do módulo** — o módulo inteiro, incluindo `/my-lots` e a contestação — o que corresponde às **90 células** (18 rotas × 5 perfis não-`ADMINISTRATOR`) do baseline de paridade; um papel com exatamente `infractions:my_lots_read` + `infractions:contest` recebe 200 em `GET /api/v1/infractions/my-lots` — devolvendo somente infrações dos lotes ligados ao chamador via `UserLotLink`, com `defense_due_on`, e `[]` (nunca 403) quando não há lote ligado — e 403 em `GET /api/v1/infractions`; no frontend, `/infractions` só aparece no menu com `infractions:read` e `/my-infractions` só com `infractions:my_lots_read`.",
      "**ER-9 — Encerramento manual de ciclo, como evento auditável.** `POST /api/v1/infractions/cycles/close` exige `rule_id`, `responsible_resident_id` e `justification` não vazia (422 se vazia) e aceita `lot_id` **opcional**, gravado só como contexto de auditoria e nunca usado no predicado de reincidência; responde 201 gravando autor e data; **a próxima infração registrada depois do encerramento** para aquele (regra, responsável) recebe sugestão com `recidivism_count == 0` e `ladder_index == 1`, enquanto **uma infração já existente mantém** `recidivism_count`, `stages_applied` e `ladder_index` inalterados (o encerramento é um corte para a frente, não uma reescrita); as contagens de linhas de `infraction` e `infraction_stage` são idênticas antes e depois, e o encerramento é listado por `GET /api/v1/infractions/cycles`; a rota é 403 sem `infractions:cycle_close`."
    ],
    "running": false,
    "blockedBy": [
      "APRAS-40"
    ],
    "created_at": "2026-08-31T20:50:20.881Z",
    "moved_at": "2026-09-06T08:06:26.785Z",
    "updated_at": "2026-09-06T08:06:26.785Z",
    "spec_path": "docs/tasks/APRAS-44-spec.md",
    "spec_iterations": 1,
    "last_review_findings": [
      "POST-APPROVAL FIX (orchestrator, before QA) - code review round 3 APPROVED but flagged CycleCloseModal.tsx:18 (and useCloseCycle/useCycleCloses) with no caller: §6.5 / ER-9 manual cycle close is unreachable from the UI, the same defect class as the promotion bridge. Wire it (button on the infraction detail or the cycles view, justification required, closes appear in the history/cycles list) with a test mounting the real router; add lot search by `block` to the modal's lot select (GET /lots/ already accepts block, useLots forwards it) so lot 101+ is registrable and fix the hint text; fix the stale comments naming useSelectableLots / NewInfractionModal.test.tsx; assert the batched source_occurrence_protocol values on the list path (not just the query slope)."
    ],
    "completed_at": "2026-09-06T08:06:26.785Z"
  },
  {
    "id": "APRAS-45",
    "title": "IAM F1: catálogo de permissões e grupos com backfill dos roles atuais",
    "status": "done",
    "justification": "Fatia 1 do modelo IAM (estilo GCP: permissões finas -> roles como pacotes -> atribuição a usuários), decisões do usuário consolidadas em 2026-09-01: permissões são CÓDIGO (registro app/core/permissions.py; convenção read/create/update/delete por módulo + ações de domínio nomeadas como purchases:decide, votes:close, gate:checkin). Roles são DADOS (user_type ganha coluna permissions JSON, migration 0030), SEM aninhamento e SEM permissão avulsa por usuário. DECISÃO FINAL DO USUÁRIO: NENHUM role nasce pronto - nem os 6 espelhos; a tela de roles começa vazia e os padrões serão criados sob demanda testando as telas. A compatibilidade durante a transição (F1-F4) vive num mapeamento EM CÓDIGO do enum user.role para conjuntos de permissões (marcado transitional, morre na F5); nada é seedado em dados. Sem grupos de sistema, sem is_customized, sem doutrina de upgrade. is_tenant_admin permanece flag = todas as permissões do tenant; ADMINISTRATOR global vira is_superuser (F3). SEM enforcement nesta fatia.",
    "priority": "high",
    "expected_results": [
      "ER-1 - the registry. `backend/app/core/permissions.py` exists and exports `PERMISSIONS`, `ROUTE_PERMISSIONS`, `UNGUARDED_ROUTES` and `LEGACY_ROLE_PERMISSIONS`; every permission string matches `^[a-z][a-z0-9_]*:[a-z][a-z0-9_]*$`, uses read/create/update/delete for plain CRUD and named domain actions otherwise (`purchases:decide`, `votes:close`, `gate:checkin`, `packages:pickup`, `assemblies:close`, ...). `tests/test_permission_registry.py::test_every_route_is_declared_or_unguarded` passes and fails when a route is added without a declared permission. Route accounting pinned to measured totals: the app exposes 190 (method,path) keys, so `len(UNGUARDED_ROUTES) == 10`, `len(ROUTE_PERMISSIONS) == 180`, sums to 190 with the two sets disjoint (if the merged APRAS-38 baseline moves the total, the test recomputes from app.main.app and the three numbers move together - the invariant, not the constant, is the requirement).",
      "ER-2 - the column and migration. `user_type` gains a `permissions` JSON column, NOT NULL, `server_default '[]'`, via `0030_add_user_type_permissions` (30-char revision id) on head `0029_add_is_tenant_admin`; `alembic upgrade head` and `downgrade 0029_add_is_tenant_admin` both succeed against Postgres on 5436, and the WHOLE of `tests/test_migrations_postgres.py` passes with TEST_POSTGRES_URL set - the two new 0030 cases AND every pre-existing case, which now migrate one revision further. Because 0030 moves the Alembic head, exactly two pre-existing lines change with it: the expected revision in `test_downgrade_removes_tenant_schema` (line 859) and in `test_is_tenant_admin_column_shape_and_backfill` (line 888) goes from '0029_add_is_tenant_admin' to '0030_add_user_type_permissions', and nothing else in either function changes (same two-line edit 86c038d made for 0029). NOTHING is seeded: after creating a tenant, every `user_type` row in it - including the six role-linked rows - has `permissions == []`, and `SELECT count(*) FROM user_type WHERE permissions::text <> '[]'` returns 0 at head.",
      "ER-3 - the legacy map is derived, not typed. `LEGACY_ROLE_PERMISSIONS` maps all six UserRole values to frozen permission sets, marked transitional (`LEGACY_MAP_REMOVAL_SLICE == \"IAM F5\"` + TRANSITIONAL banner), and `tests/test_legacy_role_permissions.py` passes: every catalogue permission has exactly one parity source, and for every (permission, role) pair membership in the map equals what production decides today. Source discipline bounded explicitly: each source is a RoleSet, RoleSetComplement, Guard or Composite reading the live production constant or callable - RoleSetComplement required for `votes:cast`/`votes:retract` (complement of voting_service.NON_VOTING_ROLES), Composite for the heterogeneous gates on `tasks:read` and `announcements:comment`. Literal is permitted ONLY for the 13 unguarded permissions and the inline gates enumerated in the spec (`tasks:create` plus the Literal halves of the two Composites); enforced by `test_literal_sources_are_allowlisted` and `test_composite_sources_have_a_live_part` (exactly two Composite sources; the authorizations rows are a SINGLE Guard, not a third). Every expected set is computed under the section-6.5 fixture contract (ER-7), so the parity result is reproducible rather than binding-dependent.",
      "ER-4 - the catalogue matches production, including its asymmetries. The parity test passes against the live guards with these rows verified against production: `gate:logs_read` -> ALL SIX roles (GET /access-logs has no _assert_gatekeeper_access; VisitorService.get_access_logs narrows to linked lots and raises only on explicit foreign lot_id); `packages:my_lots_read` -> {RESIDENT} only (PackageService.get_my_lots raises for the four _GATEKEEPER_ROLES and GUEST); and ADMINISTRATOR is a superset of every other role for every permission EXCEPT those in `ADMIN_GAP_PERMISSIONS`, which equals exactly `{\"packages:my_lots_read\"}` - asserted by exact match so a new admin gap or a closed one fails CI. No unqualified superset assertion appears in the suite. Additionally `test_all_six_role_permissions_are_the_documented_seventeen` pins by exact match the 17 permissions that resolve to all six roles - the 13 unguarded, plus `gate:logs_read`, `uploads:delete`, `residents:read` and `authorizations:gate_lookup` - so a binding that silently narrows one of the two lot-linkage rows fails CI with a named diff.",
      "ER-5 - the resolver. `deps.get_effective_permissions(user, session)` returns the union of the permissions of the user's roles in the acting tenant and `LEGACY_ROLE_PERMISSIONS[user.role]`, with no nesting, no per-user permission and no is_superuser/is_tenant_admin shortcut; `tests/test_effective_permissions.py` proves the no-role case equals the legacy set for each role, that role bundles add, that unknown stored strings survive, and that a role belonging to another tenant contributes nothing.",
      "ER-6 - nothing else moves. Measured against the APRAS-38 staged baseline (38's own touches to endpoints/auth.py, services/tenant_service.py, schemas/tenant.py, schemas/user.py are 38's, not this task's): no file under `app/api/v1/endpoints/` or `app/services/` is modified by APRAS-45; no existing test file is edited EXCEPT `tests/test_migrations_postgres.py`, whose diff is exactly the additive 0030 section appended at EOF plus the two head-pin string literals of ER-2 - a third changed line in that file, or any changed line in any other pre-existing test file, fails this ER; the UserType schemas keep exactly their current fields (`UserTypeCreate/Update == {name, allowed_menus}`, `UserTypeRead == {id, name, allowed_menus, role}`, none containing `permissions`); `git diff -- frontend/` is empty. Full backend suite green with >= 1070 collected, 0 failures, coverage >= 98.31% (baseline 1052 passed / 18 skipped; after this task the default run skips 20, the two new Postgres cases joining the 18); `ruff check` clean on every touched file.",
      "ER-7 - the parity fixture is specified, shared and side-effect free. `tests/test_legacy_role_permissions.py` implements section 6.5: `PARITY_SOURCES` holds lazy builders `Callable[[ParityWorld], Source]` (no Source constructed at import time); the session is the existing `tests/conftest.py::session` fixture, never request-scoped and never given an acting tenant, so `has_admin_capability` reduces to `role == ADMINISTRATOR` and `get_current_tenant_admin` resolves to {A}; one function-scoped `parity_world` per catalogue permission seeds the enumerated graph - six role users with is_tenant_admin=False, a Lot with a UserLotLink AND an active Resident for every one of the six, a VisitorAuthorization, an ENQUETE Vote, a Task with visible_to=[] and assigned_to_id=None - plus per-role factories new_feedback() and new_media_asset(role) for the probes that mutate. The three writing families are isolated by construction: fresh world per permission, fresh factory object per role, session.rollback() after every probe, and MediaService(storage_provider=_NullStorage()) so no file system is touched. The parity_world teardown asserts its invariant core survived the case, so a probe that mutated the shared graph fails the case by name. Consequence: the expected sets pinned by ER-3/ER-4 (residents:read = ALL, authorizations:gate_lookup = ALL, packages:read/pickup = A/D/M/R/P, votes:tally_read = A/D/M/R, tasks:read/update/comment including M, feedback:respond = A/D, uploads:delete = ALL) are reproducible by any implementer following 6.5, not artefacts of one author's binding choices."
    ],
    "running": false,
    "blockedBy": [
      "APRAS-38"
    ],
    "created_at": "2026-09-01T12:29:49.462Z",
    "moved_at": "2026-09-01T16:51:38.622Z",
    "updated_at": "2026-09-01T16:51:38.622Z",
    "spec_path": "docs/tasks/APRAS-45-spec.md",
    "spec_iterations": 2,
    "last_review_findings": [],
    "completed_at": "2026-09-01T16:51:38.622Z"
  },
  {
    "id": "APRAS-46",
    "title": "IAM F2: trocar enforcement de roles por permissões com teste de paridade",
    "status": "done",
    "justification": "Fatia 2 do IAM: substituir as guards por role (get_current_active_admin e afins) e as listas por módulo (_LOT_READ_ROLES, TALLY_STAFF_ROLES, matriz de compras, carve-outs de PORTEIRO, escopo own-lot de RESIDENT) por require_permission(...). Casos especiais viram permissões explícitas com escopo em código: morador *:read_own condicionado ao UserLotLink; porteiro gate:operate condo-wide. ANTI-ESCALAÇÃO desde já: ninguém concede grupo contendo permissão que não possui; groups:manage governa criação/edição de grupos. O enum user.role continua existindo mas nenhum check de autorização o lê ao final da fatia. O teste-estrela é a matriz de paridade.",
    "priority": "high",
    "expected_results": [
      "ER-1 - the guard exists and no route depends on a role guard. `deps.require_permission(permission)` returns a `PermissionRequired` dependency that 403s with the detail `\"The user doesn't have enough privileges\"` and is mounted on exactly the seven routes of §5.1 (`DELETE /tasks/{task_id}`, `DELETE /lots/{lot_id}`, `PATCH /users/{user_id}`, `PATCH /users/{user_id}/contact-info`, `POST /user-types/`, `PATCH /user-types/{user_type_id}`, `DELETE /user-types/{user_type_id}`). `tests/test_permission_enforcement.py` passes: `deps` no longer defines `get_current_tenant_admin` or `get_current_tenant_admin_or_manager` and no `APIRoute` reaches either; every `PermissionRequired` found on a route declares `ROUTE_PERMISSIONS[(method, path)]`; `deps.get_current_active_admin` is reachable from exactly the five `/api/v1/tenants` writes (the is_superuser carve-out F3 may adjust) and from nothing else.",
      "ER-2 - no authorization check reads `user.role`, and the arithmetic closes. The AST walker of §8.2 runs over all of `backend/app/` except `app/models/`, `app/schemas/` and `app/seed.py`, under the rule stated once in §8.2: Rule C = one count per `ast.Compare` node containing an `ast.Attribute(attr=\"role\")` whose `.value` is an `ast.Name` in `{current_user, user, admin_user}`; Rule N = one count per such attribute node *outside* any `Compare`. Measured at the merge base (command in §5.0): Rule C = 100, Rule N = 7. `test_no_authorization_check_reads_user_role` asserts the Rule-C `{module::function: count}` mapping equals `ROLE_READS_COMPARE` exactly — 15 reads in 15 functions: 7 is_superuser-marked (F3: `deps.get_current_active_admin`, `deps.get_current_admin_or_manager`, `deps.has_admin_capability`, `deps._resolve_from_header`, `endpoints/users.py::update_user`, `tenant_service::list_tenants`, `tenant_service::get_visible_tenant`), 7 object/visibility-dimension (F5: `deps.assert_manager_can_see_task`, `deps.assert_can_edit_task`, `endpoints/tasks.py::list_tasks`, `task_service::create_task`, `task_service::update_task`, `occurrence_service::_check_user_access`, `occurrence_service::get_occurrences`) and 1 resolver-plumbing read (F5: `deps.get_effective_user_type_ids`, the role-implicit `UserType` lookup `get_effective_permissions` is built on, which cannot be converted without circularity). `test_non_comparison_role_reads_are_allowlisted` asserts the Rule-N mapping equals `ROLE_READS_NON_COMPARE` exactly — 7 reads in 5 functions (`document_service::get_accessible_folder_ids` ×3 on one line, `deps.get_effective_permissions`, `endpoints/lots.py::link_user_to_lot`, `lot_service::get_lot_detail`, `tenant_service::_to_member_read`), all F5. Every entry of both maps carries `slice ∈ {\"F3\",\"F5\"}` and a non-empty reason. `test_role_read_arithmetic_closes` asserts `100 − 85 == 15` and `7 − 0 == 7`, with the 85 itemised per module in §5.4; the two baseline constants sit beside the merge-base sha. A new unlisted read, a listed read that disappears, or a subtraction that stops closing, fails CI.",
      "ER-3 - the exhaustive parity matrix is green with zero divergence. `tests/test_permission_parity_matrix.py` generates `6 × len(ROUTE_PERMISSIONS) == 1080` cells mechanically from `app.main.app` and `ROUTE_PERMISSIONS` (180 permission-mapped keys of the app's 190; the 10 `UNGUARDED_ROUTES` are excluded with the reason stated in the module docstring), issues one real HTTP request per cell against `MatrixWorld`, and asserts each recorded status code equals `tests/data/parity_matrix_baseline.json` — 1080/1080 identical, failures reported as a sorted `(role, method, path, expected, actual)` diff. `test_matrix_covers_every_permission_mapped_route`, `test_every_path_parameter_has_a_binding` and `test_every_write_route_has_a_request_body` (`set(REQUEST_BODIES)` == the 89 POST/PUT/PATCH keys of `ROUTE_PERMISSIONS`; the 24 DELETE routes take no body) all pass, so no cell can be silently dropped and no write cell can be answered by a 422. The harness lives in `tests/matrix_world.py`, is imported unchanged by both the test and the recorder, seeds `MatrixWorld` once before any cell and isolates each cell with an outer transaction plus a `join_transaction_mode=\"create_savepoint\"` session — one mechanism, no fresh-engine fallback — and contains no absolute datetime (every date is an offset from `datetime.utcnow()`).",
      "ER-4 - the baseline is provably pre-swap, by regeneration. `backend/tests/tools/record_parity_baseline.py` is committed and is the only producer of `tests/data/parity_matrix_baseline.json`; it refuses to run when `git status --porcelain -- app/` is non-empty, exiting `2` and writing nothing (no `--allow-dirty` escape), so the baseline cannot be recorded after a production file has been edited or staged — `test_recorder_refuses_a_dirty_production_tree` pins both branches of that check. Any harness gap found later is fixed by fixing `tests/matrix_world.py` and re-recording via `_meta.regenerate`, never by hand-editing the JSON. The JSON carries a `_meta` block with exactly `merge_base_sha` (40 hex, the sha this branch is cut from — the commit that landed `APRAS-45`), `generator`, `harness`, `cell_count == 1080` and `regenerate`, and no timestamp, hostname or absolute path, so the file is a pure function of the tree at that sha (only integer status codes are recorded). Executing the `_meta.regenerate` command — which creates `git worktree add /tmp/apras-parity <merge_base_sha>`, copies in only `tests/matrix_world.py` and `tests/tools/`, runs `python -m tests.tools.record_parity_baseline` there and `diff`s the result — produces a file byte-identical to the committed baseline (`diff` exits 0). No chronological claim is made and none is needed: the task lands as one commit and the proof survives squashing. `test_baseline_file_exists` fails, never skips, when the JSON is missing (`test_the_matrix_module_never_skips` asserts `pytest.skip` does not appear in the module at all); `test_baseline_declares_its_provenance` and `test_baseline_covers_exactly_the_matrix` pass.",
      "ER-5 - the baseline is the legacy map, so the chain closes. Over the same JSON, `test_every_denied_cell_is_denied_in_the_baseline` asserts that every cell whose permission is not in `LEGACY_ROLE_PERMISSIONS[role]` recorded a `403`, unless the cell is in `DENIAL_SHAPE_OVERRIDES` — which is exactly six entries, all GUEST: `GET /api/v1/tasks/` → 200, and `PATCH /api/v1/tasks/{task_id}`, `GET /api/v1/tasks/{task_id}/history`, `GET /api/v1/tasks/{task_id}/comments`, `POST /api/v1/tasks/{task_id}/comments`, `PATCH /api/v1/tasks/{task_id}/comments/{comment_id}` → 404. (`GET /api/v1/votes/{id}/tally` is not among them: `TallyNotAvailableError` maps to 403 at `app/core/exception_handlers.py:157`.) `test_denial_shape_overrides_is_exactly_six`, `test_every_denial_shape_override_is_needed` (a declared entry that is not actually a denied cell with that recorded status fails) and `test_every_denial_shape_override_has_a_reason` pass. If the recorded baseline needs a seventh entry, or a different status for one of the six, that deviation and the production line that causes it are named in the PR body (same clause as the §6.2 isolation failures) and the test is renamed to the new literal count. `test_no_cell_is_5xx_in_the_baseline` asserts no recorded status is `>= 500`, with no allowlist, so a broken world cannot freeze into the golden file; `test_no_permitted_cell_is_422_in_the_baseline` asserts no cell whose permission is held recorded a `422` outside `PERMITTED_422`, which is pinned by exact set equality and bounded by `test_every_permitted_422_is_needed`, `test_every_permitted_422_has_a_reason` (naming the unsent body field or required query parameter) and `test_permitted_422_is_bounded` (`len <= 15`; expected `0`, measured value quoted in the PR body). `test_no_permitted_cell_is_403_in_the_baseline` asserts no cell whose permission is held recorded a `403` outside `NON_ROLE_403`, which is itself pinned by exact set equality, by `test_every_non_role_403_is_needed`, by `test_every_non_role_403_has_a_reason` (each naming a §5.2 dimension) and by `test_non_role_403_is_bounded` (`len <= 40`, measured value quoted in the PR body). Combined with F1's derived-parity test — green at the merge-base commit named in `_meta.merge_base_sha` and quoted in the PR body — this establishes `post-swap == baseline == pre-swap production` and `baseline denials == LEGACY_ROLE_PERMISSIONS`.",
      "ER-6 - the two short-circuits behave as specified. `get_effective_permissions` contains no `role == ADMINISTRATOR` branch: an ADMINISTRATOR resolves through `LEGACY_ROLE_PERMISSIONS`, which is `PERMISSIONS - ADMIN_GAP_PERMISSIONS`, so `GET /api/v1/packages/my-lots` still returns its current status for an ADMINISTRATOR (matrix cell). It does union `TENANT_ADMIN_PERMISSIONS` when `is_acting_tenant_admin` is true; `test_tenant_admin_permissions_are_exactly_the_apras43_routes` pins that constant to the seven APRAS-43 routes via `ROUTE_PERMISSIONS`, and `tests/test_effective_permissions.py` gains 3 cases (bridge grants the seven; grants nothing without an acting tenant; grants nothing in a tenant that did not grant the capability) with no existing case changed. `tests/test_tenant_admin.py` passes unmodified, including `test_tenant_admin_passes_every_admin_gated_route_in_their_tenant` (8 assertions, 200/200/200/201/200/204/204/204) and the two `[403]*7` matrices.",
      "ER-7 - anti-escalation on both grant surfaces. `UserTypeCreate`/`UserTypeUpdate`/`UserTypeRead` carry `permissions` (`list[str] | None = None` on Update, so an omitted field leaves the bundle untouched), validated against `PERMISSIONS` with a 422 on an unknown string. `user_type_service.assert_can_grant` runs on `POST /user-types/`, `PATCH /user-types/{id}` and on the `user_type_ids` path of `PATCH /users/{id}`, and `tests/test_permission_escalation.py` passes all 7 cases of §9.4 — notably: an author holding `user_types:create`/`user_types:update` and not `finance:category_create` gets 403 from `POST /api/v1/user-types/` with `\"permissions\": [\"finance:category_create\"]`, the body names the missing permission, and no row is created; the same author creating a group with only permissions they hold gets 201 and the bundle reads back; assigning an over-privileged existing group through `PATCH /users/{id}` is 403.",
      "ER-8 - special scopes preserved, existing suites unmodified. A RESIDENT still sees only their linked lots' packages and a PORTEIRO is still condo-wide at the gate, proven by the *unmodified* suites: `git status --short -- backend/tests/` (equivalently `git diff --stat HEAD -- backend/tests/`; this task stages and never commits, so the worktree-only `git diff -- backend/tests/` an earlier draft named goes blank as soon as the files are staged and proves nothing) lists only these seven new files — three new test modules (`test_permission_parity_matrix.py`, `test_permission_enforcement.py`, `test_permission_escalation.py`), two new support modules (`matrix_world.py`, `tools/record_parity_baseline.py`), the new `tools/__init__.py`, and one new data file (`data/parity_matrix_baseline.json`, which is not a module) — plus the three F1 modules of §11 — `test_legacy_role_permissions.py`, `test_permission_registry.py` (2 amended assertions) and `test_effective_permissions.py` (additive only). In particular `test_tenant_isolation.py`, `test_tenant_route_scope.py`, `test_tenant_admin.py`, `test_menu_access.py`, `test_role_type_permissions.py`, `test_migrations_postgres.py`, `test_porteiro_role.py`, `test_gatekeeper.py`, `test_packages*.py` and every `test_*_rbac.py` are byte-identical to the merge base and pass.",
      "ER-9 - nothing else moves, and the suite is green. `git status --short -- frontend/` is empty (the same stage-aware form as ER-8); no file is added under `backend/alembic/versions/` and `alembic heads` still reports the single head `0030_add_user_type_permissions`; the four `SCOPE_PERMISSIONS` (`residents:read_any_lot`, `visitors:manage_any_lot`, `occurrences:manage_all`, `uploads:auto_approve`) are the only new permission strings, are not route-mapped, and each has a `LEGACY_ROLE_PERMISSIONS` entry. `uv run pytest --cov=app` is green with 0 failures, and the collected-test count is at least `M − R + 1080 + 30`, where: `M` is `uv run pytest --collect-only -q | tail -1` run in a clean worktree at `_meta.merge_base_sha` (`git worktree add /tmp/apras-mb <merge_base_sha>`) — measured `M = 1260` on the merge-base tree (APRAS-38 + APRAS-45; not inherited from F1's `1052 passed / 18 skipped`); `R = 156` is the net retirement §7.2 forces in `tests/test_legacy_role_permissions.py`, which goes 162 → 6 collected cases (157 deleted — the 152-case `test_legacy_map_matches_the_live_guards` parametrisation plus the five source-shape tests that evaluate `PARITY_SOURCES`, itemised in §7.2's table — and 1 added), because this slice deletes the production role-set constants and guards those cases read; `1080` is one parametrised case per matrix cell; and `30` is a deliberately loose integer floor for the new structural, oracle, provenance, 5xx/422 and escalation cases (this spec names roughly forty such cases, so the floor is a floor and not a target). With the measured values the floor is ≥ 2214. Two mechanical cross-checks accompany it: `uv run pytest --collect-only -q tests/test_legacy_role_permissions.py` reports exactly 6, and no *other* pre-existing test module's collected count decreases (per-module collection compared against the same merge-base worktree). `M`, `R`, the post-change count and `merge_base_sha` are quoted in the PR body. Total coverage is ≥ 90 % and no more than 0.5 pp below the merge-base total, with both measured numbers quoted in the PR body. `ruff check` is clean on every file this task touches."
    ],
    "running": false,
    "blockedBy": [
      "APRAS-45"
    ],
    "created_at": "2026-09-01T12:30:41.027Z",
    "moved_at": "2026-09-01T19:17:52.199Z",
    "updated_at": "2026-09-01T19:17:52.199Z",
    "spec_path": "docs/tasks/APRAS-46-spec.md",
    "spec_iterations": 2,
    "last_review_findings": [],
    "completed_at": "2026-09-01T19:17:52.199Z"
  },
  {
    "id": "APRAS-47",
    "title": "IAM F3: is_superuser global e is_tenant_admin como todas-as-permissões do tenant",
    "status": "done",
    "justification": "Fatia 3 do IAM, refinada pelo usuário em 2026-09-01: o ADMINISTRATOR global vira flag is_superuser no User (fora do sistema de grupos, como root - tudo em qualquer tenant; as 5 rotas globais de /api/v1/tenants passam a exigi-lo). O is_tenant_admin da APRAS-43 PERMANECE como flag no vínculo (decisão do usuário - não vira grupo): o resolver de permissões o trata como 'todas as permissões deste tenant'. Hierarquia final: is_superuser > is_tenant_admin (por tenant) > roles/grupos > permissões.",
    "priority": "high",
    "expected_results": [
      "ER-1 - the column and its migration. `User.is_superuser` exists as `boolean NOT NULL DEFAULT false`. `backend/alembic/versions/0031_add_user_is_superuser.py` is the only new file under `alembic/versions/`, chains on `0030_add_user_type_permissions`, has a revision id of 26 characters (the invariant is ≤ 32, the `alembic_version` column width), and `alembic heads` reports the single head `0031_add_user_is_superuser`. On a real Postgres (local 5436), `tests/test_migrations_postgres.py` passes with its three head-pin literals moved from `\"0030_add_user_type_permissions\"` to `\"0031_add_user_is_superuser\"` (the count is `grep -c '== \"0030_add_user_type_permissions\"'` at the merge base, quoted in the PR body) and an additive EOF section in which `test_is_superuser_column_shape_and_conversion` proves: the column is `boolean` / `is_nullable = NO` / `column_default = false` at head; a row inserted before the migration with `role = 'ADMINISTRATOR'` reads `is_superuser = true` afterwards and one with `role = 'DIRECTOR'` reads `false`; `alembic downgrade 0030_add_user_type_permissions` drops the column and `upgrade head` restores it. `test_0031_does_not_touch_is_tenant_admin` proves `user_tenant_link.is_tenant_admin` has the identical column shape before and after `0031`, and `git diff` shows no change to `0029_add_is_tenant_admin.py` or to the `is_tenant_admin` field.",
      "ER-2 - the two short-circuits, proven on the flag and not on the role. `deps.get_effective_permissions` returns `PERMISSIONS` (the whole catalogue, `packages:my_lots_read` included) whenever `user.is_superuser`, before any tenant is resolved; and returns `PERMISSIONS` when `deps.is_acting_tenant_admin(user, session)`. `app.core.permissions.TENANT_ADMIN_PERMISSIONS` no longer exists. `tests/test_superuser.py` cases 1–7 pass: a `DIRECTOR`-role user with `is_superuser=True` resolves to `PERMISSIONS` while acting in tenant A, while acting in tenant B of which they are not a member, and on a `Session` with no acting tenant; a RESIDENT tenant_admin of A resolves to `PERMISSIONS` acting in A and to exactly `LEGACY_ROLE_PERMISSIONS[UserRole.RESIDENT]` acting in B or with no acting tenant — the non-leakage assertion. `LEGACY_ROLE_PERMISSIONS` is unchanged, so `test_admin_gap_list_is_exact` still passes unmodified.",
      "ER-3 - the five global writes read the column. `deps` exposes `get_current_superuser` (403 detail `\"The user doesn't have enough privileges\"`, byte-identical to the guard it replaces) and no longer defines `get_current_active_admin` or `get_current_admin_or_manager`; the renamed structural tests in both `tests/test_tenant_admin.py` and `tests/test_permission_enforcement.py` show it is reachable from exactly `POST /api/v1/tenants`, `PATCH /api/v1/tenants/{tenant_id}`, `POST /api/v1/tenants/{tenant_id}/members`, `DELETE /api/v1/tenants/{tenant_id}/members/{user_id}`, `PATCH /api/v1/tenants/{tenant_id}/members/{user_id}` and from nothing else, and from no tenant-scoped route. On all five: a `DIRECTOR`-role user with `is_superuser=True` succeeds; an `ADMINISTRATOR`-role user stored with `is_superuser=False` gets 403; a tenant_admin of the target tenant gets 403. Granting and revoking `is_tenant_admin` via `PATCH /api/v1/tenants/{tenant_id}/members/{user_id}` is 403 for a tenant_admin and 200 for a superuser, and `tests/test_tenant_admin.py`'s behavioural tests — the seven admin-gated routes, both `[403]*7` matrices and the two menu-gate cases — pass unmodified.",
      "ER-4 - superuser is not grantable, and a tenant_admin cannot reach it. `UserUpdate` has no `is_superuser` field: `PATCH /api/v1/users/{id}` with `{\"is_superuser\": true}` from a superuser returns 200 and leaves the target's flag `False`. `app.core.permissions.SUPERUSER_ONLY_PERMISSIONS` equals `{ROUTE_PERMISSIONS[k] for k in the five keys above}` — `{\"tenants:create\", \"tenants:update\", \"tenants:members_manage\", \"tenants:members_set_admin\"}` — asserted by `test_superuser_only_permissions_are_exactly_the_four_tenant_permissions`, and `user_type_service.assert_can_grant` returns 403 (naming the offending strings) for any group containing one of them, on `POST /user-types/`, `PATCH /user-types/{id}` and the `user_type_ids` path of `PATCH /users/{id}`, for every author including a superuser; no row is created. A tenant_admin sending `{\"role\": \"ADMINISTRATOR\"}` gets 403 with the merge-base detail string, and editing any superuser (including a `DIRECTOR`-role one) gets 403 with the merge-base detail string `\"Tenant administrators cannot modify an administrator\"`. Promoting a user to ADMINISTRATOR through `PATCH /users/{id}` sets `is_superuser=True` and demoting them clears it (§7.3).",
      "ER-5 - every F3-marked role read is gone, and the ledger closes. `tests/test_permission_enforcement.py` passes with `ROLE_READS_COMPARE` reduced to the 8 entries of §5.3(a) — all carrying `slice == \"F5\"`, asserted literally so `\"F3\"` can never appear again — `ROLE_READS_NON_COMPARE` unchanged at 7 reads in 5 functions, and `test_role_read_arithmetic_closes` asserting `RULE_C_BASELINE (100) - CONVERTED_COMPARE (92) == 8` and `RULE_N_BASELINE (7) - 0 == 7`, with F3's merge-base sha beside the moved constant. The AST walkers, not a grep, are the acceptance criterion here: a textual scan of `backend/app/` for `role == UserRole.ADMINISTRATOR` and its siblings is a PR-body observation (quote the residual hits and why each is a data table, a payload read or a target read), never a pass/fail gate — it cannot distinguish an actor read from a payload read, which is the entire distinction this slice turns on. Two new pins hold the declarative layer: `test_the_declarative_layer_has_no_actor_role_read` (the Rule-C and Rule-N walks over `app/models/`, `app/schemas/` and `app/seed.py` are both empty) and `test_the_superuser_default_is_the_only_administrator_literal_in_models` (the `UserRole.ADMINISTRATOR` references under `app/models/` are exactly `{\"user.py::__init__\": 1}`).",
      "ER-6 - the parity matrix is green against the SAME baseline, not a new one. `tests/test_permission_parity_matrix.py` passes unmodified, 1080/1080, and `git status --short -- backend/tests/` shows no change to `tests/matrix_world.py`, `tests/tools/` or `tests/data/parity_matrix_baseline.json`. In particular the cell `(ADMINISTRATOR, GET, /api/v1/packages/my-lots)` is still the baseline's 403 — `PackageService.get_my_lots` refuses on `has_permission(\"packages:queue_read\")` (`{A, D, M, P}`) before the `packages:my_lots_read` branch a superuser now holds is ever reached, so granting the whole catalogue moves no cell (§10). Running F2's `_meta.regenerate` command still produces a byte-identical file (`diff` exits 0).",
      "ER-7 - the widening's two consequences are pinned, not discovered. `test_a_resident_tenant_admin_is_routed_to_the_queue`: for a RESIDENT holding `is_tenant_admin` in the acting tenant, `GET /api/v1/packages/my-lots` returns 403 with the detail *\"Use /packages/queue para ver encomendas de todos os lotes.\"* and `GET /api/v1/packages/queue` returns 200 — the same behaviour an ADMINISTRATOR living in the condominium already has, so no information is lost. `test_a_tenant_admin_upload_is_auto_approved`: a photo uploaded by the same caller is approved rather than pending. Both are named in the PR body as intended behaviour changes for capability holders.",
      "ER-8 - nothing else moves, and the suite is green. `git status --short -- frontend/` is empty; `UserRead` has no `is_superuser` field and `GET /api/v1/auth/me` is byte-identical (§8). The pre-existing test modules that change are exactly the five named in §9.2 (plus `tests/test_permission_registry.py` only if F2 landed the `TENANT_ADMIN_PERMISSIONS` pin there — report which); in particular `tests/conftest.py`, `tests/test_tenant_isolation.py`, `tests/test_menu_access.py`, `tests/test_role_type_permissions.py`, `tests/test_user_directory_scope.py`, `tests/test_legacy_role_permissions.py` and every `test_*_rbac.py` are byte-identical to the merge base and pass. `uv run pytest --cov=app` is green with 0 failures; total coverage is ≥ 90 % and no more than 0.5 pp below the merge-base total; the collected-case count is ≥ M − 8 + 15, where `M` is `uv run pytest --collect-only -q | tail -1` measured in a clean worktree at F3's merge base (`git worktree add /tmp/apras-mb <F2_SHA>`), and `15` is a deliberately loose floor for §9.1's twenty new cases and §9.2's two migration cases. The `8` is a ceiling on the cases §9.2 retires, derived rather than guessed: the deleted `TENANT_ADMIN_PERMISSIONS` pin (1, or its parametrisations); F2's escalation cases 1, 3, 4, 5 and 7, which are rewritten rather than deleted but may collapse or split as their author changes (≤ 5); and `test_effective_permissions.py`'s bridge case, likewise rewritten (1) — 7, rounded to 8. A shortfall below `M − 8 + 15` means a case vanished that this spec did not authorise: name it in the PR body. The exact retired and added counts, not just the inequality, are quoted in the PR body. `M`, the post-change count, the coverage pair and `<F2_SHA>` are quoted in the PR body, together with `CONVERTED_COMPARE`'s old and new values. `ruff check` is clean on every file this task touches. The PR body also records, as a note for F5 and not as work done here, the prose references to the guards this slice renames or deletes that survive inside test files it does not otherwise touch — at this merge base `tests/test_menu_access.py:295` (\"`get_current_active_admin` already 403s them first\", in a docstring) and `tests/test_tenants_rbac.py:4` (\"behind the existing `deps.get_current_active_admin` guard\", in the module docstring). Both are comments, both keep their modules byte-identical, and neither is edited here: rewriting a docstring in an untouched module would break the byte-identical list above for no behavioural gain. Locate them at the merge base with `grep -rn 'get_current_active_admin\\|get_current_admin_or_manager' backend/tests/` and quote whatever the actual hits are.",
      "ER-9 - the architecture doc tells the truth about the capability. `AGENTS.md`'s \"Tenant administrator\" section states that `is_tenant_admin` grants every catalogue permission inside the granting tenant and nothing outside it; no longer names the deleted `deps.get_current_tenant_admin` / `deps.get_current_tenant_admin_or_manager` (`grep -c 'get_current_tenant_admin' AGENTS.md` → `0`); carries an adjacent paragraph documenting `user.is_superuser` (migration `0031`, `deps.get_current_superuser`, not grantable via the groups API, not settable via `UserUpdate`) marked `TRANSITIONAL (IAM F3 -> F5)`; and states that a caller who is not a superuser cannot grant ADMINISTRATOR or modify a superuser. The section contains no sentence denying the widened meaning (§12 point 5): the \"does not grant the domain-service role sets\" clause is gone — `grep -c 'not\\*\\* grant the domain-service' AGENTS.md` → `0` — while the same bullet's `UserLotLink` clause and its tenant/membership-management clause survive verbatim in `git diff`, and its closing `ADMINISTRATOR` only now reads superuser only and names `deps.get_current_superuser`. Every remaining `ADMINISTRATOR` hit inside the section — `sed` the section out and `grep -n ADMINISTRATOR` it — is enumerated in the PR body and is either the menu-gate exemption comparison or the *name of a role in a payload*; none is a statement about who holds global power, and none denies the tenant_admin a permission it now holds. `git diff -- AGENTS.md` touches that section only — no hunk outside it, in particular none in the role table or the file map — and `grep -c 'get_current_superuser' AGENTS.md` is ≥ 1."
    ],
    "running": false,
    "blockedBy": [
      "APRAS-46"
    ],
    "created_at": "2026-09-01T12:30:41.069Z",
    "moved_at": "2026-09-01T23:40:01.266Z",
    "updated_at": "2026-09-01T23:40:01.266Z",
    "spec_path": "docs/tasks/APRAS-47-spec.md",
    "spec_iterations": 2,
    "last_review_findings": [],
    "completed_at": "2026-09-01T23:40:01.266Z"
  },
  {
    "id": "APRAS-48",
    "title": "IAM F4: UI de grupos de permissão e gating do frontend por permissão",
    "status": "done",
    "justification": "Fatia 4 do IAM: a experiência estilo AWS que o usuário pediu. Tela de Grupos de permissão (criar, clonar - inclusive grupos de sistema, marcar permissões por módulo, gerenciar membros nos DOIS sentidos: adicionar usuários ao grupo e grupos ao usuário). Menus e rotas do frontend passam a depender de hasPermission; DECISÃO DO USUÁRIO: visibilidade de menu DERIVA das permissões (menu visível <=> alguma permissão do módulo) - o allowed_menus deixa de ser consultado (dropado na F5). O formulário de usuário perde o dropdown de cargo e ganha grupos. /auth/me devolve as permissões efetivas do tenant ativo (compõe com o switcher da APRAS-38). REFINAMENTO 2026-09-01: sem aninhamento de roles (role contém só permissões; clonar cobre composição); sem permissão avulsa por usuário - a tela de usuário atribui N roles, nada além.",
    "priority": "high",
    "expected_results": [
      "ER-1 - the groups administration area exists. `/admin/groups` lists every group with its permission and member counts and offers create, clone and delete; `/admin/groups/:groupId` edits the name, the permission set as checkboxes grouped in one `<fieldset>` per module, and the members; role-linked groups (`role != null`) render the `groups.roleLinkedBadge` badge, expose no delete control and have a `readOnly` name input, while their permissions stay editable; a member can be added or removed from the group screen and the same groups can be assigned from the user screen, both through `PATCH /api/v1/users/{id}` with `user_type_ids`. `npx vitest run src/features/user-administration/__tests__/GroupsAdminPage.test.tsx src/features/user-administration/__tests__/GroupDetailPage.test.tsx src/features/user-administration/__tests__/GroupMembersPanel.test.tsx` passes, including `marks role-linked groups and offers no delete control`, `clones a group with its permission bundle pre-filled`, `disables permissions the author does not hold`, `adds a member through PATCH /users/{id}` and `never revokes a menu key the group already had` (saving a group stored with `allowed_menus: [\"tasks\"]` and zero `tasks:*` permissions still sends `allowed_menus` containing `\"tasks\"`).",
      "ER-2 - the API returns the acting tenant's effective permissions and the frontend gates menus and routes on them. `GET /api/v1/permissions/me` answers `{tenant_id, permissions[]}` equal to `sorted(deps.get_effective_permissions(user, session))` for the tenant in `X-Tenant-Id`, and `GET /api/v1/permissions/` returns all `len(PERMISSIONS)` catalogue entries with `module`, `action` and `superuser_only`; `backend/tests/test_permissions_api.py` passes, with `test_me_returns_the_effective_permissions_of_the_acting_tenant` proving the same user gets different sets in two tenants. In the frontend, a menu link and its route share one `ROUTE_ACCESS` rule (`routeAccess.test.ts::every NAV_ITEMS entry reuses the ROUTE_ACCESS rule of its path`) and a menu is shown iff the rule is satisfied by the fetched permission set; `Navbar.permissions.test.tsx` passes for a morador, a porteiro and a custom group holding no permission of a module (`hides Financeiro from a custom group with no finance permission`). Authorization is decided on the real (non-simulated) permission set while menus follow the simulated one: `ProtectedRoute.permissions.test.tsx::keeps route access on the real permission set while simulating` passes — an administrator holding `user_types:update`, while simulating a `RESIDENT` whose groups grant none of it, still renders `/admin/groups`, and the Navbar in the same tree shows the RESIDENT menu set. Finally, run from `frontend/`: `grep -rn \"requiredRole=\\|requiredRoles=\\|requiredCapability=\\|requiredMenu=\" src` produces no output, and `grep -rln \"UserRole\\.\" src --include='*.tsx' --include='*.ts' | grep -v __tests__ | sort` produces exactly these 22 lines and no others: `src/App.tsx`, `src/features/announcement-feed/components/AnnouncementCard.tsx`, `src/features/announcement-feed/components/AnnouncementFeedPage.tsx`, `src/features/announcement-feed/components/CommentThread.tsx`, `src/features/assembly-voting/components/AssemblyVotingPage.tsx`, `src/features/assembly-voting/components/LotVotingAdminPanel.tsx`, `src/features/asset-management/components/AssetsInventoryPage.tsx`, `src/features/feedback-management/components/FeedbackChannelPage.tsx`, `src/features/lot-management/components/LotsPage.tsx`, `src/features/purchase-management/components/PurchaseRequestDetailModal.tsx`, `src/features/purchase-management/components/PurchaseRequestsPage.tsx`, `src/features/space-reservation-management/components/ReservableSpacesPage.tsx`, `src/features/space-reservation-management/components/SpaceBookingPage.tsx`, `src/features/task-management/components/CategoriesPage.tsx`, `src/features/task-management/components/TaskForm.tsx`, `src/features/task-management/utils/simulatedPermissions.ts`, `src/features/user-administration/components/Navbar.tsx`, `src/features/user-administration/components/ProtectedRoute.tsx`, `src/features/user-administration/components/SimulationControls.tsx`, `src/features/user-administration/context/tenantState.ts`, `src/features/user-administration/context/useMenuAccess.ts`, `src/hooks/useUsers.ts` — i.e. today's 24 minus the deleted `src/features/user-administration/context/useAdminCapability.ts` and minus `src/features/user-administration/pages/AdminUserDashboard.tsx`, whose only `UserRole.` uses are the deleted role `<select>`'s options.",
      "ER-3 - the user form asks for groups, not for cargo, and a tenant switch re-evaluates without a reload. `AdminUserDashboard` contains no role `<select>` and never sends `role` in `PATCH /api/v1/users/{id}` (the backend needs no change: `UserUpdate.role` is already optional and there is no `POST /users/`); the edit modal's group list is headed `admin.editGroups`. `TenantSwitch.integration.test.tsx::re-evaluates menus after a tenant switch without a reload` passes: switching from a tenant whose permissions include `finance:read` to one that does not removes the Financeiro link with no page reload.",
      "ER-4 - anti-escalation is visible in the UI and the 403 is friendly. In the group editor a permission is rendered `disabled` iff it is `superuser_only` or absent from the author's own set — the mirror of `assert_can_grant` — while an already-granted permission the author cannot grant stays checked and is resent unchanged (`GroupDetailPage.test.tsx::keeps an already-granted permission the author cannot grant checked and resends it`); a 403 from `POST/PATCH /user-types/` or `PATCH /users/{id}` renders a localised message naming the offending permissions by their labels, never the raw backend sentence (`permissionErrors.test.ts`, 5 cases, including the `parseApiError` fallback).",
      "ER-5 - the gates are green and nothing else moved. `npm run build`, `npm run lint` (≤ 467 problems, 0 new outside test files) and `npm run test:coverage` (≥ 80 statements / 78 functions / 76 branches / 80 lines) all pass; `src/__tests__/AppRouting.smoke.test.tsx` passes unmodified and `src/__tests__/TenantBootstrapOrder.test.tsx` passes with its three assertions unchanged. The whole frontend suite is green, including the two files that mount a bare `Navbar` and would otherwise throw `No QueryClient set`: `npx vitest run src/features/purchase-management/__tests__/PurchasesRouteGating.test.tsx src/features/user-administration/__tests__/TenantSwitcher.test.tsx src/features/user-administration/__tests__/useEffectiveIdentity.test.tsx` passes, with `/purchases` and its \"Cotações de Compra\" link still granted to ADMINISTRATOR/DIRECTOR/MANAGER and denied to RESIDENT/PORTEIRO/GUEST, all four `TenantSwitcher` cases unchanged, and all seven `useEffectiveIdentity` cases unchanged (its subject gains a docstring sentence and no code). `src/i18n/__tests__/parity.test.ts` proves `en.json` and `pt.json` have identical flattened key sets; `pytest` is green with the backend's 90 % gate, `alembic heads` still reports the single head `0031_add_user_is_superuser` (no migration in this slice) and `git diff --stat backend/tests/data/parity_matrix_baseline.json` is empty. --- *(End of Expected Results. Nothing below this line is an acceptance criterion.)*"
    ],
    "running": false,
    "blockedBy": [
      "APRAS-47"
    ],
    "created_at": "2026-09-01T12:30:41.096Z",
    "moved_at": "2026-09-02T14:07:01.308Z",
    "updated_at": "2026-09-02T14:07:01.308Z",
    "spec_path": "docs/tasks/APRAS-48-spec.md",
    "spec_iterations": 1,
    "last_review_findings": [],
    "completed_at": "2026-09-02T14:07:01.308Z"
  },
  {
    "id": "APRAS-49",
    "title": "IAM F5: renomear grupos para role, dropar enum user.role e allowed_menus",
    "status": "done",
    "justification": "Fatia final do IAM: só aqui o nome muda, para nunca conviverem dois significados de role. A API/UI passam a chamar grupo de permissões de role (rename de user_type na API e tabela via migration final); a coluna enum user.role é dropada (is_superuser e grupos cobrem tudo); allowed_menus é dropado (menus derivam de permissões desde a F4); AGENTS.md, seed e scripts de demo reescritos para o modelo novo. TRAVA DE SEGURANÇA (decisão sem-espelhos de 2026-09-01): a migração que dropa o enum user.role deve RECUSAR rodar (falha explícita listando os usuários) enquanto existir usuário ativo com zero roles atribuídos e sem is_superuser/is_tenant_admin - ninguém perde acesso por acidente. O mapeamento transitional LEGACY_ROLE_PERMISSIONS da F1 morre aqui.",
    "priority": "high",
    "expected_results": [
      "ER-1 - the entity is `role` in the API, the UI and the database, and `user_type` is gone. `/api/v1/roles` serves list/create/patch/delete with `{role_id}` path params and `RoleRead {id, name, permissions, landing_path}`; the frontend administers them at `/admin/roles` and `/admin/roles/:roleId` with pt \"Papel/Papéis\" and en \"Role/Roles\"; tables `role`, `user_role_link(role_id)` and `task_visible_to_link.role_id` exist in Postgres after `alembic upgrade head`; `alembic downgrade -1` from `0032` restores every old name. From the repository root, `grep -rn \"user_type\\|UserType\\|user-types\\|userType\" backend/app frontend/src` returns no output, and `backend/tests/test_role_rename.py` passes.",
      "ER-2 - `user.role`, `user_type.role`, `allowed_menus` and the `userrole` type are dropped by a reversible migration, applied to a real Postgres, and `alembic heads` reports one head. After `alembic upgrade head` on the throwaway database: `SELECT column_name FROM information_schema.columns WHERE table_name='user'` contains no `role`; the same query on `role` contains neither `role` nor `allowed_menus`; `SELECT 1 FROM pg_type WHERE typname='userrole'` returns no row; `alembic heads` prints exactly `0033_drop_user_role_and_menus`. After `alembic downgrade -1`, all three columns and the six-label `userrole` type are back; `document_folder`'s column is named `allowed_roles_json` again and carries `0010`'s `server_default`; the six legacy rows' `permissions` are back to their pre-`0033` values; every link `0033` created is gone while every pre-existing link remains; and `SELECT to_regclass('f5_backfill_journal')` returns NULL. Roles read back for the link-less majority, not only for the linked few — over the §7.5 fixture personas: an ordinary, pre-`0033`, link-less `DIRECTOR` (no `user_role_link` row before the migration, the membership having been implicit) reads back as `DIRECTOR`; a `MANAGER` with a pre-existing explicit link to `Gerente (papel)` reads back as `MANAGER` and keeps that link; a link-less `RESIDENT`, `PORTEIRO` and `GUEST` read back as themselves; §11.2's persona 10 (`is_superuser = true`) reads back as `ADMINISTRATOR`; and no user reads back as `GUEST` who was not a `GUEST` before. The document-folder ACL round-trips: `document_folder` is renamed to `allowed_role_ids_json` holding role ids after `alembic upgrade head` (`SELECT allowed_role_ids_json FROM document_folder` parses as a JSON list of ids that exist in `role`, of that folder's tenant), and the `(folder, allowed roles)` pairs read pre-`0033`, mapped-back post-`0033` and read post-`downgrade -1` are identical. `tests/test_migrations_postgres.py` passes with `TEST_POSTGRES_URL` set (and skips wholesale without it), including `test_0033_downgrade_restores_the_schema_and_recomputes_roles` and `test_0033_document_folder_acl_round_trips`; and `tests/test_document_folder_acl.py` passes.",
      "ER-3 - the migration refuses by name in both directions — no user widened, no user locked out — and when it runs clean, permissions are unchanged and the legacy map is gone. `test_migrations_postgres.py::test_0033_refuses_a_user_explicitly_linked_to_a_foreign_legacy_role` seeds a legacy MANAGER with an explicit link to `Diretor (papel)`, runs `alembic upgrade head`, and asserts it fails with that user's email and the role name in the error message and that nothing was written (the legacy rows' `permissions` are still `[]`, `to_regclass('f5_backfill_journal')` is NULL, `user.role` still exists); with the link removed it succeeds. `test_0033_refuses_when_an_active_user_has_no_role_and_no_flag` seeds an active, non-superuser, non-tenant-admin user with no `user_tenant_link`, runs `alembic upgrade head`, and asserts it fails with that user's email in the error message; with the membership restored it succeeds. `test_effective_permissions_are_unchanged_by_0033` asserts that for the ten seeded users of §11.2 — one per legacy role (link-less), one with an extra non-legacy role, one explicitly linked to the legacy row matching its own enum, one so linked in a second tenant, all nine seeded `is_superuser = false`, plus a tenth persona seeded `is_superuser = true` — `get_effective_permissions` after the migration equals, exactly: for the nine, the recorded pre-migration bundle union `{tasks:read_all, tasks:update_any, occurrences:read_assigned}` restricted to that role (157 strings for the `ADMINISTRATOR` persona); and for the tenth, the whole 159-string catalogue via the superuser short-circuit. It also asserts the backfill created no duplicate `user_role_link` row. `grep -rn \"LEGACY_ROLE_PERMISSIONS\\|_LEGACY_ROLES_BY_PERMISSION\" backend/app` returns no output.",
      "ER-4 - seed, signup and dev-login work on the new model, and the demo comes up with three working profiles. `python -m app.seed` against a migrated database exits 0 and creates `admin@apras.com` (`is_superuser`, member of `Administrador (papel)`), `diretor1@apras.com` (`Diretor (papel)` + `Diretor Comercial`) and `gerente1@apras.com` (`Gerente (papel)` + `Gerente Operacional`), each with a default-tenant membership and no role column in sight; `POST /api/v1/auth/dev-login` for each of the three returns 200 and the token's `/api/v1/permissions/me` returns the profile's bundle; `POST /api/v1/auth/signup` creates an inactive user with zero roles and an empty permission set. `tests/test_seed.py::test_the_three_demo_profiles_have_the_recorded_bundles` passes.",
      "ER-5 - `AGENTS.md` describes the model that exists and no longer presents the old one as current. It documents: permissions in code (159 strings, 26 modules, `ROUTE_PERMISSIONS` / `UNGUARDED_ROUTES`); roles as data (one flat `permissions` list per row, no nesting, no per-user permissions, no system roles, nothing seeded with permissions); `is_superuser`, its one API surface `PATCH /api/v1/users/{user_id}/superuser` and its relationship to `is_tenant_admin`; the object-level permissions that replaced the role tiers; the `f5_backfill_journal` table and when it is safe to drop; both of `0033`'s refusals; a `### What is gone` section; and the migrate/restore runbook of §9.3. `tests/test_docs_agents_md.py::test_agents_md_does_not_describe_the_retired_model` passes: `UserRole`, `allowed_menus`, `user_type`, `UserType` and \"menu gate\" appear in `AGENTS.md` only inside `### What is gone` or the runbook block.",
      "ER-6 - every gate is green, the isolation and parity proofs hold, and the enum is grep-clean. `pytest` passes at ≥ 90 % backend coverage; `npm run test`, `npm run build` and `npm run lint` pass with each Vitest threshold ≥ its gate and ≥ the F4 merge-base measurement, and no new non-test lint finding; `ruff check backend` adds no finding. `tests/test_permission_parity_matrix.py` passes with `backend/tests/data/parity_matrix_baseline.json` byte-identical (`git diff --stat` on that path is empty), the four route paths re-keyed only through the declared `F5_PATH_RENAMES` literal. `tests/test_tenant_isolation.py`, `test_porteiro_role.py`, `test_gatekeeper.py`, `test_packages_rbac.py`, `test_residents_rbac.py`, `test_visitors_rbac.py` and `test_authorizations.py` pass with their assertions unchanged (construction-only edits). `tests/test_permission_enforcement.py::test_no_actor_role_read_survives` asserts both allowlists are empty over all of `app/`. `grep -rn \"UserRole\\|allowed_menus\\|MenuKey\" backend/app frontend/src` returns no output.",
      "ER-7 - `is_superuser` keeps a grant *and* a revoke path, superuser-only, and the last superuser cannot be demoted. `PATCH /api/v1/users/{user_id}/superuser` with body `{\"is_superuser\": true|false}` returns 200 and `{id, email, is_superuser}` for a superuser caller and flips the target's flag in the database; returns 403 (detail `\"The user doesn't have enough privileges\"`) for a tenant admin and for an ordinary user, and 401 unauthenticated; returns 400 with detail `\"The last superuser cannot be demoted\"` — leaving the flag unchanged — when the revoke would leave zero active superusers, including when the caller is demoting themselves. `(\"PATCH\", \"/api/v1/users/{user_id}/superuser\")` is in `UNGUARDED_ROUTES` and not in `ROUTE_PERMISSIONS`. It is the only route and the only allowlist entry this slice adds, so — counted from the APRAS-48 merge base, which is 192 routes / 180 mapped / 12 unguarded per APRAS-48 §3.3, not the pre-F4 190/180/10 — `len(UNGUARDED_ROUTES) == merge base + 1 == 13` and total routes `== merge base + 1 == 193`, while `len(ROUTE_PERMISSIONS) == 180` is unchanged and the parity baseline stays byte-identical (ER-6). If the merge base has moved when F5 lands, the `base + 1` invariant and the unchanged 180 are what must hold, and the PR body quotes the measured base next to the predicted 13/193. The route is classified tenant-scoped — it is mounted inside the `users` router, which carries `Depends(get_current_tenant)` — so it is not in `tests/test_tenant_route_scope.GLOBAL_ROUTES` and `len(GLOBAL_ROUTES) == 18` is unchanged. `UserUpdate` has no `role` and no `is_superuser` field, and `GET /api/v1/users/` payloads carry no `is_superuser` key. `tests/test_superuser_grant.py` passes with all twelve cases of §8.4, and `tests/test_permission_escalation.py::test_a_tenant_admin_cannot_grant_is_superuser_by_any_route` passes."
    ],
    "running": false,
    "blockedBy": [
      "APRAS-48"
    ],
    "created_at": "2026-09-01T12:30:41.128Z",
    "moved_at": "2026-09-03T00:16:11.131Z",
    "updated_at": "2026-09-03T00:16:11.131Z",
    "spec_path": "docs/tasks/APRAS-49-spec.md",
    "spec_iterations": 2,
    "last_review_findings": [
      "CR1 (BLOCKING) backend/app/api/v1/endpoints/roles.py:87 - `db_type.landing_path = role_in.landing_path` is unconditional while RoleUpdate.landing_path defaults to None (schemas/role.py:81): a PATCH omitting the field writes NULL. RoleDetailPage.tsx:34-49 sends only {name, permissions}, so any edit from the shipped UI wipes the /gate and /welcome values that migration 0033 backfills (0033:578-584) and silently regresses the PORTEIRO/GUEST landing redirects §10.2 marks exact. Guard with the same None sentinel the sibling field uses (schemas/role.py:76-80), add the omitted-field case to tests/test_landing_path.py (mirroring test_permission_escalation.py:211), and either expose a landing-path control in RoleDetailPage or leave it read-only but never destructible.",
      "NON-BLOCKING S1-S6, apply all: S1 0033 downgrade recreates role.role as the native `userrole` enum where 0018 deliberately used VARCHAR - restore VARCHAR; S2 eight dead `allowed_menus=` kwargs in five test modules (contradicts deviation 1) - remove; S3 the new role_ids self-guard compares across all tenants instead of the acting tenant (over-restrictive) - scope to the acting tenant with a test; S4 legacy_role_bundles.json `regenerate` recipe cannot reproduce the file - fix the recipe and prove it reproduces byte-identically; S5 two stale references to deleted modules - remove; S6 `allowed_role_ids` unvalidated - validate existence + same tenant with 422/404 as the sibling validations do."
    ],
    "completed_at": "2026-09-03T00:16:11.131Z"
  },
  {
    "id": "APRAS-50",
    "title": "Rodar os testes de migração em Postgres real no CI",
    "status": "done",
    "justification": "backend/tests/test_migrations_postgres.py (53 casos: guardas de recusa, round-trip de cada migração, propriedades das tabelas tenant-scoped) se auto-pula sem TEST_POSTGRES_URL e .github/workflows/ci.yml não tem bloco services:, então nunca roda no CI. Três bloqueios da APRAS-40 (tabela scoped não registrada, FK sem nome, pin de PERMISSIONS obsoleto) só foram pegos porque o revisor rodou o módulo à mão. Registrado no docs/suggestions-log.md em 2026-09-03.",
    "priority": "high",
    "expected_results": [
      "**ER-1 — CI brings up a UTF-8 Postgres 16, hands it to pytest, and can be\n      run manually on any branch.** `.github/workflows/ci.yml` declares the job\n      `backend-migrations` (\"Backend Migration Tests (Postgres)\") with a\n      `services:` container on `postgres:16-alpine` (the same major as\n      `docker-compose.yml` and AGENTS.md), a `pg_isready` health check\n      (`--health-interval 10s --health-retries 5`),\n      `POSTGRES_INITDB_ARGS: \"--encoding=UTF8 --locale=C\"`, `ports: 5432:5432`,\n      `timeout-minutes`, and job-level\n      `TEST_POSTGRES_URL=postgresql://postgres:postgres@localhost:5432/apras_test`\n      exported before the pytest step; a step before pytest prints\n      `server_encoding=UTF8` from `psql \"$TEST_POSTGRES_URL\" -tAc \"SHOW\n      server_encoding\"` and fails the job on any other value, because migration\n      `0028` seeds the non-ASCII `Condomínio Padrão`. The workflow's `on:` block\n      gains `workflow_dispatch:` while its `push`/`pull_request` filters stay\n      `[ master ]`, so `gh workflow run ci.yml --ref <branch>` is possible and\n      no new automatic runs are introduced.",
      "**ER-2 — the job is shown running all 53 cases green in CI, and cannot\n      silently go back to skipping.** `.meridian/reports/APRAS-50-inprogress-0.md`\n      carries, under `Proof of failure (throwaway branch, deleted)`, the URL of\n      the **green run** — the first of two pushes on the throwaway branch\n      `apras-50-proof`, whose only temporary content is the branch's own\n      `push:` trigger line — together with that run's `53 passed` (0 skipped,\n      0 failed) summary line for `Backend Migration Tests (Postgres)` and the\n      guard step's `collected=53 skipped=0` line. In the merged workflow, that\n      guard step runs with `if: always()` and invokes\n      `backend/scripts/assert_no_skips.py` on the job's\n      `migration-test-results.xml`, selecting `testcase` elements whose\n      `classname` is `tests.test_migrations_postgres`, printing\n      `collected=<n> skipped=<m>`, and exiting non-zero when `n < 53`, when\n      `m > 0`, or when the XML file is absent (with a legible message rather\n      than a traceback) — so a run in which the service is unreachable and every\n      case self-skips fails instead of passing green.",
      "**ER-3 — the guard is shown failing on the same throwaway branch, and\n      nothing temporary survives.** A second push on `apras-50-proof`, adding\n      one commit that removes the `ix_infraction_recidivism` creation from\n      migration `0036`, produces a **red run** in which `Backend Migration Tests\n      (Postgres)` fails — `test_0036_creates_the_recidivism_index` among the\n      failures, plus at least one expected cascade failure (a downgrade case or\n      `test_0036_matches_the_model_metadata`) — while `Backend Tests` in that\n      same run passes. `.meridian/reports/APRAS-50-inprogress-0.md` carries,\n      under the same heading and distinguishable from the green run, that red\n      run's URL, the failing test ids with an assertion excerpt, the green\n      `Backend Tests` outcome, both temporary commits' shas, and confirmation\n      that `apras-50-proof` no longer exists on `origin`. On the branch state\n      submitted for merge,\n      `git diff --name-only origin/master...HEAD -- backend/app backend/alembic`\n      prints nothing.",
      "**ER-4 — local runs without Postgres stay green, and CI does not get\n      ~3 min slower.** On the final branch, `cd backend && SECRET_KEY=test uv\n      run pytest` with `TEST_POSTGRES_URL` unset passes with exactly 53 skips,\n      all from `tests/test_migrations_postgres.py`, and satisfies\n      `--cov-fail-under=90`; the `skipif` marker and the unreachable-database\n      `pytest.skip` are unchanged. `.meridian/reports/APRAS-50-inprogress-0.md`\n      quotes three durations — `Backend Tests` on the master baseline run,\n      `Backend Tests` on ER-2's green run (unchanged within noise), and\n      `Backend Migration Tests (Postgres)` on that same green run — and shows\n      the workflow's wall clock growing ≲ 3 min; or, if it does not, records the\n      excess in that report and in `docs/suggestions-log.md` rather than\n      absorbing it by disabling cases.",
      "**ER-5 — AGENTS.md documents both local paths and names CI as the source\n      of truth, and no production file moves.** AGENTS.md gains the\n      `backend-migrations` bullet under `## CI/CD` (mentioning the guard and\n      `workflow_dispatch`) and a \"Running the Postgres migration tests locally\"\n      subsection containing the `initdb -E UTF8` recipe, the Docker recipe, the\n      `SHOW server_encoding` check, the `DROP SCHEMA public CASCADE` warning\n      against pointing `TEST_POSTGRES_URL` at the dev database on 5436, and the\n      divergence caveats (local PG14 vs CI PG16; CI's `--locale=C` vs\n      `docker-compose.yml`'s `en_US.utf8`); `backend/tests/test_docs_agents_md.py`\n      still passes. `git diff --name-only origin/master...HEAD` lists no path\n      under `backend/app/` and no file in `backend/alembic/versions/`.\n\n---"
    ],
    "running": false,
    "blockedBy": [],
    "created_at": "2026-09-06T12:57:42.782Z",
    "moved_at": "2026-09-06T15:22:58.076Z",
    "updated_at": "2026-09-06T15:22:58.076Z",
    "spec_path": "docs/tasks/APRAS-50-spec.md",
    "spec_iterations": 2,
    "last_review_findings": [
      "F1 (BLOCKING, round 2) ER-3/ER-4 - the commit message body does not exist when QA checks the ERs (commit happens after QA; specialists never commit). DECISION: the ER clauses name .meridian/reports/APRAS-50-inprogress-0.md (a 'Proof of failure' section with run URLs, durations, branch-deleted confirmation) as the artifact QA verifies; the commit-body carry-over stays in prose as an orchestrator hand-off (the orchestrator writes the commit with a body for this task), never as a QA condition. State in §4.1/§4.5 that the ORCHESTRATOR appends the proof section to the developer's report after performing the pushes/dispatch, before QA is dispatched.",
      "F2 (BLOCKING, round 2) ER-2 vs ER-3 - the only pre-merge CI run is the proof run, which must fail, so ER-2's '53 passed' log has no run to come from. DECISION: two pushes on the same throwaway branch apras-50-proof, both by the orchestrator: (1) a trigger-only commit (the task's real ci.yml plus the temporary `push: branches: [ master, apras-50-proof ]` line) -> GREEN run = ER-2's artifact and ER-4's measured duration C; (2) the break commit on top -> RED run = ER-3's artifact; then the branch is deleted from origin. Each ER names its run (URL recorded in the report). ER-4's C is measured on the green run.",
      "NON-BLOCKING, apply all seven from the report: §4.3 misattributes the temp-trigger check to ER-3 (only ER-1 catches it); say who appends the proof section and when (orchestrator, see F1); give §7's 'real PG16 bug' branch a terminal state (task BLOCKED, reported, not silently fixed); fix §4.4's cascade count (double-counted test_0036_round_trips, omitted test_0036_matches_the_model_metadata); the rest as listed."
    ],
    "completed_at": "2026-09-06T15:22:58.076Z"
  },
  {
    "id": "APRAS-51",
    "title": "Alinhar guard e mapeamento de permissão em todas as rotas de ROUTE_PERMISSIONS",
    "status": "done",
    "justification": "QA da APRAS-49 achou GET /api/v1/roles/ e QA da APRAS-40 achou GET /api/v1/tasks/ mapeados em ROUTE_PERMISSIONS (roles:read, tasks:read) mas guardados só por get_current_user: um usuário com zero permissões efetivas recebe 200 (reproduzido com o módulo tasks desligado pelo teto da 40). O mapa é a fonte de verdade do parity matrix e dos menus derivados; a guard tem que corresponder. Precisa de um teste estrutural que percorra o mapa inteiro para o desvio não voltar.",
    "priority": "high",
    "expected_results": [
      "**ER-1 — Every mapped route is in a declared enforcement form, and the\n  exception allowlist is empty.** `backend/tests/test_permission_alignment.py`\n  walks all 201 `ROUTE_PERMISSIONS` entries and places each in exactly one\n  declared form — `PermissionRequired(P)` in `route.dependant` (53),\n  `get_current_superuser` + `P ∈ SUPERUSER_ONLY_PERMISSIONS` (5), the\n  three-entry `MEMBERSHIP_GATED` (§5), the five-entry `SERVICE_ENFORCED` (§4.4),\n  or the derived in-handler bucket (135) — asserting 53+5+3+5+135 == 201 and\n  `UNENFORCED == frozenset()` by name; and the 193-case complement sweep\n  asserts each swept route refuses a caller holding the whole catalogue except\n  its mapped permission with **403**, or with the status its `REFUSAL_SHAPES`\n  entry declares (exactly two entries, both the `assert_manager_can_see_task`\n  404s), never a 2xx, with every `REFUSAL_SHAPES` key required to be a swept\n  route. The new module builds its own world from the unmodified\n  `matrix_world` helpers; `sha256(backend/tests/matrix_world.py)` is unchanged.",
      "**ER-2 — The revealed routes refuse a non-holder, and the five\n  service-enforced routes keep their documented behaviour.**\n  `GET /api/v1/roles/`, `GET /api/v1/tasks/` and the other 23 routes of §3\n  answer **403** to an authenticated member whose role carries no permission\n  and who holds neither flag, and answer their success code to a holder, to an\n  `is_superuser`, and to an `is_tenant_admin` of the acting tenant. The two\n  ballot routes answer 403 with detail `\"Este perfil não vota\"` **and** write a\n  `BallotRejection(reason=ROLE_FORBIDDEN)` row for a non-holder; the three\n  packages routes answer 403 to a caller holding neither the mapped permission\n  nor `packages:queue_read`, and their success code to a `packages:queue_read`\n  holder who lacks the mapped permission.",
      "**ER-3 — The parity delta is exactly three cells, and the frozen files\n  do not move.** `backend/tests/data/parity_matrix_baseline_51.json` is\n  recorded by the committed recorder at its own new `merge_base_sha` with 150\n  cells (25 routes × 6 profiles) and overrides the F2 file for those keys via a\n  derived `OVERRIDDEN_CELLS`; a test asserts its diff against\n  `parity_matrix_baseline.json`, keyed through `cells_of`/`F5_PATH_RENAMES`, is\n  exactly `{(GUEST, GET, /api/v1/tasks/): 200→403,\n  (GUEST, POST, /api/v1/tasks/{task_id}/comments): 404→403,\n  (GUEST, PATCH, /api/v1/tasks/{task_id}/comments/{comment_id}): 404→403}`,\n  that every corrected cell is 403 and denied by `holds()`, and that\n  `parity_matrix_baseline.json`, `_40` and `_44` are byte-identical (sha256\n  pins unchanged).",
      "**ER-4 — The counts move deliberately, the production impact is on the\n  record, and the suite is green.** `PERMISSION_GUARDED_ROUTES` is 53 (a\n  fourth, APRAS-51 group of 25 below the existing three),\n  `DENIAL_SHAPE_OVERRIDES` is 3, `EXPECTED_CELL_COUNT` and `len(load_union())`\n  are still 1206 with no `APRAS_51_CELL_COUNT` addend,\n  `test_the_three_baselines_partition_route_permissions_exactly` additionally\n  asserts `fifty_one == APRAS_51_ROUTES` and `fifty_one <= f2`,\n  `UNGUARDED_ROUTES` is still 22, `ROUTE_PERMISSIONS` still 201, `PERMISSIONS`\n  still 174; `AGENTS.md` carries the enforcement sentence and\n  `test_docs_agents_md.py` asserts it; the PR body reproduces §9's production\n  impact statement (intended loss of access for post-F5 roles lacking the\n  permission, 403 plus the existing friendly denial, no backfill and no\n  operator communication) and the three parity cells with old → new;\n  `cd backend && TEST_POSTGRES_URL=... uv run pytest` is green at ≥ 90%\n  coverage and `ruff check`/`ruff format --check` are clean.",
      "**ER-5 — The frontend loses nothing and no legacy role changes.**\n  `git diff --stat` is empty for `frontend/src/` outside test files and for\n  `frontend/src/i18n/`; `npm run lint`, `npm run build` and `npm run test` are\n  green with coverage ≥ the base run; `ROUTE_ACCESS` and `NAV_ITEMS` are\n  unchanged — including `\"/packages\": { module: \"packages\" }`, which already\n  admits both `packages:read` and `packages:queue_read` (§8) — and every test\n  profile still reaches every screen it reached before;\n  `sha256(backend/tests/data/legacy_role_bundles.json)` is unchanged."
    ],
    "running": false,
    "blockedBy": [],
    "created_at": "2026-09-06T12:57:42.810Z",
    "moved_at": "2026-09-07T01:50:27.598Z",
    "updated_at": "2026-09-07T01:50:27.598Z",
    "spec_path": "docs/tasks/APRAS-51-spec.md",
    "spec_iterations": 1,
    "last_review_findings": [
      "CR1 (BLOCKING) backend/app/api/deps.py:572 - PermissionRequired's docstring still says it is used on 'exactly the seven routes'; it is now on 53 (spec §4.1 l.208-213 required the amendment; deps.py is not in the diff and the omission is not a declared deviation). Fix the docstring to describe the real rule (any route in ROUTE_PERMISSIONS whose enforcement form is D; count asserted by test_permission_enforcement.py) and pin it with a test that greps the docstring for the retired 'seven' claim (or asserts the sentence names the constant), so the next author is not misled by the guard class itself.",
      "NON-BLOCKING, apply all eight: drop 'idem' as an accepted SECOND_GATE_DIMENSIONS value (test_permission_alignment.py:527/:221/:256) - name the real dimension; replace the request_as duplicate (:704-734) by importing/reusing matrix_world.run_cell if that does not require importing build_world/matrix_run (if it does, keep the copy but say why); remove the tautological assertion at :665; fix AGENTS.md:811 orphaned antecedent; pin the five form counts in AGENTS.md:784-786 via test_docs_agents_md.py like 174/201; rename the stale test names at test_permission_parity_matrix.py:113 and :441; add a PR-body line beside §9 listing the five ownership/self-service routes that now additionally require the mapped permission; document (report + a code comment) that POST /space-reservations/{id}/reject is double-gated on reservations:reject (route) + reservations:approve (service) with no parity cell to notice - keep the behaviour, log a follow-up in docs/suggestions-log.md is the orchestrator's job."
    ],
    "completed_at": "2026-09-07T01:50:27.598Z"
  },
  {
    "id": "APRAS-52",
    "title": "Expor o histórico de assinatura do tenant com rota e tabela na página do superuser",
    "status": "done",
    "justification": "A spec da APRAS-40 (8.4/10.3) pede uma tabela de histórico de mudanças de assinatura na página do superuser, mas 5.3 pinou exatamente três rotas e nenhuma devolve o histórico; a página foi entregue sem a tabela (desvio D10 aceito na code review de 2026-09-03). As linhas de subscription_change já existem e são gravadas por plan-change, cortesia e override.",
    "priority": "medium",
    "expected_results": [
      "**ER-1 — The operator-side history route exists, is superuser-only, and\n  is paginated.** `GET /api/v1/tenants/{tenant_id}/subscription/history`\n  (§2.1) answers **200** with the tenant's `subscription_change` rows ordered\n  `changed_at` **descending** (id descending as tiebreak), each row carrying\n  `kind` (one of `CONTRACTED`/`PLAN_CHANGE`/`COURTESY_GRANT`/`COURTESY_REVOKE`/`OVERRIDE`),\n  `modules_added`, `modules_removed`, `from_plan_name`/`to_plan_name`,\n  `reason`, `changed_by_id` and `changed_by_name`; honours\n  `?skip=`(`ge=0`, default 0) and `?limit=`(`ge=1,le=100`, default 50) with\n  disjoint pages whose concatenation equals the unpaginated read; answers\n  **200 `[]`** for a tenant with no subscription, **404** for an unknown\n  tenant, **422** out of range, **403** for every authenticated non-superuser\n  including a `tenant_admin` of that very tenant, and **401**\n  unauthenticated — each asserted by a named case in\n  `backend/tests/test_subscription_admin_api.py`, plus\n  `test_the_superuser_history_matches_the_tenant_side_history` proving the\n  operator body equals `GET /api/v1/subscription/history` for the same rows.",
      "**ER-2 — The superuser page renders the history, with an empty state,\n  pagination, and one Vitest case per change type.**\n  `TenantSubscriptionsPage.tsx` renders a table for the selected tenant with\n  the six columns of §5.3 (kind, added, removed, author, when, reason); it\n  renders `tenantSubscriptions.historyEmpty` and no table when the history is\n  empty; it pages with a 20-row Previous/Next control that resets to page 1\n  when the tenant `<select>` changes and re-requests with\n  `{skip: 20, limit: 20}` on Next; and the history refetches after a plan or\n  courtesy save. `TenantSubscriptionsPage.test.tsx` carries **five** cases,\n  one per `SubscriptionChangeKind`, each asserting the translated `pt.json`\n  label of that kind renders in its row, plus the empty-state, pagination,\n  page-reset and refetch-after-save cases. `en.json` and `pt.json` both gain\n  exactly the five `tenantSubscriptions.*` keys of §5.4 and the i18n key-set\n  parity case stays green.",
      "**ER-3 — The route is registered exactly like its three siblings, and\n  no parity cell moves.** The handler declares a real\n  `Depends(api_deps.get_current_superuser)` (never an in-handler\n  `is_superuser` check) and the key\n  `(\"GET\", \"/api/v1/tenants/{tenant_id}/subscription/history\")` is in\n  `UNGUARDED_ROUTES` (22 → **23**), in `GLOBAL_ROUTES` (27 → **28**) and in\n  **both** `ADMIN_ONLY_ROUTES` copies (15 → **16**, in\n  `test_permission_enforcement.py` and `test_tenant_admin.py`), while\n  `ROUTE_PERMISSIONS` stays **201**, `PERMISSIONS` stays **174**, and\n  APRAS-51's `test_permission_alignment.py` is unedited with its forms still\n  53/5/3/5/135 and its sweep still 193. **No `parity_matrix_baseline_52.json`\n  is created and `EXPECTED_CELL_COUNT` stays 1206** — the matrix measures\n  exactly `ROUTE_PERMISSIONS` and the committed recorder rejects an unmapped\n  route (§4); the accounting update is\n  `test_the_twenty_three_unguarded_routes_are_the_only_ones_excluded` (renamed,\n  `== 23`) with its stated reason, and the six legacy profiles are pinned\n  instead by `test_the_history_route_refuses_the_six_legacy_profiles`, which\n  answers **403** for all six. `parity_matrix_baseline.json`, `_40`, `_44`\n  and `_51` are byte-identical (existing sha/`_meta` cases pass unmodified)\n  and `sha256(backend/tests/matrix_world.py)` is unchanged.",
      "**ER-4 — No migration, and every gate green.** `git diff` under\n  `backend/alembic/` is empty and `test_migrations_postgres.py`'s case count\n  is unchanged; `cd backend && TEST_POSTGRES_URL=… uv run pytest` is green at\n  ≥ 90% coverage; `ruff check .` and `ruff format --check .` report no new\n  finding; `cd frontend && npm run lint && npm run build && npm run test` are\n  green with coverage ≥ the base run; `en.json` and `pt.json` have identical\n  key sets; and `AGENTS.md` carries §7's three edits (endpoint table row, the\n  *Assinatura e planos* sentence naming 23/16 with 201/174 unchanged, and the\n  `201/23` registry note) with `test_docs_agents_md.py` still green."
    ],
    "running": false,
    "blockedBy": [
      "APRAS-51"
    ],
    "created_at": "2026-09-06T12:57:42.825Z",
    "moved_at": "2026-09-07T03:37:36.230Z",
    "updated_at": "2026-09-07T03:37:36.230Z",
    "spec_path": "docs/tasks/APRAS-52-spec.md",
    "completed_at": "2026-09-07T03:37:36.230Z"
  },
  {
    "id": "APRAS-53",
    "title": "Upload de evidências e anexos de contestação no cliente de infrações",
    "status": "done",
    "justification": "QA da APRAS-44 (2026-09-06): a API aceita evidence_urls na infração e attachment_urls na contestação e o detalhe renderiza ambos, mas NewInfractionModal não tem controle de upload e ContestationForm é só corpo — a spec (10.1) descreve os dois. Reutilizar o padrão de upload já existente no sistema (documentos/ocorrências) em vez de criar outro.",
    "priority": "medium",
    "expected_results": [
      "**ER-1 — Evidência na abertura da infração, gateada pela permissão que a APRAS-51 exige.** `NewInfractionModal`, em modo *create*, renderiza um controle de anexos **quando, e somente quando,** o conjunto efetivo de `GET /permissions/me` (via `usePermissionSet()`) contém `uploads:photo_create`; cada arquivo sobe por `POST /api/v1/uploads/photo` (endpoint já existente, via o cliente já existente `api/uploads.ts::uploadPhoto`) e as `url` devolvidas entram em `InfractionCreate.evidence_urls` no submit. `InfractionDetailsView` exibe as evidências da infração, sem gate de permissão. Um caso Vitest cobre o fluxo com `api/uploads` mockado, **e** `__tests__/uploadContract.test.tsx` — mockando somente `api/client` e a persona — prova que o cliente real põe no fio `/uploads/photo` com `entity_type = \"INFRACTION\"` e `body.has(\"entity_id\") === false`. Os cinco fatos do servidor (5 MiB, os três MIME, `INFRACTION`, a rota e `uploads:photo_create`) são fixados por um **pino de duas pontas**, na forma que o repositório já usa em `lotSelectContract.test.tsx` ↔ `test_infractions.py`: o arquivo frontend declara cada constante como literal local, compara com o que `api/uploads.ts` exporta e **nomeia** o gêmeo; e o gêmeo — um caso pytest novo em `backend/tests/test_uploads.py` — afirma `media_service.MAX_FILE_SIZE`/`ALLOWED_MIME_TYPES`, `EntityType.INFRACTION == \"INFRACTION\"`, `ROUTE_PERMISSIONS[(\"POST\", \"/api/v1/uploads/photo\")] == \"uploads:photo_create\"` e, percorrendo o `dependant` da rota viva, que a assinatura de `upload_photo` carrega `PermissionRequired(\"uploads:photo_create\")` (a asserção da precondição APRAS-51). **Nenhum builtin do Node aparece em `frontend/src/`**, e `npm run build` (`tsc -b` com `types: [\"vite/client\"]`) sucede.",
      "**ER-2 — Anexos na contestação, com o ramo negado provado.** `ContestationForm` renderiza o mesmo controle, com `entity_id` = id da infração, e preenche `ContestationCreate.attachment_urls`; `InfractionStageTimeline` passa a exibir `attachment_urls` das entradas do histórico (hoje nenhuma linha os renderiza), somente-leitura. O teste de contrato exercita **os dois ramos com personas nomeadas**: um morador que contesta com `[\"infractions:contest\", \"infractions:my_lots_read\", \"uploads:photo_create\"]` vê `attachment-file-input` e sobe o anexo; o mesmo morador **sem** `uploads:photo_create` vê `attachment-unavailable` com a frase pt/en de `infractions.attachments.permissionRequired`, não vê `attachment-file-input`, faz **zero** chamadas ao endpoint, e **ainda assim** submete a contestação por texto com `attachment_urls: []`.",
      "**ER-3 — Nenhuma rota nova e nenhuma migração; os limites e a recusa são os do endpoint pós-51, e a UI os mostra.** `git diff --stat -- backend/app backend/alembic` do PR é **vazio**: nenhuma rota, schema, serviço, model ou migração muda. As únicas mudanças fora de `frontend/` são o caso pytest novo em `backend/tests/test_uploads.py` (o gêmeo de ER-1, §3.6(c)) e a nota de documentação em `AGENTS.md` (§3.8) — `git diff --name-only -- backend/` lista **exatamente um** arquivo, e ele está sob `backend/tests/`. Tamanho (5 MiB) e tipos (`image/jpeg`, `image/png`, `image/webp`) são os de `media_service.MAX_FILE_SIZE`/`ALLOWED_MIME_TYPES`, e o pino de duas pontas falha — de um lado ou do outro — se cliente e servidor divergirem. As três recusas do servidor — HTTP 400 `{\"detail\": \"Arquivo excede o limite máximo permitido de 5MB.\"}`, HTTP 400 `{\"detail\": \"Formato de imagem inválido. Formatos aceitos: JPEG, PNG, WebP.\"}` e, pós-51, HTTP 403 `{\"detail\": \"The user doesn't have enough privileges\"}` de `PermissionRequired` — são renderizadas inline pelo `parseApiError` do módulo, sem `if` por status, provado por um caso por corpo que rejeita a promise do upload.",
      "**ER-4 — Gates verdes e a recomendação documentada.** `npm run test:coverage` passa com os thresholds de `vitest.config.ts` inalterados (nunca abaixados) e com as métricas **medidas antes e depois** registradas no relatório do PR, sendo as de depois ≥ as de antes; `npm run lint` sem finding novo; `npm run build` sucede; `i18n/__tests__/parity.test.ts` verde com pt/en em paridade e as contagens de 27/94 intocadas; a tabela de bundle recomendado do `AGENTS.md` ganha a linha `uploads:photo_create` (✓ para ADMINISTRATOR/DIRECTOR/MANAGER/RESIDENT) e a frase que a justifica, com os ✓ declarados como recorte do fluxo de infrações; e a suíte do backend roda verde com `TEST_POSTGRES_URL` definido (`uv run pytest`, gate de 90%), **incluindo o caso novo** `test_the_upload_contract_matches_the_infraction_uploader` de `backend/tests/test_uploads.py` — o único arquivo de `backend/` no diff, e nenhum arquivo de produção do backend entre eles."
    ],
    "running": false,
    "blockedBy": [
      "APRAS-51"
    ],
    "created_at": "2026-09-06T12:57:42.843Z",
    "moved_at": "2026-09-07T12:55:36.547Z",
    "updated_at": "2026-09-07T12:55:36.547Z",
    "spec_path": "docs/tasks/APRAS-53-spec.md",
    "spec_iterations": 2,
    "last_review_findings": [
      "B1 (BLOCKING) §3.6(b)/ER-1/ER-4 - the disk-read assertions (frontend test reading backend files via node:fs/import.meta.url) are incompatible with `npm run build` (tsc -b type-checks src/ with types: [vite/client] only; nothing in src/ imports node builtins) and depart from the repo's stated convention (lotSelectContract.test.tsx: 'neither side can import the other across the language boundary, so each states the constant and names the other', with the backend half at test_infractions.py:1199 and test_module_vocabulary.py:83-133). DECISION: follow the convention - the frontend contract test states the constants (5 MiB, the three MIME types, entity_type INFRACTION, the route and the permission string) and names its backend twin; add the backend twin case (a pytest case asserting media_service.MAX_FILE_SIZE / ALLOWED_MIME_TYPES, EntityType.INFRACTION, ROUTE_PERMISSIONS[('POST','/api/v1/uploads/photo')] == 'uploads:photo_create' and, post-51, the dependency on the handler) in backend/tests/test_uploads*.py or test_infractions.py; no node builtins in frontend/src; ER-3's empty-diff claim narrows to backend/app and backend/alembic (backend/tests gains the twin), and §1's deliverable table lists it.",
      "NON-BLOCKING, apply all seven: expect(body.has('entity_id')).toBe(false); the require_permission presence check moves to the backend twin (handler signature slice); Metade 1 needs rules prop + /lots/ and /lots/{id}/residents stubs to reach submit; QueryClientProvider in both test files (useUploadPhoto is a useMutation); interpolation variables for the `hint` key; §7 gates name TEST_POSTGRES_URL for symmetry with APRAS-51; §3.8's AGENTS.md row scoped to the infractions flow (say why PORTEIRO is excluded or include it consistently with permissionFixtures.ts)."
    ],
    "completed_at": "2026-09-07T12:55:36.547Z"
  },
  {
    "id": "APRAS-54",
    "title": "Quitar a dívida de lint pré-existente do backend e tornar ruff um gate de CI",
    "status": "in_progress",
    "justification": "ruff check backend reporta ~1150 findings pré-existentes (FAST001/FAST002/DTZ003/B008/DTZ011/PLC0415/I001/E402 entre outros), o que obriga cada tarefa a provar 'zero findings novos' por diff em vez de 'ruff limpo'. Os developers já omitem response_model= para não disparar FAST001. DTZ003 (utcnow ingênuo) aparece em módulos de auditoria datada (infrações, assinatura). Registrado nos suggestions-log das APRAS-47, 49, 40 e 44.",
    "priority": "low",
    "expected_results": [
      "**ER-1 — `cd backend && uv run ruff check .` exits `0` with `All checks passed!`, and `uv run ruff format --check .` reports zero files to reformat**, under the project's own `pyproject.toml` (`select = [\"ALL\"]`, line-length 88) with the pinned `ruff==0.16.5`. The global `[tool.ruff.lint].ignore` list gains **no new entry**. Silencing happens only through (a) the three documented `per-file-ignores` blocks of §4.1/§4.5 — `alembic/versions/**`, `tests/**` (extended with `PLC0415, SLF001, S603, S607`), and `app/api/v1/endpoints/** = [\"A002\"]` — each with its reason in a comment, and no file-wide entry added for a rule with a single occurrence (`S107`, `PT011`, `BLE001` and the `tests/` `A002` take per-line `# noqa`s instead); and (b) per-line `# noqa: <CODES>  # <reason>`, every one of which carries codes and a reason and matches `#\\s*noqa:\\s*[A-Z]+[0-9]+(\\s*,\\s*[A-Z]+[0-9]+)*\\s+#\\s+\\S`. The total `# noqa` count under `backend/app/` + `backend/tests/` (base **94** at d39ff1c: app 40, tests 54 — re-measured at the branch point per §3.2) is **≤ 108**, is pinned as `NOQA_CAP` in `backend/tests/test_lint_hygiene.py` and asserted there, and the PR body states base → landed with a per-code itemization of every `# noqa` added and removed.",
      "**ER-2 — every clock read in `backend/app/` is served by one helper, `backend/app/core/clock.py`, and the stored instant is unchanged.** `DTZ001/DTZ003/DTZ005/DTZ011` report **0**, and `backend/app/core/clock.py` is **the only module under `backend/app/**` whose source text contains any of the literals `datetime.utcnow`, `date.today(` or `datetime.now(` — the last with *any* argument, `datetime.now(UTC)` included** (asserted by `backend/tests/test_lint_hygiene.py` as a text scan over every `app/**/*.py`; the single permitted occurrence is `utc_now`'s own `return datetime.now(UTC)`). This covers all **20** tz-aware sites measured at d39ff1c — `models/task.py` 1, `models/purchase.py` 5, `models/asset.py` 3, `services/purchase_service.py` 6, `services/asset_service.py` 3 become `clock.db_now`, and `core/security.py`'s 2 JWT sites become `clock.utc_now()`. **No migration is added and no column type changes** — the columns stay `TIMESTAMP WITHOUT TIME ZONE`, and `db_now()` returns the naive UTC value they already hold (today those aware writers are coerced to the same naive UTC value on write), so a row written before this task and one written after are directly comparable. A new `backend/tests/test_clock.py` proves this **once per model** — `Asset`, `PurchaseRequest` and `Task`: write through the service, read back, assert `tzinfo is None` and within one second of `datetime.now(UTC).replace(tzinfo=None)`. The PR body carries before/after response bodies for `POST /api/v1/tasks/`, `POST /api/v1/assets` and `POST /api/v1/purchase-requests` showing identical timestamp fields with no `+00:00` on either side. `tests/test_migrations_postgres.py` runs green against a real `TEST_POSTGRES_URL`, all cases, **none skipped**.",
      "**ER-3 — no behaviour changed, and the six slices are evidenced one by one.** The full suite passes with the **same passed/failed/skipped counts** as the pre-task baseline (**3135 tests collected** at d39ff1c), the only permitted delta being the cases of the two new files (`tests/test_clock.py`, `tests/test_lint_hygiene.py`), and **coverage ≥ the baseline percentage**, `--cov-fail-under=90` holding. The five frozen artefacts (`parity_matrix_baseline.json`, `_40`, `_44`, `_51`, `legacy_role_bundles.json`) are **byte-identical** — `git diff --stat master...HEAD -- backend/tests/data/` is empty and the `sha256sum` list matches the base worktree's line for line. `diff` of `app.openapi()` dumped with `sort_keys=True` before and after is **empty** (157 paths, 735 480 bytes at d39ff1c), despite FAST001 rewriting every `response_model=`/return-annotation pair. `git diff --stat master...HEAD -- frontend/` and `git diff --stat master...HEAD -- backend/alembic/versions/` are both **empty**. The PR body carries §5.0's **seven-row per-slice table** (branch-point base + six slices), each row giving file count, pytest passed/failed/skipped, coverage %, `ruff check` residual, OpenAPI diff empty/non-empty and artefact hashes unchanged/changed.",
      "**ER-4 — CI fails on any finding, and it is written down.** `.github/workflows/ci.yml` carries a `backend-lint` job running `ruff check .` and `ruff format --check .` from `backend/` with no `continue-on-error` and no `--exit-zero`; a run triggered with `gh workflow run ci.yml --ref <branch>` shows it green on the finished branch, and a deliberately reintroduced finding (e.g. an unsorted import, pushed and then reverted, or reproduced locally) turns it **red**. `ruff==0.16.5` is an exact pin in `backend/pyproject.toml`'s dev group and present in `uv.lock`. `AGENTS.md` states the gate in its CI/CD list, the pre-commit command, the `# noqa: CODE  # reason` convention with its cap, that `alembic/versions/**` is never reformatted, and that `app/core/clock.py` is the only clock."
    ],
    "running": true,
    "blockedBy": [
      "APRAS-50",
      "APRAS-51",
      "APRAS-52",
      "APRAS-53"
    ],
    "created_at": "2026-09-06T12:57:42.858Z",
    "moved_at": "2026-09-07T12:55:36.570Z",
    "updated_at": "2026-09-07T12:56:14.199Z",
    "spec_path": "docs/tasks/APRAS-54-spec.md",
    "spec_iterations": 1,
    "last_review_findings": [
      "B1 (BLOCKING) §4.4/§6/ER-2 - 17 of 20 tz-aware writers (datetime.now(UTC) / now(timezone.utc)) are unaccounted for: app/models/purchase.py (5), models/asset.py (3), services/purchase_service.py (6), services/asset_service.py (3) write AWARE values into naive columns, invisible to ruff. DECISION: slice 3 covers ALL 20 sites: every datetime.now(UTC)/now(timezone.utc) that feeds a column becomes clock.db_now() (aware->naive, same instant), security.py's JWT sites become clock.utc_now(); add a substitution-table row per shape, name purchases and assets in §6 with a risk row, and require the same captured before/after response-body evidence for the purchase/asset endpoints that get_utc_now already gets. State explicitly that today those sites persist aware values that psycopg2 coerces to naive UTC on write, so db_now() preserves the stored instant (prove it with one read-back test per model).",
      "B2 (BLOCKING) §4.5 vs ER-2 - the clock guard is defined two ways ('contains datetime.now(' vs 'bare datetime.now('). DECISION: Reading A, literal substring: clock.py is the only module under app/** whose source contains `datetime.utcnow`, `date.today(` or `datetime.now(` (any argument) - stated identically in §4.5 and ER-2, enforced by test_lint_hygiene.py with an AST/text scan; consistent with B1 (all 20 sites migrate).",
      "NON-BLOCKING, apply all seven: OpenAPI byte count 735480; noqa base 94 (SLF001 7) - fix the cap arithmetic; remark seed.py's local->UTC shift; name APRAS-44's two-clock comment in tests/test_infractions.py:44-70 and defense_due_on; narrow the four single-site tests/** ignores to per-line noqa or file-scoped entries; `git diff master...HEAD`; require per-slice evidence (test count, coverage, artefact shas, OpenAPI diff) recorded per slice in the report, not implied. Also: backend/CLAUDE.md does not exist - remove the reference."
    ]
  },
  {
    "id": "APRAS-55",
    "title": "Reorganizar navegação com menu lateral colapsável agrupado por áreas funcionais",
    "status": "done",
    "justification": "Reorganização completa da navegação: menu horizontal de 30 itens substituído por menu lateral colapsável (Sidebar) agrupado em 7 áreas funcionais (Operações, Acessos & Segurança, Comunidade & Convivência, Infrações & Regras, Cadastros, Financeiro & Planos, Sistema). Suporte a desktop expandido (w-64) e colapsado (w-20 com tooltips), persistência em localStorage, drawer responsivo no mobile acionado por menu hambúrguer, e barra superior limpa com tenant switcher, controles de simulação, idioma, perfil e logout. Aprovado em spec_review, code_review e QA. 100% dos testes passando (144 arquivos, 1324 testes).",
    "priority": "medium",
    "expected_results": [
      "ER-1: The horizontal flat list of 30 navigation links is removed from Navbar.tsx, replaced by a responsive lateral sidebar (Sidebar.tsx) grouped into 7 functional areas (NAV_GROUPS).",
      "ER-2: All 30 navigation routes from NAV_ITEMS are mapped into NAV_GROUPS with zero omissions and zero duplicates, preserving the original array declaration order and ROUTE_ACCESS entries of NAV_ITEMS.",
      "ER-3: On desktop (≥ 768px), the sidebar supports expanded (w-64) and collapsed icon-rail (w-20) modes, toggled via UI button and persisting state in localStorage under key \"apras_sidebar_collapsed\".",
      "ER-4: On mobile (< 768px), the sidebar operates as an off-canvas drawer opened via a hamburger toggle button in the header and closed by link selection, backdrop click, or close button.",
      "ER-5: Functional groups containing zero accessible items for the active user/simulated profile are automatically omitted from the rendered DOM without orphan headers or dividers.",
      "ER-6: The top header (Navbar.tsx) retains the tenant switcher combobox, role simulation controls, language switcher, user info, and logout button, adjusting its margin dynamically to match desktop sidebar expansion state.",
      "ER-7: Active routes are highlighted visually (bg-primary/10 text-primary and vertical indicator pill) and all group titles and menu controls support both pt and en locales.",
      "ER-8: All existing frontend unit and integration tests pass without regression (144 test files, 1319+ tests)."
    ],
    "running": false,
    "blockedBy": [],
    "created_at": "2026-09-07T14:54:00.848Z",
    "moved_at": "2026-09-07T15:57:23.325Z",
    "updated_at": "2026-09-07T15:58:48.223Z",
    "spec_path": "docs/tasks/APRAS-55-spec.md",
    "last_review_findings": [],
    "completed_at": "2026-09-07T15:57:23.325Z",
    "resume_context": "Interrupted mid-dispatch 2026-09-07T15:58:48Z; git master staged:0 modified:3; latest report .meridian/reports/APRAS-55-qa_review-1.md. Establish actual state with git status and git diff before writing anything."
  },
  {
    "id": "APRAS-56",
    "title": "Criar Dashboard Geral e migrar tarefas para /tasks",
    "status": "done",
    "justification": "Migração da tela de Tarefas de /dashboard para /tasks concluída, com redirecionamento de compatibilidade em /dashboard. Criado o novo Dashboard Geral (GeneralDashboardPage) em / para usuários autenticados, com banner de boas-vindas e cards de atalhos rápidos para módulos ativos. Adicionado item Início no topo do menu lateral com ícone Home. Permissões, rotas e testes unitários/integração 100% validados (145 arquivos vitest, 1.331 testes passando; 3.085 testes backend passando com 98.63% de cobertura). Aprovado por spec_review, code_review e QA.",
    "priority": "medium",
    "expected_results": [
      "ER-1: The task management view is migrated to /tasks in App.tsx, NAV_ITEMS, and NAV_GROUPS under the Operations group, and accessing /dashboard seamlessly redirects to /tasks.",
      "ER-2: A new General Dashboard component (GeneralDashboardPage.tsx) is created and rendered at / for authenticated callers, featuring a welcome banner with user/tenant context and responsive shortcut cards for active modules.",
      "ER-3: The lateral Sidebar.tsx includes an \"Início\" / \"Home\" navigation entry pointing to / with the Home icon, properly supporting expanded, collapsed, and mobile drawer states.",
      "ER-4: RootRedirect preserves role-specific landing paths (/gate for PORTEIRO, /welcome for GUEST) when accessible, while rendering the General Dashboard directly at / for standard authenticated users.",
      "ER-5: ROUTE_ACCESS correctly defines rules for /tasks and /dashboard, and routeAccess.test.ts validates 100% coverage across all 30 navigation items in NAV_GROUPS without drift or omissions.",
      "ER-6: Backend LANDING_PATHS in role.py and frontend RoleDetailPage.tsx support /tasks and / without validation errors.",
      "ER-7: Complete internationalization support for \"nav.home\" and \"dashboard.general.*\" in both pt.json and en.json with identical key-set parity.",
      "ER-8: All frontend and backend unit, integration, and smoke tests pass without regressions."
    ],
    "running": false,
    "blockedBy": [],
    "created_at": "2026-09-07T16:24:12.292Z",
    "moved_at": "2026-09-07T18:38:28.349Z",
    "updated_at": "2026-09-07T18:38:53.826Z",
    "spec_path": "docs/tasks/APRAS-56-spec.md",
    "completed_at": "2026-09-07T18:38:28.349Z",
    "resume_context": "Interrupted mid-dispatch 2026-09-07T18:38:53Z; git master staged:0 modified:3; latest report .meridian/reports/APRAS-56-qa_review-1.md. Establish actual state with git status and git diff before writing anything."
  }
]
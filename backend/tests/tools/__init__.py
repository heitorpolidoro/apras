"""Committed generators that support the test suite (IAM F2, APRAS-46 §6.4).

Not a test package: nothing here is collected by pytest. It exists so the
parity baseline has a *reproducible* producer rather than a historical one.
"""

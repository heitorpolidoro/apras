# Copy to .env before starting: the app reads its secrets from it.
#
# POSTGRES_URL is set in docker-compose.yml for the bundled database; override it
# here only to point the backend somewhere else.
